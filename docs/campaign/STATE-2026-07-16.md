# Session state — 2026-07-16 (read me first)

Handoff/compaction anchor for the review-fix campaign. **Branch `campaign/2026-07-12-skyfall`.
Everything is COMMITTED. NOTHING is pushed/merged — production is still Infinite Sands.**
The push is held for Zach's review + his "ship it".

## The one big lesson (drives everything below)
Fable's audit: *"every failure that got a machine gate stopped recurring; every failure that
got only a prose rule recurred."* This session proved it twice over — and also proved the
inverse: **a gate that measures the wrong thing is worse than no gate**, because it launders
a bug as verified.

Gates that LIED this session (all now fixed):
- `open-end` flagged the leviathan's **entrance** as a defect → a pass dutifully bricked up
  every bore, sealing the interior. Fixed: builders declare `userData.intendedOpening`.
- `seam` + `leviathan-walk` passed on a **hermetically sealed** interior — the walk gate
  TELEPORTS waypoints, and they were sitting in **mid-air** (green on an unclimbable ramp).
  Fixed: NEW `walkin` check derives its own verifiably-outside point and marches a
  player-radius sphere on real floors vs real colliders.
- `verify:solid` collision was **vacuous** for spawn-time assets (Rapier's QueryPipeline only
  rebuilds on `world.step()`). Fixed.
- The storm rig graded at exposure 1.35 (game ships 1.05) and sampled the player's feet
  instead of the fogged distance. Fixed.

**Zach's playtest caught what every gate passed, three times running. Trust the report over
the gate.**

## Done + committed (this campaign)
- **verify:solid harness** (`scripts/verify-solid.mjs`, docs in `docs/verify-solid.md`) — 7
  checks: thin / backface / open-end / floating / collision / seam / **walkin**. Self-test
  7/7 (non-vacuous). `npm run verify:solid[:selftest]`, `--asset=<name>|all`.
- **Boneyard**: sandworm-skeleton redesign (long/low/sunken), see-through fixed (inverted
  `sweptTube` side-winding), thinner ribs (1.34→0.85), jagged snapped fractures, weathered
  decay + fallen halves paired to stumps, greyer texture, blob scatter removed.
- **Skyfall**: solid entrance edges (inward-wound rim cap → `solidInner` lip), exterior
  trimesh collision, seated floaters, floor z-fight.
- **Leviathan**: REAL walk-in entrance (flank `HullCut`), terrain-derived sand drift,
  exterior collision, bow-trimesh-in-hold fixed, lighting lifted. All 7 checks green.
- **Storm**: colour-space bug fixed (raw ShaderMaterial gets no `<colorspace_fragment>` →
  sky rendered linear (96,45,13) vs fog sRGB (165,116,63); that 69-unit gap WAS the yellow
  silhouette). Wall de-spun + fade fixed so it washes over you. Silhouette gate: delta=0.
- **Bones + worm night fix**: emissive/unlit constants now driven by `ctx.time.sunHeight`.
- Docs synced; Fable audit + roadmap in `docs/campaign/audit-and-roadmap-2026-07-15.md`;
  feedback capture in `feedback-and-ideas-2026-07-15.md`.

## QUEUE (next, in order) — see task list #28/#29/#30
1. **#30 hab_dome is SEALED** (`walkin` 0/16, pre-existing) — `--asset=all` stays RED until
   fixed. Same fix pattern as leviathan.
2. **#28 Skyfall stern seam** — hairline gap at the stern fracture rim. Likely the SHARED
   `solidInner` lip weld → the fore mouth + leviathan have it too (hidden). Fix in
   `wreckForms.ts`, not a stern patch. NOTE: no gate catches inter-mesh sliver gaps
   (open-end is per-mesh topology) — consider a daylight-leak check.
3. **#29 Boneyard scatter overhaul** — scatter still uses the old lighter `_boneMat` + the
   ring-y `placeRibcage`; bring to hero quality (material, real bones, variety) + a
   **day/night render check** (2 lighting-invariant bugs found: bones, worm — no gate covers
   that class).

## Owed to Zach (feel/ear — machines can't judge)
Storm **wind loudness** (never verified headlessly) + wash-over feel + the wall's billows read
**vertically streaky/flame-like**; boneyard walk-under feel + rib gauge at distance; leviathan
drift climb feel + aft console still dark; **sprint toggle untested as input**; stamina's final
role (no longer gates anything); POI density.

## Known gaps / decisions
- **Crash wreck ≠ Skyfall** — deferred with a 5-step plan (save-schema + determinism risk).
- **Leviathan entrance is a FLANK tear**, not the fracture — the fracture is geometrically
  impossible (the 21m reared bow hinges at it and engulfs it). Zach should sanity-check that
  this reads as "one seamless entrance".
- Harness boot flakiness: 2-4 attempts/run; `npm run reap` helps; internal retry is 1/1.
- **Never `git stash` in this worktree** (it clobbered a concurrent agent). **Run agents ONE
  AT A TIME** — concurrent agents on the shared tree caused a real incident.
- Untracked probe litter, safe to delete: `scratch-baseline/`, `scripts/_scenecheck.mjs`,
  `_vultcheck.mjs`, `_skyfall_diag.mjs`, `_lev_diag.mjs`, `_lev_reach.mjs`, `_lev_walkdiag.mjs`,
  `_pngpix.mjs`, `_stormpix.mjs`, `_stormseq.mjs`, `_stormheading.mjs`.

## After the queue
Push (on Zach's OK) → **Phase 2 "Scavenger's Economy"** (crafting materials → loot variety →
panels) per Fable's roadmap. Then mega dunes + ridable sled → cave system → character/MP
(needs the GDD multiplayer contradiction resolved first).
