# Plan — 2026-07-17 (overnight run + forward roadmap)

Written attended, before Zach's overnight. Supersedes the "NEXT" pointer in
`audit-and-roadmap-2026-07-15.md` (Phase 0 shipped; Phase 1 all but done).
Read `STATE-2026-07-16.md` for the gate-lessons that govern how this runs.

## Where we are (2026-07-17)
- **Phase 0 SHIPPED** — the whole `campaign/2026-07-12-skyfall` branch is merged to `master`
  and LIVE (auto-deploy). Skyfall freighter, leviathan, boneyard + ribcage, 3 POI archetypes,
  vultures, vignettes, storm overhaul, POI spacing, toggle-sprint, and the `verify:solid`
  harness (green on all 4 hero assets after the hab_dome unseal).
- **Phase 1 "SOLID" ~90% done** — harness ✓, worm ✓, hab dome ✓, leviathan ✓, storm ✓, Skyfall
  bulk ✓, sprint toggle ✓. **Only #28 (Skyfall stern seam) + #29 (boneyard scatter) remain.**

## Decisions locked tonight (Zach)
- **Crafting stays BENCH-FREE** (press-C, recipe cards, inputs consumed anywhere). New materials
  deepen recipes; the interaction model does NOT change.
- **Do NOT bake a fixed material list overnight.** Research crafting-improvement ideas first
  (survival-sim taxonomies + how to make the *current* crafting better), then propose a **leaner**
  material set (~4-5) + recipe/drop matrix + UX improvements → **morning approval before any
  ItemId is baked.**
- **Run all three fork research digests** (multiplayer, character pipeline, cave/D254) — decision
  support only, zero build commitment.
- **Budget ceiling: 6M tokens** (hard cap; loop stops cleanly when hit).
- Carried from the 07-15 review (already applied): leviathan entrance = front opening + lifted
  lighting; boneyard = ribcage-only (no skull); storm = total whiteout; stamina = keep the stat
  for tow/melee, no sprint drain, keep sprint-thirst; POI density = leave at the Phase-1 value.

## Tonight's overnight run — Campaign "SCAVENGER'S ECONOMY (setup)"
Ordered so nothing balance-sensitive is decided while Zach sleeps. Each unit gates green +
commits; the run PAUSES at the economy proposal for morning approval.

1. **#28 Skyfall stern seam** — hairline gap at the stern fracture rim. Likely the SHARED
   `solidInner` lip weld in `wreckForms.ts` (so the fore mouth + leviathan have it hidden too) —
   fix at the source, not a stern patch. No gate catches inter-mesh sliver gaps yet; add a
   daylight-leak check if cheap. Gate: `verify:solid --asset=skyfall` + `skyfall-walk`.
2. **#29 Boneyard scatter overhaul** — scatter still uses the older lighter `_boneMat` + ring-y
   `placeRibcage`. Bring to hero quality: match the darker hero bone material, real bones via the
   ribcage vocabulary (not rounded rings), cracked/jagged variety, more skeleton types. **Add a
   day/night render check** (the class of bug that bit twice — bones + worm — has no gate). This
   is hero-ish: iterate build→shoot→critique 5-8 rounds; do NOT ship on tsc alone (rule 8).
3. **Research swarm (parallel, cheap Haiku)** — four digests to `docs/research/`:
   - `crafting-improvements` — Long Dark / Rust / Subnautica material+recipe taxonomies AND
     concrete ways to improve the current bench-free system (recipe depth, discovery, material
     identity). Feeds Unit 4.
   - `multiplayer-architecture` — co-op over the seed+descriptor world; Colyseus vs P2P/relay;
     state sync (chunkDiffs/creatures/physics authority). (Fork C3.)
   - `character-pipeline` — procedural-rig ceiling-push vs imported rigged glTF (breaks D1/D107).
     (Fork C2.)
   - `cave-feasibility` — cave-gen methods vs Rapier heightfield constraints; the D254 open
     question (can the KCC walk BELOW the heightfield sheet?). Flags the 10-min attended spike.
4. **Economy PROPOSAL (then PAUSE)** — synthesize Unit-3 crafting research into
   `docs/campaign/economy-proposal.md`: a leaner material set (~4-5, building on existing
   `scrap`), a 1-page recipe/drop matrix (per-POI loot identity: refinery→chem, hab→wiring, tank→
   metal, …), and crafting-UX improvements. **No ItemIds baked; no loot tables changed.** Set
   `awaiting_approval` → the loop stops here for Zach's morning sign-off.

**Optional (only if budget + time and clearly content-preserving):** the registry-unification
refactor — merge the 3 ad-hoc loot systems (`lootContainers.rollLoot`, `salvage.TABLES`,
`COMPONENT_LOOT`) into ONE data-driven registry, **preserving current ids + drops exactly** (pure
plumbing, `craft-unlock` gate proves loot still rolls the same). Adding materials later becomes a
data edit. Skip if it would pre-empt the material decision.

**Morning:** approve/edit the economy matrix → the build half (bake ItemIds, wire recipes, ground
loot, panel/lootable variety) runs as the next campaign, fast, against an approved spec.

## Forward roadmap (the big picture — condensed from Fable's, updated)
Effort: S ≤1 cycle · M 2-4 · L a ladder (5-10) · XL multi-campaign.

| Phase | Campaign | What | Overnight-safe? |
|---|---|---|---|
| ~~0~~ | ~~Ship~~ | ~~merge + deploy~~ | ✅ DONE 07-17 |
| 1 | SOLID | harness + fix pack | ✅ all but #28/#29 (in tonight's run) |
| **2** | **Scavenger's Economy** | materials → recipes → ground loot → panel/lootable variety | **split** — system yes, balance = morning gate |
| 3 | The Deep Desert | mega dune biome **+** ridable scrap sled (paired: crest a dune, sled down) | core yes; dune scale + ride feel = walk-test |
| 4 | Underworld | real multi-chamber cave for the egg | ❌ needs the D254 KCC spike + design approval first |
| 5 | The Forks | character model/rig/anim **then** multiplayer | ❌ decisions + GDD re-anchor first |

**Why this order:** fix before add (Phase 1); economy before more world (a material economy
multiplies every existing POI; terrain only adds); pair the sled with dunes (neither is a loop
verb alone); caves after the economy (the cave earns danger when it holds loot); forks last
(they're gated on decisions, not tokens — so research them cheaply now).

## Attended decision sheet (NOT overnight — these need you, later)
| Decision | Blocks | Notes |
|---|---|---|
| Approve the economy material/recipe/drop matrix | Phase 2 build | The morning pause from tonight's run. |
| **Audio de-cute (C1)** — a listen session enumerating the "cute/beepy" offenders + wind/music targets | audio work | By design human-in-the-loop — a gate-green audio bed (D283) already got vetoed once. Cheap + high tone payoff; schedule early. |
| Dune-sea blockout vista (height/wavelength/emptiness); sled ride-feel veto | Phase 3 | Blockout + feel checkpoints. |
| The 10-min D254 KCC-under-terrain spike; then cave gen-method + scope | Phase 4 | Do the spike on any review day — it's 10 min and unblocks the whole cave campaign. |
| **GDD re-anchor** (`/interview-vision`): is multiplayer now IN? | Phase 5 | The GDD currently *forbids* MP ("solo by design"). This reverses a written pillar — do it deliberately. |
| MP scope (2-4 co-op vs many) + infra (server vs P2P) + authority; character fork (procedural vs imported glTF) | Phase 5 | If MP lands as small co-op, the current procedural rig may ship it — decoupling the two XL forks. Tonight's research informs both. |
