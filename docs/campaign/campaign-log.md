# Campaign log — Dustfall (`campaign/2026-06-18`)

Append-only human chronicle, one entry per cycle. Your async review surface — read this (or
`/campaign-status`) anytime to see what each cycle did. Redirect via `steering.md`.

---

## Cycle 0 — campaign started · 2026-06-18 · `plan-review` pause

**Goal:** build out the remaining roadmap + backlog toward the GDD vision (lone-survivor
procedural desert; depth + finish, not new vision) until `--until=roadmap-empty`.

**Policies:** checkpoint=**milestone** · self-author=**propose** · visual-gate=**auto** ·
verify gate=**`npm run verify:all`** · branch=**`campaign/2026-06-18`** (commit every cycle).

**Budget:** max-cycles **12** (hard stop, this first run) · no soft token ceiling
(calibrate per-cycle spend after Milestone 1, then `--resume` with a higher cap).

**Baseline verify (`npm run verify:all`):** ✅ GREEN — tsc clean · `verify:placement`
0 bury-audit fails ×5 seeds (81/79/79/84/79 panels) · `verify:colliders` 0 coverage fails / 25 audits.

**Preconditions:** GDD present (§12 scope-cut authored here via `--plan-first`) · baseline green ·
destructive-action guard installed (`pre-bash-destructive-guard.mjs`, lock set) · max-cycles set ·
working tree clean. All met.

**Plan (`--plan-first`):** authored via a 6-domain backlog analysis (51 work-units) + a
coverage/GDD-compliance audit. Laid out the full ordered, milestone-grouped queue:
- **Phase A (BUILD-NOW, traversed unattended):** M1 Wreck-arc finish (calibration) → M2 Wreck
  breadth + infra gate → M3 The worm made right → M4 Critters + atmosphere → M5 Riding & rest feel.
- **Phase B (DESIGN-GATE, surface a proposal + pause):** M6 Survival & UX → M7 Wreck depth & new
  POIs → M8 Deep cave & companion → M9 Architectural-risk physics → M10 Big features & tools.
- **Excluded (GDD §11/§14):** base-building, multiplayer + char-customization. Deferred: real-PBR,
  WebGPU, Electron, dynamic-POI-gen, PARKED design-call items.
- Milestone headings written to `roadmap.md`; full per-unit detail in
  `iteration-plan.md` "Campaign 2026-06-18 — milestone ladder"; scope-cut order in GDD §12.

**Audit caught (folded into the plan):** companion-egg hard-gated behind the deep cave (M8, not
M4) · real-cloth depends on real-rope, both behind feature-flags-infra · sled-feel-tune scoped to
the existing tow/slope, NOT riding (riding is the M9 spike) · sarlacc-lure must bake the §11
awe-not-horror guardrail.

**Plan-review amendments (user steering + an evidence audit, 2026-06-18):**
- **Dropped** `sled-mechanics-feel-tune` (M5) — the slope-slide tune already shipped ACU (GAIN 6→2.5).
- **Added** `remove-wreck-sand-mounds` (M1, build-now) — strip the `makeSandMound` drifts around wrecks;
  **supersedes/drops** the §G sand-integration items (wrecked_tank sand-swallow, satellite/debris drifts, D236 banking).
- **Promoted to build-now** `sarlacc-lure-ambush` (M3, user opted in — awe-not-horror baked in) and the full
  §F/§G `wreck-polish-bundle` (M2 — non-axial mass, weathering chroma, engine droop, scout trauma, scale-anchor pocket).
- **Deferred** the §D player-model optional work (PM-S.3 / PM-E / lighting-mood / sled-on-back).
- **Dropped (won't-do)** the 5 PARKED items (hermit NPCs, salvage-durability, key-card panels, weld-kit, machete-loot).
- **Reframed as tunes, not rebuilds** (audit ground-truth): storm-sway folded into `atmosphere-feeltunes`
  (built ACW #134); `3p-camera-and-render-polish` → open sub-items only (cameraSnap + walk-cycle already
  shipped); `viewmodel-nits` → just the 3P torch flame (FP night-lighting shipped D174).

**Status:** ⏸ PAUSED at `plan-review` — awaiting `/campaign-approve` of this (amended) plan before
Cycle 1 (Milestone 1) starts. Build begins on approval + `/loop /campaign-cycle`.

---

## Cycle 0 addendum — net-new direction ideation (2026-06-18)

User asked what else is worth developing beyond the backlog. A vetted ideation pass (5 GDD-grounded lenses
→ 23 proposals → anti-feature + value/vision vetting; both vetters converged on the same 4 core-levers)
surfaced 3 net-new arcs the GDD weights heavily but ships thin: **C1 water & exposure · C2 exploration pull ·
C3 the Long-Storm endgame arc** (+ a lower-priority tone/ecology layer). Captured as **Phase C (PROPOSED)**
in `iteration-plan.md`. **Promotion decision (user, 2026-06-18):** **C2 exploration + `sun-shade-exposure` →
new build-now Milestone M5a**; **tone/ecology layer → M5b**. **Deferred** the rest of C1 (wreck-water-tanks,
wind-chill, condensation-still — "not for now"). **C3 endgame NOT pursued** — the user does not want an
endgame; the game stays open-ended ("days survived"), and the `--self-author` phase must NOT resurface a
storm-finale. Active build-now queue is now **M1-M5 + M5a + M5b**; design-gate M6-M10 unchanged.

---

## Cycle 0 addendum — review cadence set to PHASE-level (2026-06-18)

User: don't pause at each milestone — run the full phase, review after. Restructured the roadmap
`### Milestone:` markers to **PHASE boundaries**: the loop now runs all of Phase A (M1→M5b) unattended,
**committing every cycle**, and PAUSES once at "Phase A — Build-out complete." Phase B (M6→M10) is
design-gated — its design calls are made at the Phase A review, then it runs as one block → "Phase B
complete" review. **Commit granularity is unchanged: one commit per CYCLE (≈ one session); a single
milestone spans several cycles/commits.** `checkpoint` stays `milestone` (markers now sit at phase
boundaries, not per-M-milestone).

---

## Cycle 0 — APPROVED ✅ (plan-review gate released · 2026-06-18)

Human `/campaign-approve` at plan-review. The plan was authored directly into `roadmap.md` /
`iteration-plan.md` / GDD §12 (no separate proposal file to fold). **Gate cleared:** `status: active`,
`awaiting_approval: false`, `stop_reasons: []`. Launch config (user kept as-is): cap **12** (hard stop —
first stop will be `max-cycles` ~mid-Phase-A; resume via `/campaign-start --resume --max-cycles=<N>`, NOT
`/campaign-approve`), gate `npm run verify:all`, visual-gate `auto`, **PHASE-level** review,
`until: roadmap-empty`, no budget ceiling. No post-mortem drafts pending (consolidate skipped).
**Cycle 1 is released → Phase A → M1 (Wreck-arc finish).** Begin with `/loop /campaign-cycle`.

---

## Cycle 1 — M1 panel dead-code cleanup + perf baseline (2026-06-18) — SHIPPED
- **Planned:** M1's two headless units — strip dead panel-greeble code + record the perf baseline (safe, verifiable first cycle).
- **Shipped:** removed the dead `buildGreeble` interior path (9 fns + 3 orphaned materials, −242 lines `panelGreeble.ts`) + dead `ARCHETYPE_EXTRACTABLES` + the inert `colliderHint` ItemDef field (4 files); recorded the campaign perf baseline.
- **Verify:** `verify:all` PASS — tsc clean · `verify:placement` 0/0 ×5 seeds (panel counts unchanged → live path intact) · `verify:colliders` 0/25. perf-probe **drawCalls 843** (≈842 baseline → inert) / programs 71 / sceneMeshes 8401 / boot 1225ms.
- **Visual iteration:** N/A — pure dead-code removal (no visual output; the adversarial appearance gate is not applicable).
- **Spend:** ~100K approx (no budget ceiling; cycle **1/12**).
- **Commit:** `7f06022`.
- **Next (cycle 2):** the remaining M1 units — `remove-wreck-sand-mounds` → `scrap-pickup-3q-thin` → `dish-collider-feel` (the visual/feel ones) → then M2. **Verdict: CONTINUE** (M1 in progress; no milestone crossed → no pause).

---

## Cycle 2 — M1 remove wreck sand-mounds (2026-06-18) — SHIPPED
- **Planned:** `remove-wreck-sand-mounds` (user directive) — wrecks sit on bare terrain, no sand-drift mounds.
- **Shipped:** removed the `makeSandMound` drifts from poiAssembler (composite POIs) / procgenWreck (legacy ships) / megaWreck (hero) via a **determinism-preserving no-op** (kept each call's rand draws, discarded the mesh) → seeded stream byte-identical. Supersedes the §G sand-integration items.
- **Verify:** `verify:all` PASS — tsc · `verify:placement` **0 fails ×5** (seed-42 79→80: a panel the drift used to occlude in the yard-prune is now exposed — intended, still 0-fail) · `verify:colliders` 0/25.
- **Visual iteration:** appearance-verified — **adversarial visual gate PASS** (3 harsh-lens critics + a code-auditor over satellite/tank/husk/derelict side renders; **0 sev≥2**; all read grounded on bare sand via seatSink/burySink). Seating FEEL → **feel-pending** Phase-A walk-test.
- **Spend:** ~170K approx (incl. the rig-shot renders + the visual-gate workflow); campaign total ~270K; cycle **2/12**.
- **Commit:** `e5ea971`.
- **Next (cycle 3):** M1's last 2 units — `scrap-pickup-3q-thin` → `dish-collider-feel` → then **M1 complete** → M2. **Verdict: CONTINUE** (M1 in progress; no milestone crossed).

---

## Cycle 3 — M1 scrap pickup edge-on mass (2026-06-18) — SHIPPED
- **Planned:** `scrap-pickup-3q-thin` (§G) — the torn-sheet scrap read paper-thin at a 3q/edge-on angle.
- **Shipped:** `scrapMesh.ts` (shared held + world) — plate thickness 6→14mm + a fuller rolled torn edge → edge-on MASS, no new pieces (no busy-pile regression). Round-1 fix held.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25.
- **Visual iteration:** appearance-verified — **adversarial visual gate PASS** (3 harsh-lens critics on 3q/front/left renders; **0 sev≥2**; edge-on now reads with real plate mass, stays one clean torn sheet). 1 sev3 nit (curl slightly fin-ward) deferred. In-hand FEEL → **feel-pending** Phase-A walk-test.
- **Spend:** ~150K approx (rig-shots + visual-gate workflow); campaign total ~420K; cycle **3/12**.
- **Commit:** `5514fd6`.
- **Next (cycle 4):** M1's LAST unit — `dish-collider-feel` → then **M1 COMPLETE** → M2. **Verdict: CONTINUE** (M1 in progress; no milestone crossed).

---

## Cycle 4 — M1 dish collider disc — M1 COMPLETE (2026-06-18) — SHIPPED
- **Planned:** `dish-collider-feel` (§E/§G) — the flagship satelliteDish slab collider over-blocked the round dish at the diagonals.
- **Shipped:** `satelliteDish.ts` — replaced the square slab box with a `makeStaticCylinder` DISC (radius `DISH_R`, half-height `DISH_DEPTH*0.5+0.3`) at the dishPivot's exact world transform → the disc IS the round dish, no diagonal corners.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25.
- **Visual iteration:** N/A — collider-only change (no appearance). Collision FEEL (does it still snag?) → **feel-pending** Phase-A walk-test.
- **Spend:** ~90K approx; campaign total ~510K; cycle **4/12**.
- **Commit:** `cbe861c`.
- **✓ MILESTONE M1 COMPLETE (C1-C4):** panel-deadcode + perf-baseline · sand-mounds · scrap-3q · dish-disc. **Verdict: CONTINUE** — M1 is NOT the last tier before the Phase-A `### Milestone:` marker (M2-M5b remain), so phase-level review does NOT pause here. Next: **M2** (cycle 5: feature-flags-infra → security-review → wreck-polish-bundle → yard-cross-poi-merge last/careful).

---

## Cycle 5 — M2 feature-flags infra (2026-06-18) — SHIPPED
- **Planned:** `feature-flags-infra` — the dependency-enabler the M9 gate-and-wait rope/cloth cycles need; land it early + inert.
- **Shipped:** NEW `src/config/features.ts` (sibling of tuning.ts) — `FEATURES` const (`realRope`/`realCloth`, default FALSE) + `FeatureFlag` type. Documents the gate-and-wait pattern. Inert (nothing reads it yet — intentional).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25.
- **Visual iteration:** N/A — inert config scaffold (no appearance/feel).
- **Spend:** ~50K approx; campaign total ~560K; cycle **5/12**.
- **Commit:** `5d1c22e`.
- **Next (cycle 6):** M2 `security-review-repo` (a client-side-game security audit — secrets/DOM/npm-audit/hygiene; likely a documented all-clear). Then wreck-polish-bundle, then yard-merge (last/careful). **Verdict: CONTINUE** (M2 1/4; no milestone crossed).

---

## Cycle 6 — M2 security review (2026-06-18) — SHIPPED
- **Planned:** `security-review-repo` — audit the repo for vulns / leaked secrets.
- **Shipped:** an all-clear audit (client-side game, narrow surface) — 0 secrets, 0 unsafe DOM/injection sinks, clean repo hygiene — + **remediated the 1 HIGH npm-audit vuln** (vite 8.0.12 → 8.0.16 dev-server fix; 0 vulns after). Findings logged to backlog §E.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25. `npm audit` 0 vulns (was 1 high). Vite-boot smoke-test PASS (perf-probe; drawCalls 841 / programs 71).
- **Visual iteration:** N/A — security audit + dependency patch (no appearance/feel).
- **Spend:** ~80K approx; campaign total ~640K; cycle **6/12**.
- **Commit:** `27d03b6`.
- **Next (cycle 7):** M2 `wreck-polish-bundle` (the §F/§G sev-2/3 visual set — likely multi-cycle, ship `[partial]`), then `yard-cross-poi-merge` (last/careful). **Verdict: CONTINUE** (M2 2/4; no milestone crossed). **Halfway to the 12-cap** — the calibration stop lands mid-Phase-A.

---

## Cycle 7 — M2 wreck-polish delta 1: non-axial dorsal mass `[partial]` (2026-06-18) — SHIPPED
- **Planned:** `wreck-polish-bundle` delta 1 of 5 — break the heavy-class "sausage" silhouette (§F/§G).
- **Shipped:** NEW `addDorsalMass` (procgenWreck.ts) — a dorsal bridge-tower superstructure (tower + cap + offset deckhouse + window strip + twin masts) rising mid-hull on mega_freighter/bulk_hauler/freighter. hash2-only (zero rand), decoration-tagged, shared mats, rule-7.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 (determinism preserved) · colliders 0/25.
- **Visual iteration:** **iterated 5 ROUNDS** (rule-8, new visual element) — adversarial gate PASS (3 critics, 0 sev≥2; sausage broken on all 3 heavy classes). Convergence: R1 freighter end-cap → R2 mega+freighter sliver → R3 bulk squat → R4 freighter short → R5 PASS (mid-hull seat · height off wreck-bulk · vertical-capped base · tall masts). In-world feel → **feel-pending** Phase-A walk-test.
- **Spend:** ~290K approx (the costliest cycle — 5 visual-gate rounds); campaign total ~930K; cycle **7/12**.
- **Commit:** `9a49532`.
- **`wreck-polish-bundle` is `[partial]`** — 4 deltas remain (chroma, engine-droop, scout-trauma, scale-anchor pocket). **Next (cycle 8):** delta 2 = weathering chroma. **Verdict: CONTINUE** (M2 in progress; no milestone crossed). **Cap heads-up: 5 cycles left** to the calibration stop.

---

## Cycle 8 — M2 wreck-polish: chroma (already-done) + delta 3 engine-droop `[partial]` (2026-06-18) — SHIPPED
- **Planned:** wreck-polish delta 2 (weathering chroma).
- **Shipped:** **delta 2 chroma → assessed ALREADY-DONE** (the `HULL_WEATHERING_ACAY` profile already has saturated orange oxide + lifted seam-rust + rust drips; ACBB D234 finalized it post-§F; up-close render confirms) → no re-do; **pivoted to delta 3 engine-droop** — sign-randomized + widened (6-21°) + ~15% torn (dramatic hang), hash2-only (zero rand), pure rotation (no overlap).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 (determinism preserved) · colliders 0/25.
- **Visual iteration:** delta-3 **solo-triage** (the skill's lighter pass for a routine geometry tuning) — 3-seed gunship inspection shows varied torn-loose engines, believable, no float/clip. Torn FEEL → **feel-pending** walk-test. delta-2 chroma not rebuilt (verified already-done — like the dish/scrap items).
- **Spend:** ~130K approx; campaign total ~1.06M; cycle **8/12**.
- **Commit:** `f4535c8`.
- **`wreck-polish-bundle` stays `[partial]`** — deltas 4 (scout/corvette trauma) + 5 (scale-anchor pocket) remain. **Next (cycle 9):** delta 4. **Verdict: CONTINUE** (M2 in progress; no milestone crossed). **Cap: 4 cycles left.**

---

## Cycle 9 — M2 wreck-polish delta 4: guaranteed corvette/gunship trauma `[partial]` (2026-06-18) — SHIPPED (read-polish flagged)
- **Planned:** wreck-polish delta 4 (scout/corvette guaranteed visible trauma).
- **Shipped:** new `addForcedTrauma` — ONE deterministic breach on every **corvette + gunship**: an unlit black-void **gash** (reads as a hole, not a lit dark greeble) + a peeled hull-plate **flap** + a jagged **lip** strut. **hash2-only → ZERO rand** (panel stream byte-identical); added before panels (no panel welds over the hole); `isWreckDecoration`-tagged. The §F "list" was already in-world (`poiAssembler` `arch.params.list` × phash). **Caught in tsc:** `'scout'` is NOT a `ProcgenWreckClass` (rig-shot fallback) → retargeted to corvette + gunship.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 (determinism preserved) · colliders 0/25.
- **Visual iteration:** ⚠ **appearance-verified MODEST (4 rounds) — honest read-flag.** Trauma is guaranteed + determinism-safe + believable crash damage from the gash flank (player circles in-world → guaranteed-seen), but it's **flank-located** + on some seeds reads as **scattered debris near the wreck** rather than a clean attached **main-hull** breach (part-pick takes any ≥0.7 body part). The all-angle silhouette-break attempt (round 2) read as detached debris + was reverted. **Attachment + main-hull-targeting polish → backlog §F; read + feel → walk-test.** Not a clean dramatic PASS — flagged for the milestone polish review.
- **Spend:** ~230K approx (4 trauma-read rounds + a tsc class-name fix + extra gate runs); campaign total ~1.29M; cycle **9/12**.
- **Commit:** `4bca3b4`.
- **`wreck-polish-bundle` stays `[partial]`** — delta 5 (scale-anchor exclusion pocket) remains, then `yard-cross-poi-merge`. **Next (cycle 10):** delta 5. **Verdict: CONTINUE** (M2 in progress; no milestone crossed). **Cap: 3 cycles left — the cap will likely land at delta 5 + the start of yard-merge.**

---

## Cycle 10 — M2 wreck-polish delta 5 + C9 trauma read-polish → wreck-polish-bundle COMPLETE (2026-06-18) — SHIPPED ⚡ULTRACODE
- **Planned:** delta 5 (scale-anchor exclusion pocket) + (ultracode) a real fix for the flagged C9 trauma read.
- **Shipped:** **wreck-polish-bundle is now COMPLETE (5/5 deltas).**
  - **C9 trauma read-polish — the adversarial gate FOUND A ROOT-CAUSE BUG.** A 3-round Workflow gate's code-auditor (reading the geometry SOURCE, not just the renders) diagnosed that `addForcedTrauma` placed elements as if local `y=0` were the hull axis — but every hull variant seats the body centre at `y≈r*0.55` (crown `≈r*1.55`; `y=0` is the underside burial sinks), so the roof/flap floated ~0.5m off the hull = the "scattered debris" read. (Also disambiguated: that read was partly the INTENDED `addDebrisFan` — sand fragments are a feature, mis-attributed in C9.) Rewrote to the real frame + depth cues (scorch ring + 2 jagged sub-voids) + welded flap + tiny-wreck flap-skip + largest-mass part-pick. **Dropped the dorsal roof** (floated on the domed corvette across all 3 rounds — the `r*1.55` estimate overshoots it); the flank breach now reads as a strong ATTACHED hole, gate-confirmed.
  - **delta 5 scale-anchor exclusion pocket:** determinism-safe scene-graph filter removes greebles inside the anchor footprint on the lee flank (zero rand, collider-exempt).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (determinism held across the rewrite + 3 gate rounds).
- **Visual iteration:** iterated **3 adversarial Workflow gate rounds** (multi-critic + code-auditor). R1 FAIL→found the bug; R2 FAIL→fix confirmed landed, 1 residual sev2 (roof float); R3 FAIL→roof still floating→DROPPED. Final: flank breach gate-confirmed attached on every angle; residual sev1 (gunship side-void contrast) → backlog. **The gate earned its keep — a code-only change would have shipped the coordinate bug.** FEEL → feel-pending walk-test.
- **Spend:** ~600K approx (BY FAR the costliest cycle — ultracode: 3 gate rounds ≈458K subagent tokens + a full rewrite); campaign total ~1.89M; cycle **10/12**.
- **Commit:** `89d63d4`.
- **M2 status:** wreck-polish-bundle ✓ COMPLETE; **`yard-cross-poi-merge` is the last M2 unit. Next (cycle 11):** yard-merge (HIGH-RISK, D237/D239). **Verdict: CONTINUE** (M2 in progress; no Phase boundary). **Cap: 2 cycles left.** ⚠ Per-cycle spend rose ~2-4× under ultracode — flag at the cap review.

---

## Cycle 11 — M2 yard-cross-poi-merge: 3rd attempt DEFERRED to a perf session after a recon (2026-06-18) — DEFERRED ⚡ULTRACODE
- **Planned:** the last M2 unit `yard-cross-poi-merge` (HIGH-RISK, twice-reverted — D237/D239).
- **Shipped:** an informed **DEFER decision (D240)**, not code. Per the D239 revert-on-2×-fail guardrail + ultracode, ran a **3-agent Workflow recon (~205K)** mapping the merge path, the bury-audit, and the D237/D239 history BEFORE a risky 3rd attempt. Findings: (1) **marginal value** — live `wreck-yard` probe = **1705** draw calls (field **842**), both playable, dense yard frustum-culled; (2) the panel **already self-merges** its static parts at gen — only the rim frame is foldable, and folding it is what regressed the audit; (3) D239's terrain-audit regression mechanism is **still un-pinned**. → DEFER to a dedicated perf session that can make the architectural change (cache `panelDoorExtents`-world + audit the cache, or instance the salvage components). D240 + backlog document the exact next-session order (instrument the perturbation FIRST).
- **Verify:** no source change (docs-only) → `src/` byte-identical to C10's `verify:all` PASS; tsc re-confirmed clean.
- **Visual iteration:** N/A — a recon + decision cycle (no player-facing content). Honest note: low game-value cycle, but justified — the recon de-risks a future clean attempt + prevented a 3rd wasteful failed try on a marginal-value perf item.
- **Spend:** ~300K (the recon); campaign total ~2.19M; cycle **11/12**.
- **Commit:** `725524c`.
- **M2 status:** ✓ **CONTENT COMPLETE** (feature-flags + security + wreck-polish-bundle, C5-C10); yard-merge deferred. **Next (cycle 12, THE CAP):** M3 `worm-model-overhaul`. **Verdict: CONTINUE** (no Phase boundary). **Cap: 1 cycle left → cycle 12 self-halts `completed (max-cycles)` for the calibration review.**

---

## Cycle 12 (THE CAP) — M3 START: worm-model rig-shot + maw fang overhaul (2026-06-18) — SHIPPED `[partial]` ⚡ULTRACODE → 🏁 COMPLETED (max-cycles)
- **Planned:** start M3 with `worm-model-overhaul` (one bounded unit before the cap).
- **Shipped (M3 STARTED `[partial]`):** (1) a **NEW `worm-model` rig-shot scenario** (`--angle=head|side|3q`) that surfaces + frames the sand worm — the M3 hero-creature enabler, so the resume can render+gate the worm (zero game-code touch); (2) a **maw FANG overhaul** — the teeth were 0.7m cones (invisible dots on the ~12m maw) → real ~2-2.4m interleaved fangs in two recessed rings.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (the worm is a creature, not a placement/collider-audited POI; tsc is the relevant gate).
- **Visual iteration:** lean adversarial Workflow gate (2 critics) — "a massive improvement, awe not silly, proportion correct"; caught one sev2 (top-rim fangs crossing the lit rim band), **fixed in 1 round** (recess the ring inside the rim + radius pull-in + tip inward); re-render confirms a clean recessed toothed gullet. Residual sev1 (top-fang angle, near-side depth) + the FULL-BODY awe overhaul → resume (Rule 8: hero creature = 5-8 rounds). FEEL → feel-pending walk-test.
- **Spend:** ~400K (worm investigation + the rig-shot scenario + the gate); campaign total ~2.59M; cycle **12/12**.
- **Commit:** `96459a6`.
- **🏁 VERDICT: STOP — `completed (max-cycles)`.** `cycles_completed = 12 = max_cycles` → TERMINAL. The campaign self-halted at the calibration cap. **NOT a pause — do not `/campaign-approve`.** Review (see `next-session-prompt.md`: ultracode on/off, owed walk-tests, resume cap), then **`/campaign-start --resume --max-cycles=<N>`** to continue at M3.

---

## 🏁 CAMPAIGN SUMMARY — `campaign/2026-06-18` (12 cycles, ~2.59M, COMPLETED at the cap)
- **M1 ✓** (C1-C4) · **M2 content ✓** (C5-C11; yard-merge deferred D240) · **M3 ◐ started** (C12 `[partial]`).
- **Per-cycle commits** on `campaign/2026-06-18` (one revertible commit each). **Phase A (M3-M5b) + Phase B (M6-M10) remain** for the resume.
- **Cost calibration:** C1-C8 50-290K; **ultracode (C9-C12) ~300-600K** — bought a root-cause bug catch (C10) + a saved failed-attempt (C11), pricey on routine. **User decides ultracode posture for the resume.**
- **Notable engineering:** C10 adversarial code-auditor found a hull coordinate-frame bug solo iteration missed; C11 recon prevented a 3rd yard-merge failure; the determinism law (placement 0/0) held every cycle.
- **Owed:** an attended `npm run dev` walk-test pass (FEEL — wrecks/scrap/dish/engine/breach/worm); the deferred yard-merge perf session; the worm full-body overhaul.

---

# ▶️ RESUMED 2026-06-19 (overnight, ULTRACODE max-quality) — cap 12→50, status active

## Cycle 13 — M3 worm dorsal-armor overhaul → worm-model-overhaul COMPLETE (2026-06-19) — SHIPPED ⚡ULTRACODE
- **Planned:** finish `worm-model-overhaul` — the full-body awe pass (the side silhouette was a uniform ribbed tube).
- **Shipped:** **worm-model-overhaul is COMPLETE.** Added 13 overlapping keeled **dorsal armor scutes** along the spine (`sandWorm.ts` makeWormMesh) → an ancient ARMORED leviathan silhouette (awe-not-horror). Hand-model, no rand; color `0x46382a` (gate-praised).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (the worm is a creature, not a placement/collider-audited POI).
- **Visual iteration:** **3 adversarial Workflow gate rounds** (hero element, Rule 8). R1 cohesion FAIL (gappy sawtooth pyramids → stegosaurus); R2 lower+broaden+overlap (broadside ok, 3q still sawtooth apex); R3 **flat-topped frustum** (`CylinderGeometry(0.42,1,1,4)`) + stronger overlap → 3q V-notches filled into one continuous rolled armored crest → **final gate PASS** (sawtooth resolved). Residual sev3 (crest contrast / head-beef) → backlog. FEEL → feel-pending walk-test.
- **Spend:** ~320K (3 gate rounds); campaign total ~2.91M; cycle **13/50**.
- **Commit:** `ee78dc9`.
- **M3:** worm-model-overhaul ✓ COMPLETE. **Next (cycle 14):** `worm-tail-buried`. **Verdict: CONTINUE** (M3 in progress; no Phase boundary).

## Cycle 14 — M3 worm-tail-buried (Dune emergence read) (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending
- **Planned:** `worm-tail-buried` — the tail stays buried during surface arcs (emerging-from-earth read).
- **Shipped:** a **tail-sink** in `applyBodyBend` (`sandWorm.ts`) — when the body arcs above the sand, the REAR segments ramp DOWN to ~1.2 radii below the surface (quadratic rear ramp; head/front arch + the bite-arc untouched; auto-scales to arc height). The worm now ERUPTS from the earth instead of floating as a tube. Added a faithful `worm-model --angle=arc` static lunge-pose render for gating.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (worm pose code, not a placement/collider POI).
- **Visual iteration:** lean adversarial gate (2 critics) on the arc-pose render — both verdict-PASS + `tail_buried=true` ("rear dives into the sand, armor follows the bend cohesively, no defects"); 1 sev2 (dive shallow) fixed in 1 round (bury margin 0.5→1.2 radii); re-render confirms a deep plunge. Residual sev1 (front-hump height, tail-entry sand spray) → **FEEL feel-pending walk-test** (the breach MOTION is the real test).
- **Spend:** ~210K (feel unit, lighter than the hero-model cycles); campaign total ~3.12M; cycle **14/50**.
- **Commit:** `588ee60`.
- **M3:** model ✓ · tail-buried ✓. **Next (cycle 15):** `worm-charge-dive`. **Verdict: CONTINUE** (M3 in progress; no Phase boundary).

## Cycle 15 — M3 worm-charge-dive (submerged armored back-ridge tell) (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending
- **Planned:** `worm-charge-dive` — strengthen the underground charge tell.
- **Shipped:** the charge now rides LOWER (`basePos.y = groundY − MAX_RADIUS·SANDWORM_CHARGE_SUBMERGE`, new tuning 0.42) so only the **armored back-ridge** (the C13 crest) breaks the surface — a Dune submerged-tracking tell, the lunge being the full reveal. A charge `chargeDip` in `applyBodyBend` tapers the rear into the dune. Added `worm-model --angle=charge`.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (worm behavior, not a placement/collider POI).
- **Visual iteration:** lean adversarial gate (2 critics) on the charge-pose render — both verdict-PASS + `submerged_read=true` ("a credible sandworm-back racing through the dune, not a worm on top; geometry sound"); 1 sev2 (rear flat cut-off face) fixed in 1 round (the chargeDip rear-taper). Residual sev1 (displaced sand-bank, crest tonal separation) → backlog. **SOUND/MOTION** (charge timing + wake puffs) → feel-pending walk-test.
- **Spend:** ~190K (feel unit); campaign total ~3.31M; cycle **15/50**.
- **Commit:** `760dd81`.
- **M3:** model ✓ · tail ✓ · charge ✓. **Next (cycle 16):** `worm-audio-rumble` (AUDIO — no visual gate). **Verdict: CONTINUE** (M3 in progress; no Phase boundary).

## Cycle 16 — M3 worm-audio-rumble (sustained sub-bass charge approach) (2026-06-19) — SHIPPED ⚡ULTRACODE · sound feel-pending
- **Planned:** `worm-audio-rumble` — a low approach rumble during the underground charge.
- **Shipped:** a NEW sustained **sub-bass rumble** (`audio.ts` start/setLevel/stopWormRumble — detuned low sines + lowpassed looping noise under a tremolo LFO, via `_sfx`) wired to the charge: starts on `enterCharging`, ramps with proximity, stops on abort/lunge/death; main.ts stops it on pause. Pairs with C15's back-ridge.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (audio/behavior, not a placement/collider POI).
- **Visual iteration:** N/A — **AUDIO unit, no visual gate.** Gated by a **code-auditor** review of the Web Audio graph (ultracode): R1 FAIL — **3 real sev2 node-lifecycle bugs** (LFO not detached before the fade → tail clicks; `stop()` never `disconnect()`'d → stale graph branches across charges; paused-mid-charge orphaned the rumble). Fixed in 1 round → R2 **PASS** ("no leaks, no clicks, no negative-gain, all charge exits covered"). **SOUND** quality → feel-pending walk-test.
- **Spend:** ~190K (audio unit, 2 code-auditor rounds); campaign total ~3.50M; cycle **16/50**.
- **Commit:** `a248d9c`.
- **M3:** model ✓ · tail ✓ · charge ✓ · audio ✓. **Next (cycle 17):** `multi-worm-population`. **Verdict: CONTINUE** (M3 in progress; no Phase boundary). *(The code-auditor gate on an audio unit caught 3 sev2 node-lifecycle bugs a tsc-only check would have shipped.)*

## Cycle 17 — M3 multi-worm-population (infra pre-existing) + the multi-worm rumble fix (2026-06-19) — SHIPPED ⚡ULTRACODE
- **Planned:** `multi-worm-population` — several roaming worms.
- **Shipped:** **the population infra was ALREADY BUILT** (ACE Tier 2 — `SANDWORM_COUNT=2`, rejection-sampled with `SANDWORM_MIN_SEPARATION`, per-biome, multi-worm tremor selection, save/load) — verified, not rebuilt (like chroma/dish). **The genuine gap (the C16 audio-audit follow-up):** the C16 approach rumble was a single global handle wired PER-WORM → broke with 2 worms. **Fixed** with a global `applyWormRumble` pass (mirrors `applyClosestTremorEffect`) driving the one rumble from the NEAREST CHARGING worm; removed all the C16 per-worm calls.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25.
- **Visual iteration:** N/A — logic/audio refactor. **Code-auditor gate PASS** (ultracode): "nearest-selection correct, stops on no-charge, pause-safe, idempotent, all per-worm calls correctly removed." Encounter balance (the worm count) → feel-pending walk-test; `SANDWORM_COUNT` is a documented tunable (bumping draws `scatterRand` → determinism re-check).
- **Spend:** ~150K (mostly-done unit + a focused refactor); campaign total ~3.65M; cycle **17/50**.
- **Commit:** `22822d2`.
- **M3:** model ✓ · tail ✓ · charge ✓ · audio ✓ · multi-worm ✓. **Next (cycle 18):** `sarlacc-lure-ambush` (the user-requested add — LAST M3 unit). **Verdict: CONTINUE** (M3 in progress; no Phase boundary).

## Cycle 18 — M3 sarlacc-lure-ambush (the user-requested worm LURE) → M3 COMPLETE (2026-06-19) — SHIPPED ⚡ULTRACODE · 4 model rounds · feel-pending
- **Planned:** `sarlacc-lure-ambush` — the user's explicit add: a deliberate bait/lure the player places to draw the worm. Design-first (net-new).
- **Shipped:** a NEW craftable consumable **`worm_lure`** the player **plants ~4m ahead** (`items.ts onUse` → `spawnDroppedPickup` at the camera-forward XZ; static, recoverable until consumed). The worm homes on it via the EXISTING `findNearbyMeat` (the lure joins `meatItems`) at a **long scent range** — new `Tuning.SANDWORM_LURE_DETECT_RADIUS_M=70` vs meat's 30 — then surfaces to feed (the 2×-damage strike window) + consumes it. **Minimal design** (a 3-agent recon picked it over a ~625-LOC deployable device): reuses pickups + feeding + the pickup save → **no save bump.** Hooks: `types.ts` ItemId · `tuning.ts` radius · `sandWorm.ts findNearbyMeat` (per-item detect radius) · `items.ts` def (+ a runtime-only circular import for `spawnDroppedPickup`, init-safe — confirmed by a clean perf-probe boot) · `main.ts` dev-start ×2 (a craft recipe is a follow-up).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (the lure is a held item + pickup, not a placement/collider POI).
- **Visual iteration:** adversarial Workflow gate on the lure MODEL, **4 rounds** (held item). R1 FAIL (bright cherry-red gland reads cartoonish vs the muted palette) → R2 desaturated oxblood + lumpy/clotted + a bigger bone crown (1/2) → R3 rounded gland (was a faceted peak/roof) + a clean up-and-out THICK barb ring (rule 7) + collar (1/2) → R4 **removed the emissive** (the glow was the "hot orange-red" the palette critic kept flagging) → a muted dried-blood gland nestled in the pale bone crown. `reads_as_lure=true` **unanimous every round** (geometry sound, no defects). **FEEL** (the summon-and-ambush loop in play + the in-hand model edge-on) → feel-pending Phase-A walk-test.
- **Process note:** a `verify:placement` hang cost time — root cause was MY premature kills of a SLOW-but-fine verify (it runs 5 seeds via `spawnSync` + buffers ALL output to the end), which spawned port-contending zombie node processes. Lesson recorded in `next-session-prompt.md`: don't kill verify; if it hangs, `taskkill //F //IM node.exe` → confirm 0 → one clean run.
- **Spend:** ~300K (3-agent recon + net-new item + 4 model-gate rounds + the zombie recovery); campaign total ~3.95M; cycle **18/50**.
- **Commit:** `9d5576e`.
- **✓ MILESTONE M3 COMPLETE (C12-18):** model · tail-buried · charge-dive · audio-rumble · multi-worm · lure. **Next (cycle 19):** `M4 — Critters + atmosphere` (vulture-motion-feel first). **Verdict: CONTINUE** — M3 done but it is NOT the last tier before the Phase-A `### Milestone:` marker (M4-M5b remain), so the phase-level review does NOT pause here.

## Cycle 19 — M4 START vulture-motion-feel: infra pre-existing, fixed the dangling-leg soar (2026-06-19) — SHIPPED ⚡ULTRACODE · motion feel-pending
- **Planned:** `vulture-motion-feel` — flight/motion FEEL polish (the first M4 unit).
- **Shipped:** the vulture flight system was **ALREADY deeply built** (ACAH model + ACAI flight) — a full FSM (perched/flying/landing/dead + the carcass ecology circling/swooping/carrying/feeding/returning), per-state rig animation, **banking into turns** (`applyFlightOrientation` → roll), **flap↔glide envelopes**, dihedral, landing flare, terrain clearance, gaggle separation. **Verified, not rebuilt** (the recurring multi-worm/chroma/dish pattern). **The genuine gap, found by rendering the FLIGHT poses (only ever gated PERCHED):** in the soaring states the **legs DANGLED + splayed** — the leg-tuck used MIRRORED signs (`legL=-LEG_TUCK, legR=+LEG_TUCK`) so the hips swung the feet *opposite* front-back (a splay, not a tuck) and 1.2 rad only reached horizontal. **Fixed:** both hips rot.z `-VULTURE_LEG_TUCK` (same sign → tuck together) + 1.2→1.85 (back **and** up under the tail); `carrying`/`landing` legs-down untouched.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (the vulture is a creature + the change is a render-time pose, not a placement/collider POI or a procgen draw).
- **Visual iteration:** lean adversarial gate (2 critics) on the soaring silhouette (flying+circling side; frozen-flap-phase explicitly disregarded). Both **verdict-PASS**, `legs_tucked=true,true` + `reads_as_soaring_bird=true,true`, no geometry defect introduced. Residual sev1 (the tucked-leg mass is subtle at close-up side-profile — irrelevant at in-game viewing distance where the bird is a distant dark silhouette) → backlog/nit. **MOTION feel** (the animation over time — flap cadence, banking in turns, the wheel over a carcass) → feel-pending Phase-A walk-test (a frozen frame can't judge it).
- **Spend:** ~120K (mostly-done unit: assess + flight-pose renders + one targeted fix + 1 lean gate); campaign total ~4.07M; cycle **19/50**.
- **Commit:** `a0d9f1d`.
- **M4:** ✓ vulture-motion-feel. **Next (cycle 20):** `atmosphere-feeltunes` (tuning of shipped storm/fog/wind systems). **Verdict: CONTINUE** (M4 1/4; no Phase boundary).

## Cycle 20 — M4 atmosphere-feeltunes: root-caused the storm-dust "snow" read + a new storm-render enabler (2026-06-19) — SHIPPED ⚡ULTRACODE · motion feel-pending
- **Planned:** `atmosphere-feeltunes` — tuning of shipped storm/fog/wind/dust systems.
- **Shipped:** the weather system was **ALREADY exhaustively built + Rule-2-tunable** (a Dune-style sweeping storm WALL with distance-derived intensity, 3 volumetric dust layers w/ staggered ramps, FogExp2 curve, cloud coupling, sun/ambient dimming, warm vignette, anisotropic wind, shelter dampening, in-storm camera sway, wind-push, movement penalty). **Verified, not rebuilt** (the recurring pattern); the multi-session-tuned SWAY feel was deliberately **NOT blind-tuned** (walk-test territory). The headless-verifiable gap: the peak-storm LOOK had no render scenario + the dust read as SNOW. **(1) NEW `storm` rig-shot scenario** (forces a peak storm centered on the player + frames the dust-storm — the reusable atmosphere-iteration enabler). **(2) Root-caused + fixed the dust "snow-flurry" read:** the storm dust is unlit + `toneMapped:false` → it ignored the storm's sun/ambient DIMMING + rendered at full raw brightness → bright high-contrast white specks popped against the darkened sky. Fix: **couple the dust albedo to the scene-dim** (`mat.color = baseColor·(1 − STORM_SUN_DIM·intensity)`) so the dust dims in lockstep with the storm; + darkened/warmed the 3 layer colors, softened the sprite (feathered, lower peak alpha), thinned the far layer (0.40→0.30).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (atmosphere particles + a new rig-shot scenario, not a placement/collider POI or a procgen draw).
- **Visual iteration:** adversarial gate on the peak-storm LOOK, **3 dust rounds**. `reads_as_sandstorm=true` **UNANIMOUS every round** (the storm TONE — dark warm murk + fog + dimming + vignette — was always excellent). Dust arc: R1 white-speck pop (sev2) → R2 darker colors (helped, still popped) → R3 the intensity-coupled dim + softer sprite → the holistic **TONE critic now PASSES** (`dust_reads_as_sand=true`, "dim warm motes, blowing sand not snow"). Residual: a few specks over the very darkest sky band (frozen-frame; the dust streaks at 6-7 m/s in play → motion-masked) + a horizon-seam sev1 → backlog/walk-test. **MOTION feel** (sway/wind/dust-in-motion) → feel-pending Phase-A walk-test.
- **Spend:** ~250K (assess + the storm scenario + 3 gate rounds + the root-cause fix); campaign total ~4.32M; cycle **20/50**.
- **Commit:** `5877599`.
- **M4:** ✓ vulture-motion-feel · ✓ atmosphere-feeltunes. **Next (cycle 21):** `smoke-signal-plume` (likely net-new). **Verdict: CONTINUE** (M4 2/4; no Phase boundary).

## Cycle 21 — M4 smoke-signal-plume (NET-NEW): every lit fire is now a smoke beacon (2026-06-19) — SHIPPED ⚡ULTRACODE · 6 plume rounds · motion feel-pending
- **Planned:** `smoke-signal-plume` — a tall rising smoke signal visible from afar (likely net-new).
- **Shipped:** **NET-NEW** (the title pyre had smoke + `particleTrail.ts` anticipated it, but the in-game fire had none). **Every ALIVE fire now emits a tall rising smoke column** → a lit fire reads as a SIGNAL/landmark across the dunes (find camp; spot a camp). Minimal design: reuses `fire.ts` (no new item), pooled billboard grey puffs parented to the fire group, spawned at the flame top, rising + billowing + fading + drifting with the prevailing dune wind, **torn flat in a storm**. Transient → **no save bump**. + `Tuning.FIRE_SMOKE_*` (Rule 2), `__game.spawnFire`/`warmSmoke` debug hooks, a `smoke-plume` rig scenario (`--storm`).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (render-time non-raycastable sprites, not a placement/collider POI or a procgen draw).
- **Visual iteration:** adversarial gate on the plume LOOK (clear + storm), **6 rounds** (net-new visual, Rule 8). `reads_as_smoke_signal=true` every round; the iteration was the storm wind-response + boldness — faint→bold (opacity/density); R2 big-puffs blobbed → R3 smaller/denser/cooler-grey + `fog:false` + f² root-anchor (became too vertical) → R4-5 stronger shear + flattened vertical reach (tears toward horizontal) → **R6 BOTH critics PASS** ("clear bold near-vertical beacon; storm tears hard downwind; cool grey separates from both the warm sky + brown murk; no sprite edges"). **Footguns fixed:** decorative sprites hit by the interaction raycaster (`Raycaster.camera` warning + null-matrixWorld crash) → `sprite.raycast=()=>{}`; headless rAF throttling starved the real-time column → the deterministic `warmFireSmoke` fast-forward; a backlit shot (camera at the sun) → noon + side-spawn. **MOTION feel** (rise cadence/billow in play) → feel-pending Phase-A walk-test.
- **Spend:** ~350K (net-new build + debug hooks + rig scenario + 6 plume-gate rounds); campaign total ~4.67M; cycle **21/50**.
- **Commit:** `7ce6c81`.
- **M4:** ✓ vulture · ✓ atmosphere · ✓ smoke-plume. **Next (cycle 22, LAST M4):** `amban-rifle-balance` → then **M4 COMPLETE → M5**. **Verdict: CONTINUE** (M4 3/4; no Phase boundary).

## Cycle 22 — M4 amban-rifle-balance: corrected its strict dominance over the scrap gun → M4 COMPLETE (2026-06-19) — SHIPPED ⚡ULTRACODE · balance feel-pending
- **Planned:** `amban-rifle-balance` — tune the amban rifle (the last M4 unit).
- **Shipped:** assessed the shipped **4-weapon spread** (scrap_gun / amban / pulse_rifle / energy_pistol) + corrected an **objective imbalance.** The amban is fed by the SAME `scrap_bullet` ammo as the scrap_gun (range 30 / dmg 1.5 / cd 1.2 → DPS 1.25) but did **2× damage, 2× range, AND a higher DPS** (1.875) → it **strictly dominated** the starter (better in every dimension on shared ammo — the scrap gun had no niche once you salvage the amban). **Fix:** `WEAPON_AMBAN_RIFLE_COOLDOWN` 1.6→2.2 so it reads as a true MARKSMAN tradeoff (huge per-shot at 2× range, slow cadence → DPS 1.36 ≈ scrap's 1.25; the rapid scrap gun now wins sustained close-range DPS, its niche restored).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a weapon-balance constant, not a placement/collider POI or a procgen draw).
- **Visual iteration:** N/A — balance/tuning unit (no appearance change). The rifle MODEL was render-checked (reads as a long rifle). **Moderate, reversible nudge** (one constant), NOT a blind over-tune (C20/C22 discipline): the exact value + the deeper question (the amban is still ~2× ammo-EFFICIENT per bullet — accept as the rare-upgrade reward / push cd→2.4-2.6 for DPS parity / charge 2 bullets/round) are flagged for the **Phase-A milestone review** (backlog §A). **Balance FEEL** (cadence + power vs the worm[HP 12]/vultures) → feel-pending walk-test.
- **Spend:** ~120K (assess the weapon spread + the targeted balance fix); campaign total ~4.79M; cycle **22/50**.
- **Commit:** `e301823`.
- **✓ MILESTONE M4 COMPLETE (C19-22):** vulture-motion · atmosphere · smoke-plume · rifle-balance. **Next (cycle 23):** `M5 — Riding & rest feel` (speeder-riding-feel first). **Verdict: CONTINUE** — M4 done but it is NOT the last tier before the Phase-A `### Milestone:` marker (M5, M5a, M5b remain), so the phase-level review does NOT pause here.

## Cycle 23 — M5 START speeder-riding-feel: VERIFIED the exhaustively-tuned ride system (2026-06-19) — SHIPPED ⚡ULTRACODE · no code change · handling feel-pending
- **Planned:** `speeder-riding-feel` — riding handling/feel polish (the first M5 unit).
- **Shipped (a VERIFICATION cycle — honest: no source change):** the speeder riding system is **EXHAUSTIVELY tuned** (AAL/BB-CC/CC-2 — hover physics with a 4-sample terrain-follow LOOKAHEAD, velocity-control + proportional yaw turning, pitch/roll TILT + camera-roll, dust/glow/beacon, mount/dismount, a pose-SWEEP-solved rider pose; ~40 `SPEEDER_*` constants). **Verified, not re-tuned** — the riding HANDLING feel is the one thing headless can't judge, and blind-tuning a multi-session-tuned system is the C20/C22 anti-pattern. **bike-truth (the rig-on-bike inspection — 5 world angles + a numeric IK check):** the rider pose reads correct — `faceDotBikeFwd=1` (faces forward), hands **5cm** from the grips (on the bars), feet **22cm** from the pegs (hang ~14cm BELOW → read as TUCKED, acceptable for a hunched rider, NOT a floating defect like C19's vulture legs).
- **Verify:** **src byte-identical to C22's `verify:all` PASS** (no source change); tsc re-confirmed clean (the C11 docs-only precedent — a 7-min suite re-run adds nothing when `src/` is untouched).
- **Visual iteration:** N/A (no new visual). bike-truth confirmed the pose. The one genuine residual — a **ride-pose foot-LIFT** (re-bake the sweep-solved leg/ankle angles via the bike-truth IK loop to land the feet on the pegs) — is **queued** (backlog §A), NOT blind-nudged: the pose is sweep-baked, so it needs the iterate-verify loop, not a guess. **Handling FEEL** (accel/turn/boost/terrain-follow/tilt — the core of "riding feel") → feel-pending Phase-A walk-test.
- **Honest note:** a low-code-output cycle (like C11's recon→defer), but justified — the speeder is the most-tuned system in the game; the deliverable is the *verification* that it's solid + the correct hand-off of the residuals (the foot-lift to backlog, the handling to the walk-test) rather than a manufactured risky change.
- **Spend:** ~90K (the bike-truth inspection + the weapon/pose assessment); campaign total ~4.88M; cycle **23/50**.
- **Commit:** `8bcf723`.
- **M5:** ✓ speeder-riding-feel (verified). **Next (cycle 24):** `rope-attach-speeder-rear-bar`. **Verdict: CONTINUE** (M5 1/5; no Phase boundary).

## Cycle 24 — M5 rope-attach-speeder-rear-bar: explicit LMB tie/untie a sled to the tow-bar (2026-06-19) — SHIPPED ⚡ULTRACODE · tie-feel-pending
- **Planned:** `rope-attach-speeder-rear-bar` — attach the rope to the speeder's rear mount bar.
- **Shipped (a real feature build):** the rope→speeder TOWING was already built (the `'speeder'` `RopeEndpoint` kind + the `speederTowBar` mesh + a visible catmull-rom rope tube + the mount AUTO-TRANSFER); the gap was an **EXPLICIT** attach (you could only attach by mounting). **Built:** tagged the `speederTowBar` `interactType='attach_rope'`/`registry='speeder'`, pushed it to the raycast targets (parked or mounted), + sub-dispatched `interaction.ts` `case 'speeder'` (tow-bar → tie/untie; seat → the existing `mount` prompt). **LMB** ties a player-tethered sled to the speeder, or unties a speeder-tethered one back into hand — **works while PARKED** (no mounting). **Mirrors the `stakes`/`companion` rope-attach exactly** → **zero RMB conflict** (the backlog's design-gate was on the SEPARATE *RMB-on-rope-raycast-pick* UX; this uses the established LMB-on-target metaphor). ADDITIVE (the mount auto-transfer still works).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (an interaction + a userData tag, not a placement/collider POI or a procgen draw).
- **Visual iteration:** N/A — logic/interaction feature. **Code-auditor gate PASS** (ultracode, 0 sev≤2): "correct, consistent with the stake pattern, sub-dispatch sound, coexistence with the auto-transfer non-breaking." Lone nit: `attach_rope` LMB also fires a wielded-weapon swing — **PRE-EXISTING across ALL rope-attach paths** (stake/companion too), not new here → backlog §A. **Tie FEEL** → feel-pending walk-test.
- **Process note:** read the backlog design-gate carefully — "needs a design pass" applied to the *RMB-on-rope-pick* UX (a separate item), NOT the rear-bar attach. So the buildable scope (LMB-on-bar, the standard metaphor) shipped cleanly; only the RMB-pick UX stays deferred. (The C24 build-the-buildable-scope pattern — don't over-defer a unit because PART of it is design-gated.)
- **Spend:** ~250K (assess the rope/tether system + the feature build + the code-auditor gate); campaign total ~5.13M; cycle **24/50**.
- **Commit:** `956bab1`.
- **M5:** ✓ speeder-riding-feel · ✓ rope-attach. **Next (cycle 25):** `3p-camera-and-render-polish` (held-items-in-3P / foot-IK / the C23 foot-lift). **Verdict: CONTINUE** (M5 2/5; no Phase boundary).

## Cycle 25 — M5 3p-camera-and-render-polish: VERIFIED the heavily-built 3P render/camera system (2026-06-19) — SHIPPED ⚡ULTRACODE · no code change
- **Planned:** `3p-camera-and-render-polish` — a grab-bag of 3P render/camera polish (held-items-3P, footstep sync, foot-IK snap, cameraSnap, the C23 foot-lift).
- **Shipped (a VERIFICATION cycle — honest: no source change):** assessed EVERY named sub-item; **all already built.** (1) **cameraSnapNextFrame** — the flag + the chase-cam lerp exist (`controller.ts:511`), SET on save-load (`save.ts:687`) + speeder mount + dismount (`speeder.ts:991/1013`) → the camera doesn't lerp across teleports. (2) **held-items-in-3P** — a 2nd `makeViewModel` `tpMesh` on the rig's `rightHandAttach`, visible in 3P, with per-item `handAttachTransform` + `thirdPersonScale` (ABY P3); `rig3p --item=machete` confirms it renders in the hand. (3) **footstep ↔ gait sync** — already FIXED (ACE Tier 4A): phase-locked to `rig.stepCount` (no mid-air footstep audio). **The C23 speeder foot-lift is a STRUCTURAL leg-reach limit** — the seated-pose code's own comment: "the 3-DOF leg can't hit the pads exactly" (the sweep already optimized; feet read tucked/astride), NOT a tuning gap.
- **Verify:** **src byte-identical to C24's `verify:all` PASS** (no source change); tsc re-confirmed clean (the C11/C23 docs-only precedent).
- **Visual iteration:** N/A — the `rig3p` render confirmed held-items-in-3P. **Remaining (modest, deferred — NO blind build):** 3P camera-ORBIT mode (a design/feel control-scheme feature — walk-test), `thirdPersonColorBoost` for dark items (speculative "?"; the SCALE side is already done), the foot-IK idle→walk snap (cosmetic, no clean fix — continuous per-frame IK). The C20/C22/C23/C25 discipline: don't blind-build design/feel/structural items.
- **Honest note + meta:** the 2nd low-code cycle in the M5 polish run (C23 too). M5's units keep landing on **heavily-built mature systems** (riding + 3P) → verification cycles. The deliverable is the *confirmation* they're solid + the correct hand-off of the design/feel/structural remainder to the walk-test. **Flagged for the milestone review:** the user may want to re-prioritize toward M5a (exploration & exposure) / Phase B, which have more net-new feature work.
- **Spend:** ~90K (the full sub-item assessment + a rig3p render); campaign total ~5.22M; cycle **25/50** (HALFWAY).
- **Commit:** `c25cf7e`.
- **M5:** ✓ speeder-riding-feel · ✓ rope-attach · ✓ 3p-camera-and-render-polish. **Next (cycle 26):** `lie-down-to-sleep`. **Verdict: CONTINUE** (M5 3/5; no Phase boundary).

## Cycle 26 — M5 lie-down-to-sleep: a fade-to-black sleep transition (2026-06-19) — SHIPPED ⚡ULTRACODE · fade-timing feel-pending
- **Planned:** `lie-down-to-sleep` — a lie-down/rest mechanic.
- **Shipped (a real feature build):** sleep was built (E on a bedroll/tent → a "REST: how long?" menu → 4h/8h → **INSTANT** time-skip + stat recovery + autosave). **The genuine rest-feel gap:** resting was an abrupt menu-skip with no embodied beat. **Built a fade-to-black sleep TRANSITION:** picking a duration now hides the menu → fades a full-screen black layer in (`SLEEP_FADE_MS=800`) → advances time + recovers stats DURING the black (the existing one-shot, world frozen) → holds (`SLEEP_FADE_HOLD_MS=450`) → fades back to the rested world → resumes. So rest reads as actually SLEEPING (the conventional survival-game beat). + `Tuning.SLEEP_FADE_*` (Rule 2), a `#sleep-fade` CSS layer (z=150, above the menu), a `_fading` re-entry guard.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a UI overlay + CSS, not a placement/collider POI). **A FLAKE on the first run:** seed 2 = "NO AUDIT LINE (boot/render failed)" (a headless boot/render timeout; the other 4 seeds 0-fails) → **re-ran clean** per the skill (a single-seed boot flake — a UI-only change can't touch the placer; not a regression).
- **Visual iteration:** N/A — logic/UI feature. **Code-auditor gate PASS** (ultracode, 0 sev≤2): "the timeout chain aligns exactly with the CSS transitions; the time-skip applies fully-black (no flash of the world mutating); the `_fading` re-entry guard + the paused-world assumption (inShelter reflects the dialog-open spot) hold; no timer leaks." **Fixed the 3 sev3 nits** it flagged: the pause overlay is revealed behind the fade (unlock() → showPauseOverlay) → the black now **captures pointer events** during the sequence so a blind-click can't hit a hidden pause button; `closeSleepOverlay` now guards `_fading` (symmetric). **FADE TIMING** (the exact feel) → feel-pending walk-test (`SLEEP_FADE_*` tunable).
- **Spend:** ~280K (assess the sleep system + the fade build + the code-auditor gate + 3 fixes + a flake re-run); campaign total ~5.50M; cycle **26/50**.
- **Commit:** `24c6719`.
- **M5:** ✓ speeder-riding-feel · ✓ rope-attach · ✓ 3p-camera-polish · ✓ lie-down-to-sleep. **Next (cycle 27, LAST M5):** `viewmodel-nits` (3P torch flame) → then **M5 COMPLETE → M5a**. **Verdict: CONTINUE** (M5 4/5; no Phase boundary).

## Cycle 27 — M5 viewmodel-nits: the 3P torch flame now lives → M5 COMPLETE (2026-06-19) — SHIPPED ⚡ULTRACODE · flicker feel-pending
- **Planned:** `viewmodel-nits` — the small viewmodel nits (3P torch flame + FP night lighting); the LAST M5 unit.
- **Shipped:** **(1) FIXED the 3P torch flame.** `updateHeld` (the torch's flame flicker + burn-drain + world-light) only ran on the FP `vm.itemRoot`, so the 3P hand copy (`tpMesh` on `rightHandAttach`) had its `'torchFlame'` group stuck at `visible:false` — a dark stick in 3P. **Fix:** in `updateViewModel`'s per-frame hook, after the FP `updateHeld`, **MIRROR** the FP flame's animated state (visible + the group transform + each cone/ember's position/scale/opacity) onto the 3P copy — the FP flame stays the single source (no functional-state re-run → no double burn-drain / double light-arm). + a NEW `rig3p --lit` flag (lights a torch + sets night) to render it. **(2) FP held-item night-lighting = ALREADY FIXED** (ACAF: the vm-scene's `vmSun/vmMoon/vmAmbient` mirror the world sun/moon/ambient, so held items dim at night; the backlog item was stale).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a render-time held-item mirror + a test-only rig-shot flag, not a placement/collider POI).
- **Visual iteration:** `rig3p --item=torch --lit` confirmed a **live glowing flame in the 3P hand** (was a dark unlit stick). **FLAME flicker MOTION** → feel-pending walk-test (the mirror is exact, so the 3P matches the FP).
- **Spend:** ~150K (assess the torch/vm system + the mirror fix + the rig-shot flag + a render); campaign total ~5.65M; cycle **27/50**.
- **Commit:** `9cd358d`.
- **✓ MILESTONE M5 COMPLETE (C23-27):** speeder-riding-feel · rope-attach · 3p-render-polish · lie-down-to-sleep · viewmodel-nits. **Next (cycle 28):** `M5a — Exploration & exposure` (horizon-landmark-silhouettes first — more net-new). **Verdict: CONTINUE** — M5 done but it is NOT the last tier before the Phase-A `### Milestone:` marker (M5a + M5b remain), so the phase-level review does NOT pause here. **Getting close to the Phase A milestone pause (after M5b).**

## Cycle 28 — M5a horizon-landmark-silhouettes: fog-resistant skyline nav silhouettes (2026-06-19) — PARTIAL ⚡ULTRACODE · per-model impostor deferred
- **Planned:** `horizon-landmark-silhouettes` — the first M5a (Exploration & exposure) unit: make distant hero landmarks read as navigation cues from across the map (they currently fade into the fog).
- **Shipped (`[partial]`):** **The gap (confirmed by a NEW `--scenario=vista [--dist=]` rig-shot):** the world's FogExp2 (density 0.0018) fades distant geometry toward the SKY color, so a hero landmark at ~600m is **invisible** — it blends into the sky + can't be steered by. **Built the SYSTEM** (`src/world/horizonSilhouettes.ts`): a fog-RESISTANT (`fog:false`) dark silhouette billboard per major landmark — the hand-modeled flagships via `addHorizonSilhouettesByName(['megaShip','megaWreck','satelliteDish','crashedHull'])` after `placePOIs` + the taller hero-landmark wrecks inline in `placeHeroLandmarks` — **distance-gated** (`HORIZON_SILHOUETTE_FADE_START 240 → FULL 460`) so it fades IN exactly as the real model fogs OUT, Y-billboarded to face the camera, ticked in `main.ts` after the camera is final. **Determinism-safe** (no rand draw → placement 0/0 untouched) + **decoration-only** (no collider). The vista renders (150 / 350 / 600m) show the cross-fade (real angular model → silhouette) + a dark landmark mass now standing on the skyline at 600m (was invisible).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (decoration billboards, no new rand, not a placement/collider POI).
- **Visual iteration:** ⚠ **adversarial gate (2 critics, pixel-level) FAILed the flat-billboard look** — reads as a blob/rectangle (no structure), is FAINTEST at FAR range where it's the sole cue (the r1 texture faded out at the TOP = the part against the sky), + floats above the dune. **Iterated 2 rounds:** r2 = a STRUCTURED hand-drawn wreck silhouette (hull + tower + mast) + darker/bolder (`0x262320` @ 0.85) → reads as a recognizable dark structure now. **The LOOK is still below bar** → shipped `[partial]`; the **per-model IMPOSTOR** (RTT the actual landmark outline — the dominant fix), a texture-margin ground-tuck, + far-range boldness are **deferred to backlog §A / D241**. This is the Rule-8 case (a rough visual `verify:all` waved through, the gate caught).
- **Spend:** ~330K (the vista-scenario gap assessment + the system build + 2 hook iterations + 2 gate rounds + the adversarial gate ~84K); campaign total ~6.05M; cycle **28/50**.
- **Commit:** `434cd4a`.
- **M5a unit 1/4 `[partial]`** (system + nav cue ship; impostor polish → backlog). **Next (cycle 29):** `M5a — salvaged-spyglass` (the horizon-silhouette impostor is a backlog follow-up, not the next roadmap unit). **Verdict: CONTINUE** — M5a in progress; not terminal (roadmap non-empty, cycle 28/50), not a milestone (M5a + M5b remain before the Phase-A marker).

## Cycle 29 — M5a salvaged-spyglass: hold-RMB horizon zoom + a scope vignette (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending walk-test
- **Planned:** `salvaged-spyglass` — the 2nd M5a unit: a spyglass to zoom + scan the horizon (read the C28 silhouettes, spot landmarks to navigate toward).
- **Shipped:** A **net-new `spyglass` item** + a **hold-RMB FOV-zoom mechanic** (`player/spyglass.ts`: eases `camera.fov 78°→24°` = `SPYGLASS_FOV` ~3.25× while RMB-held + equipped, restoring to the settings FOV; driven off `mouseHeld(2)`, not `wieldLmb`, so LMB stays inert) + a **scope vignette** (a dark telescope tunnel + a clear magnified centre, fading in with the zoom; this module owns its overlay div) + a **craft recipe** (#17: `scrap×3 + cloth×1`, discovered by experimentation) + a **dev-start** for the walk-test + a **NEW `spyglass-view [--raw]` rig-shot**. Pairs with the C28 silhouettes (the silhouette says "something's there"; the spyglass says "it's a wreck").
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (an additive item + a render-time FOV mechanic + a recipe; no new scatter-rand, no world collider). *(An earlier run self-failed EXIT=1 — a concurrent render's `taskkill node.exe` killed the verify seeds mid-run; the clean re-run passed. Lesson re-logged: never kill node while verify runs.)*
- **Visual iteration:** **SCOPE-VIEW gate PASSED 1st pass** (the gameplay-critical visual — "reads unmistakably as looking through a spyglass; large obvious magnification; the wreck goes from a horizon speck to a readable structure"). The **held-item MODEL took 3 gate-driven rounds** (r1 "stubby thermos" + flat-cap lens → r2 longer/structured → r3 classic spyglass taper [narrow eyepiece → flared objective bell, ~6.5:1] + nested step-downs + bold steel collars + **solid dark recessed lenses behind brass bezels**, fixing r2's transparent-glass flat-cap + a recess-direction bug). Reads clearly as a brass spyglass in **profile/3q** (the in-play views). Residual gate nitpicks (a perfectly-end-on objective funnel + matte brass under the FLAT item-studio light, the latter a lighting artifact since the FP viewmodel is sun-lit in play) = rare-view/low-impact → shipped without a 4th round (converge, don't thrash). **FEEL** (zoom speed/curve, vignette timing, the missing aim-down-sight pose) → walk-test.
- **Spend:** ~450K (the net-new feature: assess → item + mechanic + overlay + recipe + 2 rig-scenarios + the SCOPE gate + 2 MODEL gates + 3 model rounds); campaign total ~6.5M; cycle **29/50**.
- **Commit:** `0a01b1b`.
- **M5a unit 2/4 ✓** (the horizon-silhouette impostor + the spyglass model end-on/material polish remain in the backlog). **Next (cycle 30):** `M5a — vista-crest-reveal`. **Verdict: CONTINUE** — M5a in progress; not terminal (roadmap non-empty, cycle 29/50), not a milestone (vista-crest-reveal + sun-shade-exposure + M5b remain before the Phase-A marker).

## Cycle 30 — M5a vista-crest-reveal: cresting a ridge lifts the fog + a swell (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending walk-test
- **Planned:** `vista-crest-reveal` — the 3rd M5a unit: reward climbing — cresting a ridge should open up a vista.
- **Shipped:** A **net-new feel/atmosphere system** (`world/vistaReveal.ts`, ticked after `updatePlayer` + `updateWeather`, inside the pause gate). Throttled terrain sampling computes the player's **prominence** (height − an 8-dir × 2-radius ring average); on the **rising edge** of prominence crossing `VISTA_PROMINENCE_M` (7m), off a `VISTA_COOLDOWN_S` (50s) cooldown, and not in a storm → **fire a reveal**: the **fog briefly LIFTS** (`fog.density ×0.5` at peak, eased attack→sustain→release ~7.9s, re-multiplied each frame *after* `updateWeather` sets the base so it never compounds) so the vista opens up + the distant C28 silhouettes sharpen, under a soft warm **audio swell** (`playVistaReveal` — an open-fifth/octave pad). **Closes the M5a exploration loop:** crest a ridge → the air clears + a landmark surfaces on the skyline → glass it (C29 spyglass) → steer toward it.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a transient per-frame fog modulation + audio; no scatter-rand, no geometry/collider).
- **Visual iteration:** logic/audio/atmosphere unit → **code-auditor review CLEAN** (no sev1/sev2 across 7 checks: fog non-compounding, envelope terminates, deterministic, null-safe, audio leak-free, pause-gated, the unwired `resetVistaReveal` correct-by-design). A `vista --dist=600 --fogmult=0.5` render confirmed the lift reads as a gentle clearing (the megaShip + horizon crisp up). **FEEL** (prominence threshold, cooldown, envelope timing, fog amount, swell character) → walk-test.
- **Spend:** ~300K (assess hooks → the detection + envelope + the fog-lift + the audio swell + the rig-shot preview + a code-auditor review); campaign total ~6.8M; cycle **30/50**.
- **Commit:** `d24334f`.
- **M5a unit 3/4 ✓.** **Next (cycle 31):** `M5a — sun-shade-exposure` (the **last** M5a unit; then **M5b → the Phase-A milestone PAUSE**). **Verdict: CONTINUE** — M5a in progress; not terminal (roadmap non-empty, cycle 30/50), not a milestone (sun-shade-exposure + M5b remain before the Phase-A marker). **The Phase-A pause is approaching — the walk-test backlog (C26-C30 feel items) is growing; flag it at the pause.**

## Cycle 31 — M5a sun-shade-exposure: wreck/dune shade relieves heat → M5a COMPLETE (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending walk-test
- **Planned:** `sun-shade-exposure` — the **last** M5a unit: make direct sun vs shade matter for survival.
- **Shipped:** **ASSESS-FIRST** (the brief flagged it) confirmed the heat model already heated by `sunHeight` + cooled `inShelter`; this **extends** it. NEW `world/sunExposure.ts` (ticked after `updatePlayer`, before `updateStats`, in the pause gate) computes `ctx.player.sunExposure01` (1 = full sun, 0 = shaded) two ways: **(1)** a throttled heightfield **raymarch toward the sun** (dune shade — long low-sun shadows); **(2)** a **ray-vs-AABB slab test** against the big WRECK boxes (reusing the C28 `getSunOccluders()` silhouette boxes — a tall wreck casts a real ground shadow even at midday) + the deployable shelter zones. `survival.ts` scales heat GAIN by `sunExposure01` (floored `SHADE_HEAT_FLOOR 0.35`) + gently cools a hot player in deep shade → ducking into a wreck's shadow to cool off is now a real tactic (caps the M5a exploration arc: crest → glass → reach → **shelter in its shade**). NEW `__game.sunInfo()` debug hook.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a per-frame heat modulation reading existing geometry; no scatter-rand, no new collider).
- **Visual iteration:** logic/survival unit → **code-auditor review SHIP CLEAN** (slab math, raymarch, survival integration with temp clamps, determinism, null-safety, smoothing, perf — all sound). A NEW `--scenario=sun-probe` killed the naive terrain/shelter-zone approach as **INERT** (gentle dunes shade only at grazing sun; tiny cozy zones) → **pivoted (D242) to reusing the C28 wreck boxes as occluders**; a TARGETED probe confirmed a point on a 20m wreck's shadow side reads shaded + the sunny side clear. **FEEL** (heat floor / cool rate / raymarch thresholds) + the **coverage gap** (only wrecks ≥8m cast shade) → walk-test / backlog.
- **Spend:** ~500K (assess the heat/shelter path → build the raymarch + slab test + survival hook → a probe that caught the inert approach → the wreck-box pivot + a targeted re-probe + the code-auditor review); campaign total ~7.3M; cycle **31/50**.
- **Commit:** `c73617e`.
- **✓ MILESTONE M5a COMPLETE (C28-31):** horizon-landmark-silhouettes `[partial]` · salvaged-spyglass · vista-crest-reveal · sun-shade-exposure. **Next (cycle 32):** `M5b — Living world & tone` (wordless-prop-scenes first) — **the LAST Phase-A tier.** **Verdict: CONTINUE** — M5a done but it is NOT the last tier before the Phase-A `### Milestone:` marker (M5b remains), so the phase-level review does NOT pause here. **⚠ When M5b's last unit ships, the loop PAUSES at the Phase-A milestone for the user's walk-test + Phase-B design review.**

## Cycle 32 — M5b wordless-prop-scenes: no-text death tableaux (2026-06-19) — SHIPPED ⚡ULTRACODE · 3 gate rounds · residual polish queued
- **Planned:** `wordless-prop-scenes` — the first M5b unit: environmental storytelling with no text.
- **Shipped:** `world/wordlessScenes.ts` scatters **4 hand-composed prop tableaux** across the mid-field (radius 110-460m) that tell a story you read by looking — caps the M5a arc (crest → glass a wreck → reach it → **find who died here**). Composed from the existing slumped-seated `makeSkeleton` + simple set-dressing. **3 archetypes** (cycled): **the last fire** (a skeleton by a cold stone fire-pit + a dropped canteen), **the watcher** (slumped against a graduated stone cairn, facing the horizon), **the pair** (two skeletons leaning toward each other across a dead fire). **Determinism-safe** (a dedicated `makeRng(worldSeed^k)` stream → the scatter rand is untouched) + **decoration-only** (no colliders) + **clears scatter rocks within 5m** of each tableau for a clean stage.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (dedicated-RNG placement leaves the scatter stream untouched; no colliders). The r3 render-only tweaks keep the result valid; tsc re-confirmed.
- **Visual iteration:** adversarial gate (2 critics) → **3 gate-driven rounds.** Concept + somber tone **gate-confirmed both rounds** ("deliberate human tragedies, not random bone-piles"). r1 blobby (oversized rubble-fire, beach-ball/floating skull, dark-merged, tiny canteen) → r2 shrank+lit the props, fixed the floating skull (lowered + ellipsoidal in the shared `makeSkeleton`), bigger canteen, archetype variety, rock-clear → r3 **skull now reads** (recessed eye-socket discs + brow + nasal — the load-bearing fix both rounds flagged), **pair reads** (widened ~1.7m + inward lean + central fire), a **deliberate graduated cairn**. Shipped on self-assessment after the top blockers (the encounter/up-close view reads as death tableaux); residual far-distance prop-legibility + a backlit-skull-reads-dark-at-3q + occasional edge rocks → backlog.
- **Spend:** ~600K (assess props/hooks → compose 3 archetypes + the dedicated-RNG placement + rock-clear + the rig-shot scenario → 2 adversarial gate rounds + 3 build rounds); campaign total ~7.9M; cycle **32/50**.
- **Commit:** `72898e5`.
- **M5b unit 1/5 ✓.** **Next (cycle 33):** `M5b — wind-mood-soundscape` (AUDIO → code-auditor). **Verdict: CONTINUE** — M5b in progress; not terminal (roadmap non-empty, cycle 32/50), not a milestone (4 M5b units remain before the Phase-A marker). **⚠ The Phase-A milestone PAUSE fires when M5b's LAST unit (worm-far-horizon-crossing) ships.**

## Cycle 33 — M5b wind-mood-soundscape: a PROCEDURAL wind bed that shifts timbre by time of day (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending walk-test
- **Planned:** `wind-mood-soundscape` — make the wind/ambient bed carry mood.
- **Shipped:** **ASSESS-FIRST found the real gap:** the Session-X soundscape (`soundscape.ts`) loads wind/ambient/music from `/public/audio/`, which is **EMPTY** — the CC0 sample pack never landed, so the whole ambient bed has been **SILENT** the entire project (only `audio.ts`'s procedural SFX sounded). Per Dustfall's no-sample-files ethos, **synthesized the wind procedurally**: a looping pink-ish noise buffer fanned into a low-pass **body** (the wind's tone) + a band-pass **whistle** (a lonely moan) → the ambient bus (so the existing `setStormMuffle` master low-pass still applies). It **carries mood** — the body cutoff tracks sun height (`2600Hz` bright airy day → `620Hz` dull lonely night moan), a storm opens it toward `5200Hz` (full roar), level tracks the existing `windLvl` + a gust shimmer, the whistle is subtler by day / fuller at night. `setTargetAtTime` per frame = zipper-free. The game finally **breathes** (was silence between SFX). NEW `__game.audioState().pwind` debug fields.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (an audio-graph change; no scatter-rand, no geometry/collider).
- **Visual iteration:** logic/audio unit → **code-auditor review READY TO SHIP / CLEAN** (8 checks: produces sound, leak-free, `setTargetAtTime` smooth + bounded, mood curve correct, null-safe, no conflict with the silent sample stems, storm-muffle applies). Audio can't be heard headlessly (suspended context, no gesture) → **FEEL** (levels / cutoffs / whistle character) + the still-silent day/night-life + music beds → walk-test / backlog. **D243** (the silent-soundscape finding → procedural pivot; the recurring "verify what RUNS, not what's coded" lesson, like C31's sun-shade).
- **Spend:** ~280K (assess soundscape/samples [found silence] → synthesize the wind graph + mood modulation + the audioState exposure + a boot-check + a code-auditor review); campaign total ~8.3M; cycle **33/50**.
- **Commit:** `cd4b632`.
- **M5b unit 2/5 ✓.** **Next (cycle 34):** `M5b — rare-sky-phenomena` (VISUAL → render + gate). **Verdict: CONTINUE** — M5b in progress; not terminal (roadmap non-empty, cycle 33/50), not a milestone (3 M5b units remain before the Phase-A marker). **⚠ The Phase-A milestone PAUSE fires when M5b's LAST unit (worm-far-horizon-crossing) ships.**

## Cycle 34 — M5b rare-sky-phenomena: a dramatic rare FIREBALL/bolide (2026-06-19) — SHIPPED ⚡ULTRACODE · gate PASS in 2 rounds
- **Planned:** `rare-sky-phenomena` — occasional awe-inducing sky events that reward looking up.
- **Shipped:** **ASSESS-FIRST** found the sky already has small fast **shooting stars** (Session V) + moon + planet, so built a distinct, dramatic **FIREBALL/bolide** (`sky.ts`): rare (200-540s interval, night-only), slow (~3-4.5s, ≈4× the small shooters), with a **white-hot core** sprite inside a **warm amber halo head**, a **tapering ribbon of fading glow-puffs** (the trail, reddening toward the tail), and an **entry-flash bloom** — the "whoa, did you see that?!" moment. Brightness scales with `nightVisibility` (gone by day / under storm); spawns via `Math.random` like the existing shooters (runtime sky visual, not placement determinism). NEW `__game.triggerFireball()` + a `--scenario=fireball` rig-shot.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (a sky-render addition; no scatter-rand, no geometry/collider).
- **Visual iteration:** adversarial gate (2 critics) → **2 rounds → PASS.** r1 FAILed (read as "a soft amber dot / planet": no hot core, a 1px hairline trail, too small) → r2 added the white-hot core sprite, replaced the Line with the glow-puff ribbon, and scaled it up → **both critics PASS, `reads_as_fireball=true`, no sev2.** Residual = sev1 polish (a tighter core / crisper bloom) + a motion-dependent trail-length → backlog/walk-test.
- **Spend:** ~430K (assess the sky [shooters exist] → build the fireball [core+halo+puff-trail+flash] + the debug hook + the rig-scenario → 2 adversarial gate rounds); campaign total ~8.8M; cycle **34/50**.
- **Commit:** `3507fd4`.
- **M5b unit 3/5 ✓.** **Next (cycle 35):** `M5b — diurnal-cycle` (likely heavily already-built — ASSESS, then verify-and-defer or a small addition). **Verdict: CONTINUE** — M5b in progress; not terminal (roadmap non-empty, cycle 34/50), not a milestone (2 M5b units remain before the Phase-A marker). **⚠ The Phase-A milestone PAUSE fires when M5b's LAST unit (worm-far-horizon-crossing) ships — ~1-2 cycles away.**

## Cycle 35 — M5b diurnal-cycle: dawn/dusk tonal beats mark each day boundary (2026-06-19) — SHIPPED ⚡ULTRACODE · feel-pending walk-test
- **Planned:** `diurnal-cycle` — the day/night cycle as a tone layer.
- **Shipped:** **ASSESS-FIRST** found the day/night cycle heavily built + tuned (12-min days, `daysSurvived` → the day-7 "long storm", sky NIGHT→DUSK→DAY + light shifts + dusk warmth), so I did **not** blind-tune the pacing (a multi-session feel call → walk-test). The genuine GAP: crossing a day boundary was **silent**. Added `world/dayBeats.ts` — detects the sunrise/sunset **threshold crossings of `sunHeight`** (rising/falling edge at ±0.04, once per crossing, storm-suppressed) and plays a tonal swell: a warm hopeful rising **major** chord at DAWN ("you survived the night") + a cooler settling lower chord at DUSK ("the cold night comes"), both `playDayBeat` on the ambient bus. Gives the survival rhythm emotional weight.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (audio + a sun-height edge check; no scatter-rand/geometry/collider).
- **Visual iteration:** logic/audio unit → **code-auditor review** (crossing logic sound — fires once, frame-hop-proof, pause-safe; audio graph clean/leak-free; determinism). Caught one **sev2**: `resetDayBeats()` was exported but **unwired** → a stray dawn/dusk beat could fire on load/new-game (the module `_prevSy` persists). **Fixed** by calling it in `handoffToGame` (the common new-game/continue/title-skip path). The diurnal VISUALS were verified already-built (rendered across many prior cycles). **FEEL** (beat character/levels, day length) → walk-test.
- **Spend:** ~300K (assess the day/night system [fully built] → the dawn/dusk tones + the crossing detector + a code-auditor review + the resetDayBeats fix); campaign total ~9.1M; cycle **35/50**.
- **Commit:** `d048cb0`.
- **M5b unit 4/5 ✓.** **Next (cycle 36):** `M5b — worm-far-horizon-crossing` — **the LAST M5b unit AND the LAST Phase-A unit.** **Verdict: CONTINUE** — M5b in progress; not terminal (roadmap non-empty, cycle 35/50), not a milestone (1 M5b unit remains before the Phase-A marker). **⚠⚠ When worm-far-horizon-crossing ships (cycle 36), the loop PAUSES at the Phase-A milestone** for the user's walk-test (the large feel/polish backlog from C26-C35) + the Phase-B (M6-M10) design review.

## Cycle 36 — M5b worm-far-horizon-crossing `[partial]`: a distant worm sweeps the dunes (2026-06-19) — PARTIAL ⚡ULTRACODE · → PHASE A COMPLETE → MILESTONE PAUSE
- **Planned:** `worm-far-horizon-crossing` — the sandworm as a distant majestic presence (the LAST Phase-A unit).
- **Shipped (`[partial]`):** `world/wormHorizonCrossing.ts` — rarely a worm's dorsal ridge surfaces **far** away (430-850m, beyond detection), sweeps **across** the horizon along a tangent arc (the body undulating + riding the dune heights), throws the low **attenuated rumble**, then submerges — "that thing is out there." **Decoupled from the threat AI** (no collider, no detection, no save, no close-encounter interaction; a flat unlit fog-resistant dark silhouette ridge). The **SYSTEM ships + works** (traverse / surface-submerge / rumble; verify-clean; `Math.random` runtime-visual like the sky shooters/fireball). NEW `--scenario=worm-crossing` + `__game.triggerWormCrossing/advanceWormCrossing`.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (decoupled spectacle; no collider, no seeded-scatter draw).
- **Visual iteration:** ⚠ adversarial gate (2 critics, pixel-level) → **FAIL after 4 build / 2 gate rounds** → `[partial]`. r1 spiky cones → r2 rounded domes (beads + a detached fleck) → r3 over-merged into a smooth tadpole-blob-with-an-eye → r4 **an articulated headless rolled dark ridge** (a silhouette that pops vs the sand, scalloped hump rhythm, middle-thick taper). The gate still wants a convincing **colossal** worm — a **breach berm** (erupt THROUGH the dune), **colossal scale**, a **multi-S spine**, a **dust wake** — a real art pass (**backlog §A**; **D244**). Shipped `[partial]` rather than thrash further at the tail of the run; the worm-look is part of the user's Phase-A review.
- **Spend:** ~620K (assess the worm AI → build the decoupled crossing [traverse/surface/rumble + the rig-scenario + debug hooks] → 4 build rounds + 2 adversarial gate rounds on the silhouette); campaign total ~9.7M; cycle **36/50**.
- **Commit:** `703d5dd`.

## ⏸ MILESTONE — PHASE A: BUILD-OUT COMPLETE (M1→M5b) — CAMPAIGN PAUSED (after cycle 36)
- **The unattended Phase-A block is DONE.** M1→M5b all shipped over cycles 1-36 (this overnight ran 13-36). The loop **PAUSES** at the `### Milestone: Phase A — Build-out complete` marker (checkpoint=milestone).
- **State:** `status:paused`, `awaiting_approval:true`, `stop_reasons:["milestone-review"]`. **No ScheduleWakeup — the loop has STOPPED.**
- **AWAITING the user (before `/campaign-approve`):** (1) **walk-test** the whole Phase-A build (a large FEEL batch is owed — C23-C36, see the next-session-prompt hand-off list); (2) **art-direct the two `[partial]` looks** (C28 horizon-silhouette impostor + C36 colossal-worm); (3) decide the **silent day/night-life + music beds** (C33); (4) the **Phase-B (M6-M10) design review** (design-gated). 
- **To resume:** `/campaign-approve` releases Phase B → then `/loop /campaign-cycle`. **Verdict: STOP (milestone-review).**

## ▶ PHASE B — APPROVED + RELEASED (2026-06-20) — campaign RESUMED
- The user approved the `proposal-cycle-37` review (equivalent of `/campaign-approve` at the Phase-A milestone gate). `status:active`, `awaiting_approval:false`, `max_cycles` → **75**. Phase B = **M6–M10 FULL** (M7 included). Design calls baked into the roadmap + proposal: survival = forgiving Long Dark (flip `GOD_MODE` off); HUD-removal behind `FEATURES.diegeticSurvival` + a default-ON pause-menu opt-in; **flip-authority = AUTONOMOUS** (the loop may flip `FEATURES.*`/kill-switches once gates pass; reversible; user vetoes FEEL at the Phase-B review; **the D81 save-bump rule still STOPs the loop**); cave = ONE + ramp + no-horror; hover-bike = repairable-speeder; machete = new id; crafting pair = `fire_kit` vs `signal_kit`. NOTE: D1 "Skyfall" (the M7 crashing-ship-event) shipped during the pause via `/loop` OUTSIDE the cycle count → M7 unit DONE, `cycles_completed` stayed 36. The loop now runs M6→M10 unattended, pausing only at the **Phase B milestone** (after M10).

## Cycle 37 — M6 ① crafting-chooser-colliding-recipe: a NEW signal_kit flare lights the dormant chooser (2026-06-20) — SHIPPED ⚡ULTRACODE · → PHASE B BEGINS
- **Planned:** lead Phase B with the guaranteed win — light up the already-built, idle multi-match crafting **chooser** (ACAS B3) via ONE data-level recipe collision.
- **Shipped:** a NEW **`signal_kit` (SIGNAL FLARE)** sharing `fire_kit`'s exact recipe (`branch×3 + scrap×1`) → combining those materials now surfaces a 2-way **"fire kit OR signal flare"** chooser ("warm yourself **OR** call out"). `signal_kit` = a **one-shot transient flare**: LMB fires a bright ember-trailing arc skyward, consumed; `world/signalFlare.ts` (additive sprites only — no dynamic light → no shader-recompile hitch; never touches `ctx.fires`/save). NEW files/hooks: `signalFlare.ts`, `playSignalFlare` (audio), `SIGNAL_FLARE_*` (tuning), the flare item (def + viewmodel + icon), recipe id 18, ghost-ring exemption, `__game.fireSignalFlare`, a `signal-flare` rig scenario, + extended `craft-chooser` to assert the REAL collision.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25. **Primary deliverable verified headless:** the `craft-chooser` scenario reports `real=true` (`branch×3+scrap×1 → ["fire kit","signal flare"]`, CRAFT gated; single/none controls correct). **No save bump** (additive item + additive recipe id — the C29 spyglass precedent).
- **Visual iteration:** adversarial gate (2 critics) on the flare FX, **2 rounds**. R1 FAIL (convergent sev2s: beaded trail · no cooling gradient/taper · reads straight not arced · daytime head blow-out) → R2 fixed (dense overlapping ember ribbon + hot→cool lerp + size taper + wind drift + tighter halo + apogee framing/lean) → **BOTH critics PASS, no sev≥2**. Residual sev3 nits → backlog §A. In-motion FEEL → Phase-B walk-test.
- **Decisions:** **D245** — `signal_kit` shares `fire_kit`'s recipe (not the proposal's `scrap×2+branch×1`, which `scrap_bar` id 15 already owns → would make a 3-way + change fire_kit's recipe); the flare is a transient consumable, not a fire (no save-bump risk; genuine either/or). friction 1.
- **Spend:** ~250K (net-new item + transient FX module + a 2-round visual gate [4 critics ~135K] + 4 rig renders + verify:all); campaign total ~9.95M; cycle **37/75**.
- **Commit:** `4efbea2`.
- **M6 unit 1/4 ✓.** **Next (cycle 38):** **M6 ② survival-rebalance-newgame** (the KEYSTONE — flip `GOD_MODE` off + tune a forgiving Long-Dark curve in `tuning.ts`; hard-dep for HUD removal + M10's economy). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 37/75), not a milestone (the Phase-B marker is after M10; this is the 1st of ~17 units).

## Cycle 38 — M6 ② survival-rebalance-newgame (the KEYSTONE): GOD_MODE off + a forgiving Long-Dark curve + health regen (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** make survival REAL (the game shipped with `GOD_MODE:true` → the player never died) without making it punishing — the M6 keystone + a hard-dep for HUD removal + M10's economy.
- **Shipped:** flipped `GOD_MODE` + `DEBUG_UNLIMITED_STAMINA` **OFF** and tuned a **forgiving Long-Dark** curve to *computed* time-to-death targets (not blind feel): a PREPARED player (drinks/eats/shelters) survives **indefinitely** + slowly **HEALS** (NEW health-regen-when-provisioned block in `updateStats`); a NEGLECTED player dies in **~8–10 min** on the urgent paths (heat/cold/thirst), **~15 min** on hunger. Slowed the urgent drains ~4–5× + softened every damage rate ~4× so a depleted stat is a recoverable spiral, not a cliff (shade/shelter still cool/warm an order of magnitude faster → retreat is always a way out). The `die()` godmode floor is now gated on `Tuning.GOD_MODE || ctx.flags.devMode` (**D246**) so DEV-mode boots stay safe (rig-shots, feature walk-tests) while REAL new-games are lethal. NEW `__game.survivalProbe` (deterministic time-to-death, bypassing the dev floor) + `__game.triggerDeath` (forces the real death path + reports the overlay un-hid) + a `survival-probe` rig scenario.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25. **NEW `survival-probe` gate PASS** (heat 7.54 / cold 8.67 / thirst 10 / hunger 15 min; prepared never-dies + heals 0.3→1.0; death-overlay un-hides). **Code-auditor adversarial review PASS** (no sev≤2 — regen/damage can't overlap, the godmode gate is correct, the probe restores all 12 mutated fields). **No save bump.**
- **Visual iteration:** N/A — logic/balance unit, no visual output. The FELT survival pacing + the re-enabled stamina cadence (6s sprint/9s recover, never play-tested) are **FEEL-pending the Phase-B walk-test** (the user vetoes FEEL at the review). The BALANCE was validated numerically (the probe) + adversarially (the code-auditor), which is the right depth for a no-visual unit.
- **Decisions:** **D246** — forgiving Long-Dark curve via computed time-to-death targets + a dev-mode-gated godmode floor + health-regen-when-provisioned. friction 2 (the curve is gate-pinned but the FELT pacing is inherently walk-test-only).
- **Spend:** ~200K (assess the survival model → design the curve → retune 8 constants + add regen + the devMode gate → build the probe + triggerDeath hooks + the rig gate → 1 code-auditor agent ~68K → verify:all); campaign total ~10.15M; cycle **38/75**.
- **Commit:** `64eb812`.
- **M6 unit 2/4 ✓ (the KEYSTONE).** **Next (cycle 39):** **M6 ③ flat-color-texture-audit** (scope-first — upgrade the ~6–8 weakest flat-shaded surfaces via the existing 9 procedural shader factories; zero new programs/assets). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 38/75), not a milestone (the Phase-B marker is after M10; 2 of ~17 units done).

## Cycle 39 — M6 ③ flat-color-texture-audit: 8 flat surfaces → existing procedural shaders, ZERO new programs (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** scope-first — fix the ~6–8 weakest flat-shaded surfaces the player sees up close via the EXISTING procedural shader factories (zero new programs/assets; NOT the D107 PBR fork).
- **Shipped:** an Explore-agent scan ranked the candidates; upgraded **8 surface-groups** — fire logs + tent/largeTent poles → `createWoodGrainMaterial` (aged deadwood + bark); bedroll pad/pillow/blanket + journal cover/spine + largeTent rug → `createFabricMaterial` (woven canvas/leather); journal black-box body + lantern wires → `createMetalMaterial` (weathered/scratched). NEW `__game.campStudio()` (deploy the camp objects in a row + report the program count) + a `camp-studio` rig scenario as a reusable render gate.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25. **OBJECTIVE CONSTRAINT proven DEFINITIVELY:** a git-stash A/B of the 6 material files through the camp-studio render gave **programs 82 = 82** (flat vs factory) → **zero new shader programs** (the audit's hard requirement; the factories all pre-exist in-scene, and `bark` is a runtime uniform not a shader fork — D247).
- **Visual iteration:** appearance gate PASS — 1 adversarial critic on the camp render (fire/bedroll/lantern): reads as aged wood / woven cloth / weathered metal, colors preserved, no broken surfaces, no sev≤2 (took ~4 framing iterations to get a usable close shot — the harness only had distant/held-item scenarios). Residual sev3 nits + the journal/tent-pole close-up reads (factory-transitivity-covered) → backlog/walk-test.
- **Decisions:** **D247** — swap flat Lambert to the EXISTING world-space factories (locker wood, tents fabric, wrecks metal), proven zero-new-programs by a git-stash A/B render. friction 1.
- **Spend:** ~210K (1 scan agent + 8 swaps + the camp-studio render harness + ~4 framing iterations + 1 critic + the git-stash A/B proof + verify:all); campaign total ~10.36M; cycle **39/75**.
- **Commit:** `4372e29`.
- **M6 unit 3/4 ✓.** **Next (cycle 40):** **M6 ④ remove-hud-stat-bars** (behind `FEATURES.diegeticSurvival` + a pause-menu opt-in; diegetic survival tells replace the HUD bars) — **COMPLETES M6**. **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 39/75), not a milestone (the Phase-B marker is after M10; M6 completing does not pause per checkpoint=milestone).

## Cycle 40 — M6 ④ remove-hud-stat-bars: diegetic survival replaces the HUD bars → M6 COMPLETE (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** the M6 finale — remove the always-on HUD stat bars + surface survival state DIEGETICALLY (the payoff of C38's real survival).
- **Shipped:** per-stat screen-edge VIGNETTES (cold=blue, thirst=sepia [both boosted ×1.7 in diegetic mode so they're the primary readout], NEW **heat**=amber, **hunger**=sickly-green starvation pall, **low-health**=blood-crimson PULSE) + procedural AUDIO (NEW `playHeartbeat` quickening as health drops, `playStomachGrowl` while starving). Behind `FEATURES.diegeticSurvival` (master flag, **flipped ON** — bars hidden by default) + `settings.diegeticSurvival` (a pause-menu opt-in, default-ON; the bars are the always-available floor). NEW `ui/diegeticMode.ts` shared state, `hud.setStatsBarsVisible`, `statVignette.ts` heat/hunger/health additions, `__game.diegeticProbe`/`showDiegeticVignette` + `diegetic-probe`/`diegetic-vignette` rig gates.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25. **NEW `diegetic-probe` gate PASS** (each stat lights only its tell; the bars hide/show on toggle). **No save bump** (a localStorage setting, not the save schema).
- **Visual iteration:** appearance gate PASS in **3 ROUNDS** (2 critics). r1 too faint over the bright desert → r2 boosted + bolder gradients + FIXED a render footgun (a CSS opacity transition lagged a single-frame screenshot → disable the transition in the render hook; without this I'd have wrongly over-cranked them) → r3 a critic FAILed heat-vs-health as indistinguishable (both warm-red) → separated the hues (heat amber-orange / health blood-crimson) + a sickly-green hunger → PASS, all four distinct + readable. The FELT read ("survive without bars?") is **walk-test-pending**.
- **Decisions:** **D248** — diegetic survival design (flag + default-ON opt-in; per-stat vignettes + audio; the hue-separation lesson + the CSS-transition-vs-single-frame-render footgun). friction 2 (the load-bearing "is it survivable without bars" question is walk-test-only; the user may veto HUD-removal at review).
- **Spend:** ~300K (a large UI/FX/audio unit + 3 visual iteration rounds + 3 adversarial critics + the transition-bug diagnosis + verify:all); campaign total ~10.66M; cycle **40/75**.
- **Commit:** `e537ca8`.
- **M6 unit 4/4 ✓ → M6 COMPLETE** (① chooser · ② survival · ③ flat-color · ④ diegetic HUD). **Next (cycle 41):** **M7 ⑤ procedural-wreck-overhaul** (net-new structure axes in the socket grammar; wider/weirder ships). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 40/75), not a milestone (the Phase-B marker is after M10; M6 completing does NOT pause per checkpoint=milestone).

## Cycle 41 — M7 ⑤ procedural-wreck-overhaul: new structure axes make wrecks read wider/weirder, not tubes (2026-06-20) — SHIPPED ⚡ULTRACODE · M7 BEGINS
- **Planned:** fix the long-standing "every wreck is a long tube" complaint — net-new structure axes in the socket grammar.
- **Shipped:** ASSESS-FIRST (a recon agent) found the socket-grammar `derelict` archetype already builds non-linear hulls but was under-varied + under-weighted. Extended it ADDITIVELY: 2 NEW components — **`splayedEngineCluster`** (a fanned 3-4-bell stern) + **`dorsalMast`** (a tall housing'd sensor spike); **`assembleDerelict` grew 3→5 forms** (trimaran with pod NOSE cones · stacked tower · NEW mast · NEW asymmetric outrigger+mast · NEW layered tower+mast — **no bare-tube form left**); the hull was **elongated** (a short-fat roll can't read as a blob); `list` 0.20→0.26 + `bury` 0.55→0.45; **`ARCH_WEIGHTS` shifted ~0.08 `ship`→`derelict`** per biome so the wider/weirder hulls show ~as often as the legacy tubes. + a NEW rig **`--pinyaw`** length-frame.
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders 0/25 (the new components declare colliders + are phash-deterministic; the salvage-panel stream stays stable).
- **Visual iteration:** appearance gate PASS in **3 ROUNDS** (recon agent + 5 adversarial critics). r1 FAIL (thin pin-mast · a bare tube · an asymmetric blob+stick · a loose-barrel trimaran · the splay under-shown) → r2 (thicker housing'd mast · layered tower+mast replaces the bare tube · pod nose cones · bigger asymmetric outrigger · splay rate ↑) → r3 (elongated hull + the `--pinyaw` LENGTH-FRAME — the "blob" was an **END-ON camera artifact**, not a bad asset) → BOTH critics PASS, 4 distinct silhouettes, no broken geometry. In-world FEEL → walk-test.
- **Decisions:** **D249** — extend the socket-grammar `derelict` (NOT migrate the legacy ship, D227 deferral kept) with new structure-axis components + a length-frame rig pin; **the end-on-framing footgun** (a Z-long subject in an X-long-assuming framer reads as a blob — render another angle before "fixing" the asset). friction 2.
- **Spend:** ~400K (recon agent + 2 components + the 5-form rewrite + elongation + re-weight + the rig pin + 3 visual rounds + 5 critics + verify:all ×3); campaign total ~11.06M; cycle **41/75**.
- **Commit:** `69765a9`.
- **M7 unit 5/7 ✓.** **Next (cycle 42):** **M7 ⑥ more-wreck-types-new-pois** (watchtower/debris-trail/well via the socket grammar). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 41/75), not a milestone (the Phase-B marker is after M10).

## Cycle 42 — M7 ⑥ more-wreck-types-new-pois [partial]: a NEW watchtower landmark (a walkable ramp-vantage) (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** add NEW non-ship POIs (watchtower / well / debris-trail) via the socket grammar.
- **Shipped (`[partial]`):** the **WATCHTOWER** — a scavenged-metal desert lookout (4 braced corner legs [cross-ties + diagonal braces] → a railed deck under a wide hipped sun-shade roof) with a straight **WALKABLE ramp** up to the deck (a "ramp-vantage", NOT a ladder movement-mode). NEW `watchtower()` component (legs/deck/ramp declare colliders; the braces/railing/roof/posts/rails/mid-strut are `isWreckDecoration`) + NEW `assembleWatchtower` archetype + `ARCH_WEIGHTS` (~0.07/biome, rarest in the wreck_yard) + **added to the `collider-audit` default list** (25→30 audits). The ramp is a real inclined-box collider at **~37°** (< the KCC ~50° climb limit) so the player walks up; the archetype `list` is tiny (0.04) so the slope stays walkable. **D250.**
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 · colliders **0/30** (the watchtower is now gate-audited). Determinism holds (one `seedOf` draw, the rest phash).
- **Visual iteration:** appearance gate PASS in **2 ROUNDS** (2 critics). r1 FAIL/split (too squat → a raised hide · a tiny floating-parasol roof · a narrow ambiguous ramp) → r2 (TALLER ~1.6× + diagonal X-bracing + thicker legs · a WIDE roof overhanging the deck on visible posts · a wider ramp overlapping the deck + a mid-support strut) → PASS, reads as an intentional scavenged lookout. The walkable-ramp **FEEL → walk-test**.
- **Decisions:** **D250** — the watchtower's ramp-vantage as a WALKABLE inclined collider at a KCC-safe slope (not a new ladder movement-mode); + the load-bearing reminder that a new archetype must be ADDED to the `collider-audit` list or its coverage isn't gate-verified. friction 1.
- **Spend:** ~250K (the watchtower component + archetype + 2 visual rounds + 3 critics + verify:all ×2); campaign total ~11.31M; cycle **42/75**.
- **Commit:** `76b6ccd`.
- **M7 ⑥ `[partial]` — watchtower DONE (1 of ~3 new POIs).** **Next (cycle 43):** **⑥ continued** — the well/cistern + the debris-trail. **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 42/75), not a milestone; a partial-SHIPPED (the watchtower is complete + gate-passed) is progress, not a stuck wall (`consecutive_no_progress` stays 0).

## Cycle 43 — M7 ⑥ more-wreck-types-new-pois COMPLETE: the WELL + the DEBRIS-TRAIL (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** finish ⑥ by adding the 2 remaining non-ship POIs (well/cistern + debris-trail) via the socket grammar.
- **Shipped:** ⑥ **COMPLETE** (watchtower was C42). (1) **WELL** (a scavenger water cache) — NEW `wellHead()` component: a metal CURB ring (taller, irregular salvaged rim-plates, a chunky cap band) around a **RECESSED dark shaft** (an inner wall dropping into shadow + a near-black `_shaftVoidMat` void floor → reads as a HOLE, not a lid) with a 2-post **WINCH** (a thick windlass DRUM + an offset L-crank + grip + a rope + a banded hanging bucket); curb + the 2 posts declare colliders, the rest is `isWreckDecoration`. NEW `assembleWell` archetype. (2) **DEBRIS-TRAIL** (a directional crash-ejecta streak, vs `debris_field`'s scattered disc) — NEW `assembleDebrisTrail`: a heavy lootable **IMPACT chunk** (1.7× scale, tilted, bedded deep) at the deep end with a dark skid streak + crater, and debris trailing back getting **SMALLER** on a strict monotonic gradient along a tight centreline; reuses `debrisPiece`, now extended with a **`scale` param** (multiplies geo + collider + bbox together; default 1 = byte-identical to `assembleDebris`); NEW dark `_skidMat`. Both registered in `ARCH_WEIGHTS` + the **`collider-audit` list (30→40 audits)**.
- **Housekeeping:** cleared the **C40-overdue `decisions.md` archival** — rolled D189–D206 into `decisions-archive.md` (active back to ~45: D207–D251); both headers + the pointer note updated; count script-verified (188+18=206).
- **Verify:** `verify:all` PASS — tsc · placement 0/0 ×5 (determinism holds with both new archetypes) · colliders **0/40** (well + debris_trail covered + audit-listed).
- **Visual iteration:** appearance gate PASS (2 critics, one per POI). **WELL — 2 ROUNDS:** r1 FAIL (bare-gallows winch · capped-looking mouth · thin clean curb) → r2 (thick windlass drum + L-crank · recessed shaft void · taller curb w/ rim-plates + gussets) → PASS. **DEBRIS-TRAIL — 3 ROUNDS:** r1+r2 FAIL (reads as a centrally-clustered PILE, redundant w/ `debris_field` · gradient not monotonic · scorch invisible · impact chunk not dominant) → r3 (longer 15-19m tight line + clear gaps + a strict small→big gradient + a dark skid/crater + a decisively-bigger tilted impact chunk) → PASS (directional across the set). Sev-3 nits → backlog §A. The walkable-up + eye-pull **FEEL → walk-test**.
- **Decisions:** **D251** — a DIRECTIONAL procgen feature needs a strict monotonic gradient on a tight axis + visible 3D mass, NOT a flat ground decal (the trail's 3-round lesson); + the reusable `debrisPiece` `scale` param + the well's recessed-shaft / thick-drum / built-mass reads. friction 1.
- **Spend:** ~400K (2 POIs + the `debrisPiece` scale refactor + 2 new materials + 5 gate rounds total + 6 critic-passes + the decisions archival + verify:all); campaign total ~11.71M; cycle **43/75**.
- **Commit:** `345994e`.
- **M7 ⑥ COMPLETE (watchtower + well + trail = 3 new non-ship POIs).** **Next (cycle 44):** **M7 ⑦ walkable-wreck-interiors** — the LAST M7 unit (XL, spike→build: generalize D1's enterable `crash_husk`). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 43/75), not a milestone (a UNIT completed, not M7-the-milestone; the Phase-B pause is after M10). `consecutive_no_progress` stays 0.

## Cycle 44 — The solitude pass: remove the watchtower + ruin the well (steering-driven) (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Steering received:** user inbox note — *"remove the watchtower… everything should be a wreck/decayed for a hundred years… i want the player to feel alone and have almost no signs of living human life, this goes for pois as well."* → processed at the cycle-44 boundary; archived to `steering-archive.md`, recorded as a vision-delta + **D252**. Confirmed the scope with the user (interactive): remove watchtower + well are the targets (older wreck POIs left as-is); the **well kept but de-peopled into a ruin**.
- **Planned:** apply the solitude principle — strip the two C42/C43 POIs that read as recent, maintained, inhabited infrastructure.
- **Shipped:** (1) **Removed the `watchtower` archetype entirely** — `watchtower()` component + `assembleWatchtower` + its `ARCH_WEIGHTS` rows (all 4 biomes) + its collider-audit-list entry. (2) **Re-scoped the `well` from a maintained water cache into a long-DRY RUIN** — kept the curb + the recessed dry shaft, but COLLAPSED the winch: a tall leaning broken post + a snapped stub (both still declare colliders) + a fallen cross-beam dropped across the mouth + ~40% of the rim-plates slumped onto the sand + scattered rubble. NO rope, NO bucket, NO working crank/drum. Lowered/weathered the curb. (3) Codified the principle: **D252** + a new `docs/vision-deltas.md` (a standing constraint on every future POI, incl. M7 ⑦).
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders **0/35** (the collider-audit list drops 40→35: 7 archetypes × 5 seeds, watchtower removed; the well covered by curb + 2 posts). **Footgun caught + fixed:** widening the curb's base radius (curbR×1.08→×1.10) pushed the curb's square AABB corners just past the curb collider's AABB+tol on a large-radius seed (s2024, cover 19%) — the audit AABB-samples, so a cylinder collider's `radius` must cover the mesh's MAX (base) radius, not the nominal. Fix: curb collider `radius: curbR*1.1`.
- **Visual iteration:** appearance gate — re-rendered the well at seeds 1/42/1337 (`--scenario=procgen-wreck --archetype=well`); reads as an abandoned collapsed dry-well ruin across seeds (no maintained-infrastructure read). 1 refinement round (dropped the fallen beam onto the curb so it reads collapsed, not propped). The "floating piece" in s1 was a distant background wreck, not a defect. The watchtower removal is pure deletion (no render needed). FEEL (does the world read as solitary in-play) → walk-test.
- **Decisions:** **D252** — the solitude principle (POIs must read as century-decayed wreckage, never recent/maintained human infrastructure). friction 1 (clean reversible removal/re-scope, but now a TONE INVARIANT the campaign must hold).
- **Spend:** ~250K (steering processing + 2 component edits + archetype/weights/audit-list cleanup + 2 visual rounds + the doc suite); campaign total ~11.96M; cycle **44/75**.
- **Commit:** `dbd0d75`.
- **Next (cycle 45):** **M7 ⑦ walkable-wreck-interiors** — the LAST M7 unit (XL, spike→build), now constrained by D252 (the enterable interior must read as decayed wreckage). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 44/75), not a milestone (the Phase-B pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 45 — M7 ⑦ walkable-wreck-interiors: enterable wrecks join the ambient scatter → M7 COMPLETE (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** generalize the D1 enterable `crash_husk` so WALKABLE-interior wrecks appear in the ambient procedural scatter (the last M7 unit; an A/B architecture spike first).
- **Recon (fanned to an Explore agent):** mapped the machinery — **there is NO portal/teleport mechanic**; `crash_husk` is enterable purely because `huskShell` is a hollow shell (open torn ends + open top) with ONLY side-wall colliders + `auditExempt`, so the player walks IN. `dressCrashInterior` is a reusable role-driven interior kit in the same file. **SAVE verdict: NO bump** (entering touches zero save state; an ambient archetype spawns from the world seed). → cleared the one D81 STOP risk this cycle had.
- **Shipped (Option B — a new archetype, not a decorator, D253):** NEW `enterable_wreck` archetype — reuses `huskShell` + `dressCrashInterior`, registered in `ARCHETYPES` + `ARCH_WEIGHTS` (rare; commonest in the wreck_yard, rarest in open dune). **Determinism pin:** spends exactly ONE shared-stream draw (`seedOf`); role via `phash(s,7)`; the variable-count interior dressing runs on an **isolated `makeRng(s)`** so the salvage-panel stream never desyncs. **D252 pin:** added an `aged` flag to `dressCrashInterior` → a DEAD console (no emissive glow) for the century-old wreck (a fresh crash keeps the flicker). Renormalized the `ARCH_WEIGHTS` rows to sum to 1.0 (were ~1.04, compressing the tail; slack trimmed off the legacy `ship` tube).
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** (determinism holds; panel counts shifted with the renormalized weights but 0 bury-fails) + colliders **0/40** (8 archetypes × 5 seeds; `enterable_wreck` passes via the hollow-shell exemption + side-wall colliders).
- **Visual iteration:** appearance gate PASS — rendered `--scenario=procgen-wreck --archetype=enterable_wreck` at seeds 1/42/1337 (3q/front/zoom). Reads as a large hollow walkable hull (s1 torn-end cavity; s42 open ribbed trough with a dark furnished interior); interior reads DEAD (no powered glow → D252). s1337's 3q faces the closed flank (an orientation/framing artifact, D249, not a defect). The up-close interior detail + the walk-IN traversal/dark-nav FEEL → walk-test (the rig can't place a 1st-person interior cam).
- **Decisions:** **D253** — generalize via a self-contained `enterable_wreck` archetype (not a decorator); isolated dressing RNG for determinism; `aged` dead-console for D252; no save bump. friction 2 (interior FEEL is walk-test-only; the ambient wreck is coupled to the crash interior kit).
- **Spend:** ~350K (1 recon agent + the archetype + the determinism/aged pins + the weight renorm + 5 render passes + the doc suite + verify:all); campaign total ~12.31M; cycle **45/75**.
- **Commit:** `15d6e02`.
- **M7 COMPLETE** (⑤ overhaul · ⑥ new POIs · C44 solitude pass · ⑦ interiors). **Next (cycle 46):** **M8 ⑧ deep-cave-design-spike** (A/B worktree — collision topology; writes `feature-deep-cave.md`). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 45/75); **NOT a milestone pause** (M7 is not the last tier before the Phase-B marker — M8/M9/M10 remain; the pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 46 — M8 ⑧ deep-cave-design-spike: pick the cave collision topology (2026-06-20) — SHIPPED ⚡ULTRACODE · M8 BEGINS
- **Planned:** the design spike that de-risks the cave before the XL build — pick the collision-topology approach + write `docs/feature-deep-cave.md`. The hard blocker: the world ground is a Rapier heightfield (one Y per XZ) which cannot enclose an underground volume.
- **Recon (fanned to an Explore agent):** mapped terrain/physics/KCC + precedents. Key finds: (1) the **Sarlacc crater** already carves the heightfield + mesh bit-identically from a seed anchor (`terrain.ts:163-173`) — deterministic, no save state → the proven descent-carve path; (2) the KCC (50° max slope, 0.3 m autostep/snap) walks fine under a roof + on an enclosed floor, so a ~37° funnel + a roof collider work with no new movement mode (D125); (3) colliders via the cheap box/cylinder `attachDeclaredColliders` kit; (4) save needs NO bump (⑨'s companion state is an additive `companionEggTaken?` field). 
- **Shipped (the spike deliverable — a design doc + a decision, NO gameplay code):** chose **Option A** — a Sarlacc-style terrain FUNNEL descent + a box/cylinder room-kit enclosed interior (walls + a real roof collider + ≤37° ramps + a doorway gap via the hollow-shell `auditExempt` idiom), dressed dead (D252), dark-nav via a cheap ambient-darken-below-Y + torch glow. Rejected (B) trimesh/marching-cubes (needs a new voxel subsystem; clip risk) and (C) a kit with no carved descent (folded into A). Wrote `docs/feature-deep-cave.md` (full spec + the ⑨/⑩ sub-task breakdown + success criteria) + **D254**.
- **Verify:** docs-only cycle (no `src/`/`scripts/` change — `git status src scripts` empty) → `src` byte-identical to C45; `tsc` re-confirmed clean; placement 0/0 ×5 + colliders 0/40 hold from C45 (the C11/C23/C25 docs-only precedent).
- **Visual iteration:** N/A — a design-spike cycle (no visible prototype shipped; the deliverable is the doc + the D-entry).
- **Decisions:** **D254** — cave = Sarlacc funnel + a box/cylinder room-kit interior (not a trimesh carve); ambient-darken dark-nav; additive `companionEggTaken?` save field, no bump. friction 2 (low-risk topology reusing 3 proven subsystems, but ⑨ is the campaign's biggest single net-new BUILD + its dark-nav FEEL is prototype/walk-test-only).
- **Spend:** ~250K (1 recon Explore agent + the topology analysis + `feature-deep-cave.md` + D254 + the doc suite); campaign total ~12.56M; cycle **46/75**.
- **Commit:** `995f5a2`.
- **Next (cycle 47):** **M8 ⑨ deep-cave-build** (XL — the seeded `caveAnchor` + the funnel carve + the room-kit interior + dark-nav, per `feature-deep-cave.md`). **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 46/75); NOT a milestone pause (⑧ is a unit within M8; the Phase-B pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 47 — M8 ⑨ deep-cave-build [partial]: the cave descent FUNNEL (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** the XL cave build per `feature-deep-cave.md` — the seeded anchor + the funnel carve + the enclosed interior + dark-nav.
- **Shipped (`[partial]` — the foundation, the riskiest core-terrain piece):** the **cave DESCENT FUNNEL**. A seed-derived **`caveAnchor`** (`world/biomes.ts` — far [520-1150 m], away from spawn/graveyard/Sarlacc pit; rng appended after the Sarlacc anchor → deterministic) + `caveAt()` falloff, a **funnel carve** in `world/terrain.ts` (the proven Sarlacc recessed-funnel technique: a soft rim lip, a ~39° mid-wall < the KCC 50° climb limit [D125], a flat floor — carved into the shared heights → mesh + Rapier heightfield + `heightAt()` all dip together, the player physically walks down), a **dark cave-mouth coloring** (darker than the Sarlacc pit), `Tuning.CAVE_PIT_*`, and a NEW `cave` rig scenario. **The enclosed roofed interior + dark-nav + the companion-egg site → cycle 48 (the `[partial]` continuation).**
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** + colliders **0/40**. Determinism win: the panel counts are IDENTICAL to C45/C46 (74/68/83/87/93) → the biome sampler's rng is ISOLATED from the POI-scatter stream, so the new `caveAnchor` draws perturbed nothing; the carve buried no panels (0 occ-fails).
- **Visual iteration:** appearance gate PASS (`--scenario=cave --angle=aerial`) — reads as a distinct DARK recessed funnel mouth in the open desert (an 8.8 m carve at anchor [471,144]); the dark coloring + the depression read as a descent into the earth. The `approach` angle's framing hid the funnel behind the near dune lip (a rig-framing nit → backlog). **Footgun fixed:** the heavy-world screenshot flakes ("fonts loaded" → 30 s timeout) — moved the numeric `console.log` BEFORE the screenshot + wrapped it in try/catch (the carve numbers survive a screenshot flake). The interior LOOK + the descent/dark-nav FEEL → ⑨-continuation + walk-test.
- **Decisions:** none new — this executes D254 (the C46 topology decision).
- **Spend:** ~300K (the anchor + carve + coloring + the rig scenario + the screenshot-robustness fix + verify:all + the doc suite); campaign total ~12.86M; cycle **47/75**.
- **Commit:** `b09a136`.
- **Next (cycle 48):** **M8 ⑨ continued** — the enclosed roofed interior module (box/cylinder room-kit at the funnel floor: walls + roof collider + ≤37° ramps + a doorway gap, audit-listed) + the ambient-darken dark-nav + decayed dressing, per `feature-deep-cave.md`. **Verdict: CONTINUE** — not terminal (roadmap non-empty, cycle 47/75); not a milestone; a `[partial]`-SHIPPED is progress (the funnel is complete + gate-passed). `consecutive_no_progress` stays 0.

## Cycle 48 — M8 ⑨ continued [partial]: the enclosed roofed CHAMBER at the funnel floor (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** the enclosed interior module at the cave funnel floor (per `feature-deep-cave.md`) — turn the C47 descent funnel into a space the player walks INTO.
- **Shipped (`[partial]` — the interior SHELL):** NEW `world/deepCave.ts` — `spawnDeepCave(scene, world, terrain, caveAnchor)` places a ROOFED CHAMBER on the carved funnel floor: 4 walls + a roof + a SPLIT entrance wall leaving a DOORWAY gap (+ a lintel), all declared as box colliders via `attachDeclaredColliders` (7 meshes : 7 colliders). Wired into `main.ts` (a fixed feature, one per world, spawned after the Sarlacc pit). `Tuning.CAVE_ROOM_*`. **Key geometry call:** NO separate floor slab — the player walks the CONTINUOUS funnel terrain inside the room (a flat slab at the deepest centre would sit below the bowl-shaped terrain at the room edges → an unclimbable >0.3 m step at the doorway); walls instead extend ~1 m below the centre floor to seal into the rising bowl (no under-wall gap). Roof kept tall (~3.8 m centre / ≥2.3 m edges) so it clears the KCC 0.3 m snap-to-ground.
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** (counts identical to C47 → deterministic; the cave is a fixed module, not in the scatter) + colliders **0/40** (the archetype audit; the cave is NOT an archetype so it's not in that sweep — its 7 colliders are declared explicitly + structurally confirmed via a `cave`-scenario mesh/collider count: `caveFound:true caveMeshes:7`).
- **Visual iteration:** PARTIAL — extended the `cave` rig scenario with `interior`/`inside` angles. The renders confirm the chamber's walls + floor + dark enclosure EXIST, but **the interior is inherently too dark for a full LOOK gate until the dark-nav lighting lands (next cycle)** — that gate legitimately belongs with the dark-nav work. Structural soundness confirmed (7 colliders at the funnel floor). The walk-IN FEEL (KCC under the roof, the descend→enter transition, getting in/out) is **walk-test-gated** (the spec flagged this).
- **Decisions:** none new — executes D254.
- **Spend:** ~330K (the deepCave module + the geometry-soundness reasoning + main wiring + the rig interior angles + collider sanity + verify:all + the doc suite); campaign total ~13.19M; cycle **48/75**.
- **Commit:** `528b6d8`.
- **Next (cycle 49):** **M8 ⑨ continued** — the **dark-nav** (ambient-darken-below-Y + torch glow, so the interior is dark-but-navigable AND visually gate-able) + decayed dressing (D252) + multi-chamber depth + the M8 ⑩ companion-egg site. **Verdict: CONTINUE** — not terminal (cycle 48/75); not a milestone; a `[partial]`-SHIPPED is progress (the interior shell is built + gate-verified). `consecutive_no_progress` stays 0.

## Cycle 49 — M8 ⑨ continued [partial]: cave DARK-NAV (ambient-darken + torch glow) (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** the dark-nav (the priority — it makes the cave navigable AND unblocks the interior visual gate) + dressing, per `feature-deep-cave.md`.
- **Shipped (`[partial]` — the dark-nav):** `updateDeepCave(ctx, cave)` in `world/deepCave.ts`, wired into the `main.ts` tick after `updateLighting`/`updatePlayer`. As the player descends into the cave (depth below the funnel rim × horizontal proximity to the anchor → a `d` factor), it multiplies `ctx.lights.ambient`/`sun` down to a floor (the cave reads DARK) and lights the immediate area with a NEW **torch** — a cheap NO-SHADOW `PointLight` that follows the player (off elsewhere). Global dimming is safe (the player only SEES the cave once enclosed; `d`=0 on the surface). `Tuning.CAVE_AMBIENT_FLOOR/SUN_FLOOR/TORCH_INTENSITY/TORCH_DIST`. Exposed `ctx.deepCave` (+ `GameContext` type) so the rig can render it. **Decayed dressing + multi-chamber + the ⑩ site → next cycle.**
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** (deterministic; the per-frame dark-nav runs clean in the harness, doesn't touch placement) + colliders **0/40**.
- **Visual iteration:** appearance gate PASS — extended the `cave` rig scenario's `interior`/`inside` angles to render the REAL dark-nav (dark ambient/sun + the real cave torch). The `inside` shot reads as a dark cave with a warm torch pool on the floor + near walls fading to black; the `interior` shot reads as a torch-lit funnel floor with a dark doorway portal into the chamber. "Dark but navigable", no-horror, solitary (D252). The exact darkness balance + the in-MOTION torch reach → walk-test.
- **Decisions:** none new — executes D254.
- **Spend:** ~300K (the updateDeepCave logic + torch + tuning + main/ctx wiring + the rig dark-nav render + verify:all + the doc suite); campaign total ~13.49M; cycle **49/75**.
- **Commit:** `dff3abe`.
- **Next (cycle 50):** **M8 ⑨ continued** — decayed dressing (D252, sparse dark props) + (stretch) multi-chamber depth + the **M8 ⑩ companion-egg site** at the deepest chamber. **Verdict: CONTINUE** — not terminal (cycle 49/75); not a milestone; a `[partial]`-SHIPPED is progress (the dark-nav is built + gate-verified). `consecutive_no_progress` stays 0.

## Cycle 50 — M8 ⑨ continued [partial]: cave DECAYED DRESSING (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** dress the dark cave chamber as a long-dead place (D252) so it reads as somewhere, not a bare box.
- **Shipped (`[partial]` — the dressing):** sparse, dark, long-dead props in `world/deepCave.ts`'s chamber (18 decoration meshes, all `isWreckDecoration`, NO colliders, on the floor within the room footprint): 2 rubble piles tucked in the back corners, 4 scattered fallen rock chunks, a **dry skeleton** slumped against the back wall (a muted-bone group — a long-dead scavenger, NOT a fresh body, D252), and 2 collapsed ceiling slabs. NEW `_caveBoneMat` (muted dried bone). NO powered/lit/maintained objects.
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** (deterministic; the dressing is decoration, no colliders, no scatter impact) + colliders **0/40**. The cave mesh count rose 7→**25** (confirmed via the `cave` scenario) — the +18 dressing meshes.
- **Visual iteration:** appearance gate PASS — `--scenario=cave --angle=inside` reads as a dead, debris-strewn cave under the torch: dark rubble piles, a fallen slab, and rock chunks scattered on the floor in the warm torch pool, fading to black. Dead, decayed, solitary, no-horror (D252). The skeleton's visibility (against the dark back wall) + the overall feel → walk-test.
- **Decisions:** none new — executes D254.
- **Spend:** ~230K (the dressing props + the bone material + the inside render + verify:all + the doc suite); campaign total ~13.72M; cycle **50/75**.
- **Commit:** `8d0a093`.
- **Next (cycle 51):** **M8 ⑨ continued** — multi-chamber depth (extend `deepCave.ts` to 2-3 connected chambers + ≤37° ramps + doorways toward the spec's 4-8 chambers) reserving the DEEPEST chamber as the **M8 ⑩ companion-egg site**; this completes ⑨ and preps ⑩. **Verdict: CONTINUE** — not terminal (cycle 50/75); not a milestone; a `[partial]`-SHIPPED is progress (the dressing is built + gate-verified). `consecutive_no_progress` stays 0.

## Cycle 51 — M8 ⑨ COMPLETE: the ⑩-egg-site dais + a scope call (single-chamber cave; multi-chamber → backlog) (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** multi-chamber depth (toward the spec's 4-8) + the ⑩ companion-egg site.
- **Found (the constraint that drove the scope call, D255):** the funnel is a radial smoothstep bowl — flat only near the centre (r≲4), rising steeply outward (r=6 ≈ +2.5 m, r=9 ≈ +5 m). So **adjacent rooms on the bowl floor don't fit** (a chamber offset ~6 m → sub-2 m ceilings / walls buried in the rising bowl), and **descending rooms** require walking UNDER the terrain heightfield — whether the Rapier heightfield blocks the KCC capsule FROM BELOW is unresolved (D254's open risk) and can't be walk-tested headless. Building multi-level under-terrain rooms blind risks broken traversal.
- **Shipped:** (1) the **M8 ⑩ companion-egg SITE** — a low stone dais (+ rim) at the chamber's deep end (`deepCave.ts`; decoration, 2 meshes) marking where the egg will rest. (2) **The scope call (D255):** ⑨ ships as a COMPLETE single-chamber cave (funnel + roofed chamber + dark-nav + dressing + the ⑩ dais — verified + delivers the M8 deep-cave experience); the **4-8-chamber expansion is deferred to `backlog.md` §A**, gated on a WALK-TEST of the under-terrain-heightfield KCC handoff (if it works → an `addChamber` refactor + descending ramps; if not → an architecture revisit). The user decides at the Phase-B milestone review with real play feedback. **⑨ COMPLETE.**
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** + colliders **0/40** (the dais is decoration, no colliders; the cave mesh count rose 25→27).
- **Visual iteration:** appearance gate PASS — `--scenario=cave --angle=inside` shows the dais as a low circular platform (a marked site) on the torch-lit cave floor among the dressing. The cave remains dark/dead/navigable (D252).
- **Decisions:** **D255** — deep cave ships as ONE chamber; the 4-8-chamber expansion deferred to backlog (the bowl can't host a spread; descending rooms are walk-test-gated). friction 2 (an honest scope reduction surfaced rather than built-blind; revisited at the milestone review).
- **Spend:** ~230K (the ⑩-site dais + the bowl-geometry analysis + D255 + the backlog entry + verify:all + the doc suite); campaign total ~13.95M; cycle **51/75**.
- **Commit:** `898a796`.
- **M8 ⑨ COMPLETE** (funnel · chamber · dark-nav · dressing · ⑩-site; multi-chamber → backlog). **Next (cycle 52):** **M8 ⑩ companion-egg-cherry-pick** (re-apply the `2d4035b` spine at the dais; the additive `companionEggTaken?` save field — watch D81). **Verdict: CONTINUE** — not terminal (cycle 51/75); not a milestone (⑨ is a unit; M8 still has ⑩; the Phase-B pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 52 — M8 ⑩ companion-egg-cherry-pick → M8 COMPLETE (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** re-apply the `2d4035b` companion-acquisition spine at the cave egg-dais (the last M8 unit). Flagged as the campaign's most likely **D81 SAVE_VERSION-bump STOP**.
- **D81 recon (gating, FIRST):** `git show 2d4035b` + grepped current code — the spine was REVERTED (companion spawns at the player's side; `companionAcquired`/egg/`despawnCompanion` absent). Its save design was already D81-safe: an **additive optional `companionAcquired?: boolean`**, NO version bump. → the D81 STOP did NOT trigger; ⑩ shipped autonomously. **`SAVE_VERSION` stays 15.**
- **Shipped (the spine, re-applied across 8 files, retargeted from `rockyEntrance` to `deepCave`):** `CaveEgg` type + a glowing-ovoid egg on the cave dais (`deepCave.ts`); `ctx.egg` + `flags.companionAcquired` (`GameContext.ts`); `despawnCompanion` (`companion.ts`); the additive `companionAcquired?` save field + save/load [default TRUE on absent → legacy keeps the companion] (`save.ts`); the `'eggs'` registry + the `'hatch'` case [E → `spawnCompanionAt` at the egg, set acquired, remove egg] + the `'hatch'` `InteractType` + VERB (`interaction.ts`, `inventory/types.ts`, `interactPrompt.ts`); the boot reconcile in `handoffToGame` [boot always spawns the companion + builds the egg; acquired → remove the egg, else → despawn the boot companion] + dev paths set acquired (`main.ts`). A NEW game now acquires the companion by finding the cave + hatching the egg; legacy saves keep theirs.
- **Verify:** `verify:all` PASS — tsc + placement **0/0 ×5** + colliders **0/40** (the egg is an interaction target, no collider; no scatter/determinism impact).
- **Visual iteration:** appearance gate PASS — `--scenario=cave --angle=inside` (with the egg re-attached for the shot — the rig boots dev → the reconcile detaches it): a pale, softly-glowing ovoid on the dais, the focal point of the dark chamber under the torch. D252-safe (the one intentional artifact, reads as a found egg). The acquisition FEEL (find → hatch → the boot spawn-then-despawn timing) → walk-test (as `2d4035b` itself flagged).
- **Decisions:** **D256** — ⑩ re-applied the `2d4035b` spine retargeted to the deep cave with NO SAVE_VERSION bump (the additive `companionAcquired?` field; the biggest D81 risk navigated cleanly). friction 2.
- **Spend:** ~400K (the D81 recon + the 8-file spine re-apply + the egg + the boot reconcile + the rig egg-shot + verify:all + D256 + the doc suite); campaign total ~14.35M; cycle **52/75**.
- **Commit:** `690cfcd`.
- **M8 COMPLETE** (⑧ spike · ⑨ cave · ⑩ companion). **Next (cycle 53):** **M9 ⑪ rideable-sled-spike** (A/B worktree, kill-switch; **re-table if both fail** — no 3rd KCC try per D125). **Verdict: CONTINUE** — not terminal (cycle 52/75); **NOT a milestone pause** (M8 is not the last tier before the Phase-B marker — M9/M10 remain; the pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 53 — M9 ⑪ rideable-sled-spike: Option C decided (the speeder's proven ride generalizes), flag landed (2026-06-20) — SHIPPED ⚡ULTRACODE · M9 BEGINS
- **Planned:** the architectural-risk spike for "stand on a towed sled + ride it" — A/B candidate approaches, behind a kill-switch, re-table if both fail (D125).
- **Steering received (during this cycle) → applied:** user wrote *"lets pause before starting M10 so I can review and plan accordingly."* → recorded a **PAUSE-FOR-USER-REVIEW gate at the M9→M10 boundary** (`pause_before: "M10"` in campaign-state + a ⏸ marker in roadmap.md + archived to steering-archive.md). M9 continues unattended (⑫ ⑬); when M9 completes, the cycle that would start M10 ⑭ PAUSES (awaiting_approval) for the user's review. Resume via `/campaign-approve`.
- **Recon (decisive — no blind build):** read **D125** (the earlier attempt tabled — 3 delta-based architectures all failed; the KCC slope-projection/autostep/contact fights a moving kinematic platform; the player slid off in 5-10 frames). D125 named "**full Option C parenting** — skip the KCC while riding" as the recommended next attempt. Found that **Option C is ALREADY proven in-engine**: the SPEEDER ride is exactly it — `speeder.mounted` gates `updatePlayer` + `updateSpeeder` teleports the capsule to the rider seat each frame (`setNextKinematicTranslation`), bypassing the KCC moving-platform issue. The sled is a moving body like the speeder → the pattern generalizes directly.
- **Shipped (the spike DECISION + scaffold, D257):** adopted **Option C for the sled** (NOT re-tabled — a viable path exists): a `ridingSled` state gating `updatePlayer` like `mounted` + a per-frame capsule-to-sled-rider-seat teleport in `updateSleds` (which already runs BEFORE `updatePlayer`). Landed **`FEATURES.rideableSled`** INERT (default OFF, gate-and-wait pattern — like `realRope`/`realCloth`). The D125 delta-based approaches stay DEAD. Implementation plan documented for the ⑪-build; riding FEEL (the exact D125 failure mode) is walk-test-gated, so the build wires it behind the flag + the user flips + walk-tests before adoption.
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (the flag is an inert const; nothing reads it yet → shipped behavior unchanged).
- **Visual iteration:** N/A — a design/architecture spike (the deliverable is the decision + the flag scaffold; the ride is feel-gated, no visual output this cycle).
- **Decisions:** **D257** — generalize the speeder's proven seat-teleport ride to the sled (Option C, behind `FEATURES.rideableSled`); the D125 delta approaches stay dead. friction 2.
- **Spend:** ~200K (the D125 recon + the speeder-ride recon + the flag + D257 + the doc suite + verify:all); campaign total ~14.55M; cycle **53/75**.
- **Commit:** `de5c8b5`.
- **Next (cycle 54):** **M9 ⑫ real-rope-physics** (a Verlet/segmented rope sim behind `FEATURES.realRope`, default OFF; CCD-from-the-start per D124). **Verdict: CONTINUE** — not terminal (cycle 53/75); not a milestone (⑪ is a unit; M9 has ⑫/⑬; the Phase-B pause is after M10). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 54 — M9 ⑫ real-rope-physics [partial]: the Verlet rope SOLVER + the dynamic-sag visual (2026-06-20) — PARTIAL ⚡ULTRACODE
- **Planned:** replace the inextensible position-snap rope (D126) with a Verlet sim behind `FEATURES.realRope` (default OFF), CCD per D124, for the 4 rope callers.
- **Shipped (`[partial]` — visual-first, D258):** NEW `world/verletRope.ts` — a Verlet rope SOLVER (point-masses + distance-constraint relaxation + gravity/damping; pure). Wired into the sled rope's VISUAL: when `realRope` is ON, the 3 sag mid-points of the existing 7-point CatmullRom tube become **Verlet-simulated** (dynamic hang/swing/taut) instead of the static parabolic droop. `Tuning.VERLET_ROPE_GRAVITY/ITERS/DAMPING`; per-sled `ropeVerlet` state (lazy). Default OFF → the proven static-sag rope runs unchanged. The **body-coupling** (rope drives the towed body) + **CCD** (D124) + the **other 3 callers** (companion/stake/kill-drag) → the ⑫-continuation.
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (the flag is OFF; the realRope branch is the first reader of the M2 inert flag — it compiles + leaves the OFF path byte-proven). **Solver math probe** (standalone): a slack rope (2 m ends, 5 m rope) sags to midpoint Y 2.76 (≪ chord 5); a taut rope (5 m ends) stays at Y 4.81 (≈ straight) → hangs when slack, goes taut when stretched. ✓
- **Visual iteration:** the dynamic-sag LOOK is behind the default-OFF flag (I didn't flip it — gate-and-wait + the rope feel is walk-test-gated); the solver is validated by the math probe. The in-game sag/swing LOOK + the tow FEEL → walk-test (flag flip).
- **Decisions:** **D258** — ⑫ lands visual-first (the Verlet solver + the dynamic sag behind `FEATURES.realRope`); body-coupling + CCD + the other callers are the continuation. friction 2.
- **Spend:** ~300K (the recon [D124/D126 + the rope code] + the Verlet solver + the sled wiring + the math probe + verify:all + D258 + the doc suite); campaign total ~14.85M; cycle **54/75**.
- **Commit:** `3cdcc4e`.
- **Next (cycle 55):** **M9 ⑫ continued** — the rope body-coupling (the Verlet rope drives the towed body, replacing the inextensible snap) + CCD (D124) + the other 3 caller visuals; OR (if the body-coupling proves walk-test-gated/risky like the cave) defer it to backlog + move to ⑬ real-cloth-physics (which reuses the now-landed solver). **Verdict: CONTINUE** — not terminal (cycle 54/75); not a milestone (the Phase-B pause is after M10; note the steering `pause_before: "M10"` fires once M9 completes). `consecutive_no_progress` stays 0.

## Cycle 55 — M9 ⑫ real-rope-physics scope call: ship the Verlet FOUNDATION, defer body-coupling to a walk-test (2026-06-20) — SHIPPED (⑫ COMPLETE) ⚡ULTRACODE
- **Planned:** the ⑫ continuation — body-coupling (the Verlet rope drives the towed body) + CCD (D124) + the other-caller visuals.
- **Decision (D259):** **ship ⑫ as the Verlet real-rope FOUNDATION** (C54's solver + sled-rope dynamic-sag visual behind `FEATURES.realRope`) and **defer the remainder to backlog**, gated on the user's rope-VISUAL walk-test at the imminent M10 pause. **Rationale:** (1) the body-coupling is D125-ADJACENT (a towed body chasing a moving rope-end is the exact tar-pit class D125 documents) AND feel-dominant (can't be headless-validated) — the sled tow already works crisply via the inextensible snap; replacing it with Verlet tension BLIND, before the user has confirmed they even want the Verlet rope, is premature + risks worse feel + repeats the D125 build-blind mistake; (2) the other-caller visuals (kill-drag's per-entity tubes need a `Map<id,RopeVerlet>` + `dt` threaded into `updateKillDrag`; companion/stake have no sagging tube) are low-value (default-OFF, rare ropes) before the direction is validated. **Correct gate-and-wait sequencing:** the user evaluates the rope VISUAL at the M10 pause (C53 steering); if wanted, the body-coupling is the follow-up (with the D125 re-table escape hatch). Mirrors the cave (D255) + sled-ride (D257) scope calls.
- **Shipped:** D259 + the backlog items (⑫ body-coupling + CCD + other-caller visuals → §A, walk-test-gated; also surfaced the ⑪ rideable-sled BUILD to §A). **No code change** — `src/` is byte-identical to C54 (the realRope branch + the solver are already in). ⑫ now reads COMPLETE on the roadmap (the Verlet foundation is landed behind the flag for the user's M10 evaluation; the body-physics is explicitly deferred).
- **Verify:** `tsc --noEmit` PASS (no src change). placement/colliders unaffected (docs-only cycle — `src/` untouched since C54's green `verify:all`; the C11/C23/C46 docs-only precedent).
- **Visual iteration:** N/A — a scope/decision cycle, no new visual output (the C54 rope visual stands; its LOOK + the deferred body-coupling FEEL → the M10 walk-test).
- **Decisions:** **D259** — ⑫ ships as the Verlet foundation; body-coupling + CCD + other-caller visuals deferred to a walk-test-gated backlog. friction 2.
- **Spend:** ~120K (the boot + the rope-caller recon [killDrag/companion/stake rope sites] + D259 + the backlog items + the doc suite + tsc); campaign total ~14.97M; cycle **55/75**.
- **Commit:** `61081c0`.
- **Next (cycle 56):** **M9 ⑬ real-cloth-physics** — a 2D Verlet grid behind `FEATURES.realCloth` (default OFF), tent-door/flag only; **reuses ⑫'s now-landed `verletRope.ts` solver primitives**. ⑬ is the LAST M9 unit → **after ⑬, the steering `pause_before: "M10"` FIRES** (the loop pauses for the user's M10 review). **Verdict: CONTINUE** — not terminal (cycle 55/75); ⑬ is M9 (the pause is after M9 completes, i.e. after ⑬). `consecutive_no_progress` stays 0 (SHIPPED — a scope decision that advances the roadmap).

## Cycle 56 — M9 ⑬ real-cloth-physics → **M9 COMPLETE → PAUSE (steering pause_before:M10)** (2026-06-20) — SHIPPED + PAUSED ⚡ULTRACODE
- **Planned:** ⑬ real-cloth — a 2D Verlet grid behind `FEATURES.realCloth` (default OFF), tent-door/flag only, reusing ⑫'s solver. The last M9 unit.
- **Shipped:** NEW **`world/verletCloth.ts`** — a 2D Verlet CLOTH solver (the 2D extension of ⑫'s `verletRope.ts`: the same integrate + distance-constraint relax loop, over a `cols×rows` grid w/ structural links to each point's right + down neighbour, the TOP ROW pinned, gravity + a gentle local-Z billow). Wired into the **large-tent door-flap** (`largeTent.ts`): when `realCloth` is ON, `updateLargeTents` drives the existing subdivided `doorPanel` PlaneGeometry's vertices from the grid each frame the flap is visible (placed BEFORE the `doorAnim===target` early-out; in LOCAL space so the parent transform + the `scale.y` roll still work). `Tuning.CLOTH_GRAVITY/ITERS/DAMPING/WIND/WIND_FREQ`; per-tent lazy `clothState`. Default OFF → the static flat panel runs unchanged. D260.
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (flag OFF → the door geometry is untouched). **Standalone cloth solver math probe:** the top row stays pinned (no drift), the flap hangs at rest length (a fully-lintel-pinned flat panel doesn't vertically droop — correct; the dynamic is the billow), a steady wind displaces it +1.0 m in local Z, no NaN. ✓
- **Visual iteration:** the cloth LOOK + sway FEEL are behind the default-OFF flag (not flipped — gate-and-wait + cloth feel is walk-test-gated); the solver is validated by the math probe. The in-game billow LOOK + feel → walk-test at the M10 review.
- **Decisions:** **D260** — ⑬ real-cloth = a 2D Verlet grid reusing ⑫'s solver, driving the large-tent door-flap's billow behind `FEATURES.realCloth`. friction 1.
- **M9 COMPLETE** — ⑪ (C53) + ⑫ (C54+C55) + ⑬ (C56). All M9 units shipped behind their flags (`rideableSled`/`realRope`/`realCloth`, all OFF, walk-test-gated).
- **Spend:** ~260K (the boot + the cloth-target recon [tent/largeTent/door-flap] + `verletCloth.ts` + the largeTent wiring + the math probe + verify:all + D260 + the doc suite); campaign total ~15.23M; cycle **56/75**.
- **Commit:** `bc987ed`.
- **⏸ PAUSED — steering `pause_before: "M10"` (C53).** M9 is complete; the next work is M10. Per the user's directive ("pause before starting M10 so I can review and plan accordingly"), the loop STOPS here: `status=paused`, `awaiting_approval=true`, `stop_reasons=["steering-pause-before-M10"]`. **Verdict: STOP (pause).** The loop does NOT schedule another cycle. **Human action:** review M9 (walk-test the 3 flag-gated systems — flip `rideableSled`/`realRope`/`realCloth` in `npm run dev`; see [backlog.md](../backlog.md) §A for the owed walk-tests) + plan M10, then resume into M10 via `/campaign-approve` (which clears `pause_before`).

## C56→C57 boundary — APPROVED + RESUMED into M10 (user steering 2026-06-20)
- **Steering received** (at the M9 pause, after a server walk-test): "ok lets continue with the campaign loop cycle. just want to hold off on doing the drop pod start sequence as that is a big feature that i want to tackle later in more detail. there's lots of feedback to give on phase A and B but want to wait till we run through the remaining before we tackle."
- **Actions:** (1) cleared the gate — `status=active`, `awaiting_approval=false`, `stop_reasons=[]`, `pause_before=null`; (2) **deferred ⑯ drop-pod-intro** → backlog §A (HELD feature, design in detail later via its own `/feature-slice`); M10 = ⑭+⑮+⑰ only; (3) Phase-A/B feedback deferred to the post-M10 Phase-B milestone pause (noted on that milestone). Archived in `steering-archive.md`. Committed separately as `ea5920e`.

## Cycle 57 — M10 ⑭ scrap-machete-pry-tool (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** ⑭ — a craftable scrap machete that pries salvage panels (per backlog §116-118 + the roadmap "new `scrap_machete` id").
- **Shipped (D261):** NEW **`scrap_machete`** item — a craftable PRY TOOL (`wieldLmb: 'click_use'`, the same salvageables hover-path as `scrap_bar`) with a distinct CRUDE-bolo viewmodel (wide blunt belly + chipped edge + bolted tang + cloth grip + dark oxidized-scrap material — reads as improvised, distinct from the honed `machete` weapon + the `scrap_bar` crowbar). **Recipe id 19** (scrap×2 + cloth×1 — a free input multiset, no collision-chooser). **Pry-gate** (interaction.ts ×2) now accepts **either** `scrap_bar` OR `scrap_machete` (additive — doesn't strand existing scrap_bar holders; prompt → "need a pry tool"). Added to the ItemId union + `ALL_ITEM_IDS` (the dev spawner auto-includes it via `ALL_REGISTERED_ITEM_IDS`).
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (adding an item doesn't touch placement/colliders). **No save bump** (a new item id is additive — old saves just lack it; D81-safe).
- **Visual iteration:** rendered scrap_machete vs `machete` vs `scrap_bar` via `item-studio` (front + 3q); **2 rounds** — r1 read a touch clean → darkened the blade to oxidized scrap + enlarged the edge-chips → r2 reads clearly as a crude, damaged, improvised bolo, distinct from both neighbours. In-hand swing/pry FEEL → walk-test.
- **Decisions:** **D261** — scrap_machete is a new craftable pry tool that ADDS to the gate (not a destructive scrap_bar swap); the found `machete` stays the melee blade; the dual chop+pry unification + scrap_bar retirement + the bespoke pry animation are deferred (user/feel calls). friction 1.
- **Spend:** ~280K (the recon [item union + machete/scrap_bar defs + pry-gate + recipe system] + the item def/model + recipe + gate + 2 render rounds + verify:all + D261 + the doc suite); campaign total ~15.51M; cycle **57/75**.
- **Commit:** `6baf105`.
- **Next (cycle 58):** **M10 ⑮ craftable-hover-bike** — the repairable-speeder (one vehicle, two states: broken → repaired hover-bike; dep ⑭). **Verdict: CONTINUE** — not terminal (cycle 57/75); not a milestone (the Phase-B pause is after M10 completes, i.e. after ⑰). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 58 — M10 ⑮ craftable-hover-bike (repairable-speeder) (2026-06-20) — SHIPPED ⚡ULTRACODE
- **Planned:** ⑮ — the "hover-bike = repairable-speeder" design call (ONE vehicle, two states: broken → repaired).
- **Shipped (D262):** the existing speeder is now REPAIRABLE behind **`FEATURES.repairableSpeeder`** (default OFF) — NOT a new vehicle. `SpeederState.broken`; `updateSpeeder` early-outs to a new **`updateBrokenSpeeder`** when broken (no hover/ride/mount — a dead bike can't be mounted). **Repair:** E nearby with `SPEEDER_REPAIR_SCRAP` (4) scrap → `removeItems` → `broken=false` → hover resumes. NEW `'repair'` interactType + verb; the seat shows "[E] repair" (or passive "need N scrap") when broken. **Broken rest = gravity-settle** (a broken bike gets `gravityScale 1` → falls + rests FLUSH on terrain; repair flips it back to 0 so the PD hover takes over) + a static visual lean (`SPEEDER_BROKEN_PITCH/ROLL`) + lights off. **Additive save** (`speeder.broken?`, optional inside the already-optional `speeder?` object) — **NO SAVE_VERSION bump** (pre-⑮ saves load with broken=false, the proven rideable speeder; D81 honored).
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (flag OFF → the speeder spawns working, current behavior). Forced-flag-on preview confirmed (NUMERICALLY): broken spawns + falls + rests flush (0.49 m above ground = collider resting, vy 0 settled), lights off, leaning ~10°/7°.
- **Visual iteration:** the broken pose is a transform on the existing polished speeder model (no new geometry). Verified numerically (the heavy-scene preview screenshot hit the documented timeout — `shared-memory`/`dustfall_preview_gotchas`); the first attempt floated ~0.86 m (read as "hovering-but-tilted") → switched to gravity-settle → rests flush. The in-game broken LOOK + repair FEEL → walk-test.
- **Decisions:** **D262** — ⑮ = the existing speeder made repairable (broken→working) behind the flag; reuse-not-fork; gravity-settle for the rest; additive save. friction 1.
- **Spend:** ~340K (the recon [speeder spawn/ride/state + save + the mount/hover internals] + the broken state/spawn/update + the repair interaction + the gravity-settle fix + the additive save + a forced-on preview verify + D262 + the doc suite); campaign total ~15.85M; cycle **58/75**.
- **Commit:** `33cd3d2`.
- **Next (cycle 59):** **M10 ⑰ pickup-instancedmesh** — the LAST M10 unit (perf: InstancedMesh for pickups, human-attended). **After ⑰ → the Phase-B milestone pause** (the user's full Phase-A/B feedback + walk-test). **Verdict: CONTINUE** — not terminal (cycle 58/75); not yet the milestone (⑰ remains before the Phase-B pause). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 59 — M10 ⑰ pickup-instancing: MEASURE + plan, build deferred to the human review → M10 done → ⏸ PAUSE (Phase-B milestone) (2026-06-20) — SHIPPED + PAUSED ⚡ULTRACODE
- **Planned:** ⑰ — convert world pickups to `InstancedMesh` (perf; "human-attended" per the roadmap). Last M10 unit.
- **Measure-first (the deliverable):** live preview eval on a new-game world — **382 world-scatter pickups (239 scrap + 137 branch + 6 relic_core), each a separate draw call** (already sharing pooled geometry/materials, ACAH, but still 1 call each). **Of 505 total draw calls, ~382 (~75%) are pickups.** Instancing each type → ~3 calls → ~126 total (a ~75% cut). High-value — the measure-first check PASSED (it's a real cost, not a non-problem).
- **Decision (D263):** ship ⑰ as the **measure + a concrete instancing plan**, and **DEFER the build to the human-attended Phase-B review** (→ backlog §A). Why: ⑰ is roadmap-labeled "human-attended perf"; the human review is the IMMEDIATE next step; and instancing is a CORE item-collection-loop rewrite (the take raycast reads `userData.pickupId` off the hit mesh → instancing needs an `instanceId`→pickup map + swap-remove + the bob-matrix path) — a silent bug breaks item collection, so it shouldn't be blind-shipped autonomously at depth right before the human can validate it. Consistent with D255/D257/D259 (ship the foundation/measure, defer the human/feel/coupled part).
- **No code change** — `src/` byte-identical to C58 (the measure was a preview eval; the build is the deferred work). `tsc` clean; placement/colliders hold (docs-only; C11/C23/C46 precedent).
- **Visual iteration:** N/A — a measure/decision cycle.
- **Decisions:** **D263** — ⑰ measure-first (pickups = 75% of draw calls) + plan; the core-loop instancing rewrite deferred to the human-attended review. friction 2.
- **M10 reached the Phase-B boundary:** ⑭ ✓ (C57) + ⑮ ✓ (C58) built; ⑯ drop-pod-intro user-deferred (C56→C57 steering); ⑰ measured + planned + human-deferred (this cycle). The autonomous Phase-B build (M6→M10) is complete to the milestone.
- **Spend:** ~140K (the boot + the pickup-system recon [pickups.ts + the take raycast] + the live preview MEASURE [382 pickups = 75% of draw calls] + D263 + the backlog plan + the doc suite + tsc); campaign total ~15.99M; cycle **59/75**.
- **Commit:** `ad8f97a`.
- **⏸ PAUSED — Phase-B milestone review** (`checkpoint: "milestone"`). M10 is at the Phase-B boundary ("Phase B — Build-out complete"). `status=paused`, `awaiting_approval=true`, `stop_reasons=["milestone-review"]`. **Verdict: STOP (pause).** The loop does NOT schedule another cycle. **Human action (the BIG review):** give the held Phase-A/B feedback; walk-test everything (flip the M9/M10 flags — `realRope`/`realCloth`/`rideableSled`/`repairableSpeeder` — + play the whole game; see [backlog.md](../backlog.md) §A); design the deferred ⑯ drop-pod-intro; decide ⑰ (build the instancing human-attended — measured 75% win, planned in D263). Then `/campaign-approve` to resume (or steer).

## Phase-B milestone review (2026-06-20) — APPROVED + resumed into the M11→M13 review-fix pass
- **User gave Phase-A/B feedback** (the big review) → `/triage-ideas` → 13 items into backlog.md (the "Fresh triage 2026-06-20" block). **Execution call (user):** campaign runs the bounded fixes (M11 wreck/panel · M12 sand-worm · M13 audio) AUTONOMOUSLY with the user BATCH-VALIDATING after each tier (the audio + close-up feel can't be self-verified); the Skyfall hero wreck + the cave rework are DEDICATED solo sessions (NOT the loop). Cleared the milestone gate (status=active); promoted the fixes to roadmap with a milestone marker after EACH tier (pause-per-batch). Commit `cc43ade`.

## Cycle 60 — M11 investigation: root-caused the wreck-panel bugs (2026-06-20) — INVESTIGATION (no code; informs the M11 build)
- **Reproduced on the live preview:** all **83 world salvageables register + tag correctly** (`untagged_not_openable: 0`) → the "not openable" panels are NOT a registration bug. They're a **geometry-reachability** issue: panels placed where the interaction look-ray hits the hull first (embedded / wrong-facing) — the SAME root as the "floating" ones (bad panel mounts). So ⓐ+ⓑ are one fix: seat the mounts flush + reachable (no new world-rand — phash only, D226).
- **Render-ability confirmed:** the isolated `procgen-wreck` rig DOES render (the screenshot timeouts only hit the heavy full-world scene) — wreck looks ARE iterable via `--archetype=<x> --seed=<n> --angles=side --zoom=`. The `wrecked_tank` reads structurally-OK from framed exterior angles; the user's interior-ribbing/panel-float complaints are close-up reads (likely a flat panel lifting at the corners on the curved tank top).
- **Outcome:** no code shipped; the finding + the per-batch-pause cadence set up. **Verdict: CONTINUE** — cycle 61 does the M11 build (seat the panel mounts flush/reachable + the wrecked_tank ribbing/structure/panel). `consecutive_no_progress` stays 0 (investigation advanced the unit). Spend ~200K (recon + live-preview reproduction + 2 rig renders + the cadence setup); campaign total ~16.19M; cycle **60/75**.

## Cycle 61 — M11 ⓐ: wreck panels "not openable" FIXED (culled panels now hide) (2026-06-20) — SHIPPED [partial] ⚡ULTRACODE
- **Planned:** M11 build — start with the not-openable + floating procgen-wreck panels.
- **Root cause (D264):** the interaction raycast is `intersectObjects(targets, true)` over ONLY the registered panels → the hull does NOT occlude, so "not openable" is NOT a reachability issue. The bug: `pruneBuriedPanels`'s `cull()` (the shared prune for buried/occluded panels) removed the registry entry + deleted the panel's interact tags **but left the mesh VISIBLE** → a visible panel that won't open.
- **Shipped:** `cull()` now also HIDES the culled panel subtree (`s.panel.visible=false` + its `panelDoor`/`panelInterior`). One fix covers EVERY procgen + wreck-yard cull path (all route through this shared `pruneBuriedPanels`). D264.
- **Verify:** `verify:all` PASS (tsc + placement + colliders — the fix is render-only; the bury-audit checks surviving panels' positions, unaffected). **Live-preview confirmed: 21 culled panels now hidden, 79 openable intact, 0 visible-orphan procgen panels** (was the bug). No save touch.
- **Known remainder → backlog §A:** ~3 far-out (wreck-yard/hero range) `addAccessPanel` meshes stay visible-but-unregistered — a DIFFERENT path (hand/hero wrecks register only "above-ground" panels, ACAS A4; the rest are un-culled + unregistered, not reached by `pruneBuriedPanels`). Targeted follow-up: register-or-hide them.
- **Decisions:** **D264** — not-openable = culled-but-visible panels; fix = hide on cull. friction 1.
- **Spend:** ~300K (the raycast/prune/registration trace + the live-preview root-cause audit [panel structure: wrapper+body, `panelShape` marker] + the cull-hide fix + verify:all + D264 + the doc suite); campaign total ~16.49M; cycle **61/75**.
- **Commit:** `959df3f`.
- **Next (cycle 62):** **M11 continued** — ⓑ floating panels (seat the mounts flush — esp. flat panels lifting on curved hulls) + ⓒⓓⓔ wrecked_tank (ribbing/structure/panel), render-verified via the isolated rig. Then the M11 batch pause for the user's wreck walk-test. **Verdict: CONTINUE** — M11 not done (ⓑⓒⓓⓔ remain before the batch milestone). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 62 — M11 ⓑ+ⓔ: floating-panel seating (tank + derelict crest panels sunk) → ⏸ PAUSE for M11 batch-1 validation (2026-06-20) — SHIPPED [partial] + PAUSED ⚡ULTRACODE
- **Planned:** M11 cont — ⓑ floating panels + ⓒⓓⓔ wrecked_tank, render-iterated.
- **Shipped (ⓑ+ⓔ):** the FLOATING-PANEL root for the crest-mounted panels = a flat panel centered on a CURVED hull top, whose edges overhang the curving-away hull (a daylight gap from 3/4 angles → "floating/unconnected"). Fix: SINK the crest panel mounts below the peak so the edges embed into the curve — `wreckedTank` (`r*2 → r*2-0.28`) + `derelict` (`r → r-0.22`), both `poiComponents.ts`. Fixed-value offsets → NO new world-rand (phash determinism intact). Rig-confirmed the tank panel seats in side profile (the 3/4 "float" was largely the adjacent manway+rungs).
- **Verify:** `verify:all` PASS (tsc + placement + colliders — the mount sinks are deterministic, no rand). [render-verified the tank + derelict via the isolated `procgen-wreck` rig.]
- **Could NOT confidently fix blind (→ user batch-validation):** ⓒ tank interior ribbing "floating" + ⓓ tank "disconnected/floating" structure — these are CLOSE-UP/interior reads I couldn't cleanly reproduce from the rig's framed exterior angles. Rather than churn blind, deferring to the user's walk-test to pin down precisely.
- **Decisions:** (extends D264 — same panel system). No new D-entry.
- **Spend:** ~280K (the tank panel root-cause [flat-on-curve overhang] + 3 rig render rounds + the tank+derelict sinks + verify:all + docs); campaign total ~16.77M; cycle **62/75**.
- **Commit:** `c62e71e`.
- **⏸ PAUSED — M11 batch-1 validation** (`checkpoint: milestone`; the user chose autonomous + batch-validate). M11's CONFIDENT panel fixes are in (ⓐ not-openable C61 + ⓑⓔ floating-panel seating C62). `status=paused`, `awaiting_approval=true`, `stop_reasons=["milestone-review"]`. **Verdict: STOP (pause).** **Human action:** walk-test the wrecks — (1) do panels OPEN now (no dead non-openable teases)? (2) do the salvage panels sit FLUSH (no floating)? (3) the wrecked_tank — describe what reads as floating/disconnected (the interior ribbing? which pieces?) so the ⓒⓓ structure fix lands right (I couldn't reproduce it from renders). Then `/campaign-approve` (with notes) → M11-cont (tank ribbing/structure + the 3 hand/hero-wreck stragglers) → M12 worm → M13 audio.

## Cycle 63 — M11 ⓒⓓ: wrecked_tank ribbing/structure/flap rework (user batch-1 notes) (2026-06-20) — SHIPPED [partial] ⚡ULTRACODE
- **From the user's batch-1 walk-test** (screenshot, into the torn end): ribbing floating as concentric hoops · torn-flap "spikes" floating off to the side · shell reads knife-thin at the cut.
- **Shipped (poiComponents.ts `wreckedTank`):** (ⓒ) ribs moved `0.9r → 0.85r` to HUG the inner liner (0.88r) + 3 longitudinal STRINGERS tie the rings into a cage (was 3 floating hoops); (thin-hull) a thick cut-RIM annulus (0.85r→r) caps the outer↔liner wall gap at the torn lip → reads as a thick wall cross-section, not a knife-edge; (ⓓ flaps) torn flaps re-anchored — BASE on the rim, peeling back over the lip, + SHORTENED to a jagged torn lip (`fh r*0.4-1.0 → r*0.16-0.38`) so they read as torn metal teeth, not floating spikes (also dropped the bbox height 7.4→5.5). Determinism-safe (phash only, fixed offsets — no new world-rand). NEW rig `tear` angle (camera into the +X torn end) added for future verification (its head-on-into-dark-bore screenshot flakes; the wide side/3q render confirms the fix).
- **Verify:** `verify:all` PASS (tsc + placement 0/0 ×5 + colliders 0/40). Rig side/3q renders confirm: short jagged torn lip (not floating spikes), connected dome/hoops/body. **Caveat:** the rig buries the tank deeper (buryFrac 0.57) than the user's in-game instance, so the into-the-tunnel ribbing angle couldn't be reproduced headless → **the user re-validates the ribbing close-up.**
- **Decisions:** extends D264 (panel/wreck system) — no new D-entry.
- **Spend:** ~400K (the user-feedback diagnosis + the rib/stringer/rim/flap rework + ~6 rig render rounds [fighting the burial/framing + the tear-angle flake] + verify:all); campaign total ~17.17M; cycle **63/75**.
- **Commit:** `PENDING`.
- **⏸ STILL PAUSED — M11 batch-1** (awaiting the user's close-up re-validation of the tank ribbing). Reload `localhost:5180` → walk to a tank → confirm the ribbing reads connected (not floating). If good → `/campaign-approve` → M11 stragglers → M12. If still off → iterate.

## Cycle 63 (cont) — M11 ⓒⓓⓔ applied to the HUSK too (huskShell) (2026-06-20) — SHIPPED [partial]
- **User clarified** the floating wreck is the HALF-CYLINDER **husk** (huskShell — `hollow_husk`/`crash_husk`/`enterable_wreck`), NOT the tank. Same 3 issues, separate component.
- **Shipped (poiComponents.ts `huskShell`):** ribs — arc clamped to ≤ the shell's covered arc (1.25π→1.05π so tips don't poke past the shell edge) + 3 longitudinal stringers tie the rings into a cage (was floating hoops); torn-rim flaps SHORTENED (0.5-0.9 → 0.2-0.36) + peeled flatter (were long side spikes); breach flaps shortened; salvage panel reseated FLUSH on the +Z flank at the widest point (θ≈π/2, normal=+Z; was floating at a fixed y=1.4 on the curve). phash-only, no new rand.
- **Verify:** `verify:all` PASS. Rig 3q render confirms the ribs now read as CONNECTED ARCHES (a cage) + short flaps. The flank panel + the end-on view couldn't be rig-rendered (the spawned rig server degraded after many runs + the into-the-dark-end angle flakes) → user confirms the panel close-up.
- **Commit:** `383b43d`. Still ⏸ paused at M11 batch-1 for the user's close-up re-validation (tank + husk).

## Cycle 63 (cont 2) — M11 husk ROOT-CAUSE fix: ribs touch the shell + spikes removed (2026-06-20) — SHIPPED [partial]
- **Root cause (the "ribs not connected"):** `makeFormerRings` applies a hidden 0.84× shrink (`radius*(0.84−i·taper)`), so passing ~r put the ribs at ~0.81r — floating ~19% inside the shell. Both wrecks. FIX: pass `r*1.14` → the largest ring lands ~0.96r, touching the shell wall.
- Also: REMOVED the torn-rim + breach CONE FLAPS (persistent floating spikes across rounds — the open-top edge + flank gash carry the torn read) · removed the floating stringer I'd added (the rib-radius fix makes the ribs read connected without it) · embedded the flank panel deeper (`r-0.15 → r*0.82`).
- **Verify:** `verify:all` PASS. Rig 3q render confirms the ribs now read as CONNECTED ARCHES spanning the trough + NO spikes. Panel + multi-angle → user confirm (rig flank/end angles flake).
- **Commit:** `c81f807`.

## M11 batch-1 — wreck fixes VALIDATED by the user (2026-06-21) — session handoff
- User walk-tested the tank + husk wreck fixes (ribs connected, spikes gone, floating plank/seam fixed, panels relocated off the ribs): **"this looks ok for now."** The root cause across both wrecks was `makeFormerRings`' hidden `0.84×` radius shrink (ribs floated ~19% inside the shell). All committed (C61-C63 + follow-ups), `verify:all` green, no save touch.
- User is starting a FRESH session to continue. Campaign left ⏸ paused at M11 batch-1 (`/campaign-approve` to continue). **Next:** the 3 hand/hero-wreck straggler panels (§A) → M12 sand-worm (ridges/jump/audio) → M13 weapon+vehicle audio. Resume brief written to `docs/next-session-prompt.md`.

## M11 batch-1 — APPROVED ✅ (milestone-review gate released · 2026-06-22)
- Human `/campaign-approve` at the M11 batch-1 milestone-review (the user had already walk-tested + signed off the wreck fixes 2026-06-21 — "looks ok for now"). Nothing to fold (a milestone review carries no proposal). **Gate cleared:** `status: active`, `awaiting_approval: false`, `stop_reasons: []`, `consecutive_no_progress: 0`. No deferred post-mortem drafts pending (consolidate skipped).
- **Framework upgrade applied from here** (recorded in `resume_note`): the visual gate now renders the PLAYER'S REAL in-game view (not the isolated rig); hero/defining geometry is delegated to the **procedural-modeler** agent iterating to a quality BAR (5-8 rounds); the ANTI-PUNT rule (never ship a scaffold + defer the hard part) is enforced — applies especially to M12's worm work.
- **Resumed** → the loop continues the M11→M13 review-fix pass: M11 stragglers → M12 sand-worm → M13 audio, pausing at the **Phase-B review fixes complete** milestone after M13. 63/75 cycles (~12 headroom); if it runs out, the loop STOPS at the cap and tells the user. Launch with `/loop /campaign-cycle`.

## Cycle 64 — M11 stragglers: hide the 3 mega-wreck companion panels → M11 COMPLETE (2026-06-22) — SHIPPED ⚡ULTRACODE
- **Planned:** the M11 remainder — the "3 hand/hero-wreck unregistered-but-visible panels" (D264's noted remainder), register-or-hide them.
- **Root-caused EMPIRICALLY (didn't trust the backlog's guess).** The backlog §A entry blamed "545-626 m, ACAS A4 above-ground filtering" on crashedHull/megaShip/megaWreck/heroLandmarks. An Explore agent + grep of ALL `placeWreck` callers + a **live-preview scene enumeration** (the C61 method: traverse for `userData.accessPanel`, classify registered / culled-hidden / visible-unregistered) found the truth: exactly **3 stragglers**, all at **172-245 m**, chain `Group < Scene`, positions matching the mega-wreck companion formation (`bestX±dx`, offsets {35,-40}/{-45,20}/{25,35} → coords matched to the meter). They are the mega-wreck flagship's **3 decorative "scale-reference" companion wrecks** (`fuselage`/`engine_cluster`/`cargo_container`, [poi.ts](../poi.ts)) — placed via `placeWreck` with the **return discarded, never `registerSalvageable`** (every OTHER placeWreck caller registers: convoy/caravan/heroLandmark/procgenPoi/wreckYard). An unregistered-but-visible `placeWreck` panel = the "won't-open" dead tease; these are NOT routed through `pruneBuriedPanels` (the procgen path).
- **Shipped (D265):** new **`hideAccessPanel(group)`** helper in `wrecks.ts` (mirrors D264's cull-hide idiom — body + `panelDoor` + `panelInterior` → `visible=false`), called on each companion in the poi.ts loop. **Hide, not register** — registering 3 panels would consume `rand` (the AAT condition draw) → desync the scatter stream + re-baseline the world (the D208 hazard / phash-only discipline); the companions are explicitly decorative; hiding matches D264 + the M11 "no dead tease" theme. **Tradeoff (user veto at the M-review):** the companions are now SEALED rather than openable salvage.
- **Verify (headless):** `verify:all` PASS — tsc + placement **0/0 ×5** (counts 64/59/66/76/78 unchanged → zero `rand` drift confirmed) + colliders **0/40**. Render-only, no save touch.
- **Visual iteration — the framework-upgrade REAL-VIEW gate** (not an isolated rig): live-preview enumeration of the PLAYER'S REAL generated world showed **3 → 0** visible-unregistered stragglers, confirmed across **3 independent seeds** (the world seed is random per boot; the mega-wreck spawns every seed → the fix is STRUCTURAL, not seed-luck). 67 registered + 21 culled-hidden panels intact on the original seed. (This is the correct gate for a registry/visibility fix — reading the actual scene-graph the renderer uses, the C61-accepted method — rather than a pixel render of a distant prop.)
- **Decisions:** **D265** — M11 stragglers = mega-wreck companion wrecks placed-but-not-registered; fix = hide. friction 1. Also archived **D207–D220** → `decisions-archive.md` (active decisions 59 → 45: D221–D265; the >50 threshold).
- **M11 COMPLETE** (ⓐ C61 · ⓑⓔ C62 · ⓒⓓ C63 user-validated · stragglers C64). Spend ~250K; campaign total ~17.42M; cycle **64/75**.
- **Commit:** `838c7e1`.
- **Next (cycle 65):** **M12 sand-worm** — ⓕ remove dorsal ridges · ⓖ attack = charge-straight then DIVE from current pos (no airborne jump) · ⓗ alert audio → a quiet low rumble + screen-shake buildup. **Verdict: CONTINUE** — M11's milestone marker was ALREADY consumed (paused C62, approved 2026-06-22 this session); finishing the stragglers crosses no NEW un-cleared marker, and the fix is fully self-verifiable (no feel/audio). The next genuine pause is the **M12 milestone** (worm FEEL + alert LISTEN can't self-verify). `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 65 — M12 ⓕ: remove the sand-worm dorsal ridges (2026-06-22) — SHIPPED ⚡ULTRACODE
- **Planned:** start M12 sand-worm. First the full worm-system recon (serves all 3 units), then ⓕ remove the dorsal ridges.
- **Recon (an Explore agent mapped the whole worm system — reused for ⓖ + ⓗ):** MODEL `src/enemies/sandWorm.ts` — the C13 dorsal-armor SCUTES (13 keeled spine plates, ~lines 313-337) vs the circumferential ridge rings (lines 292-311, body segmentation) vs the body segments. ATTACK FSM — `tickLunge` (~1195-1232, the high arc: `basePos.y = baseY + sin(t·π)*SANDWORM_BREACH_ARC_PEAK` [=20m]), `enterLunge` (~1159-1193), `applyBodyBend` (~1560-1594, the pose w/ tail-sink + charge-dip). AUDIO `src/audio/audio.ts` — `playWormRoar` (~657, loud one-shot on alert), `startWormRumble`/`setWormRumbleLevel` (~717, sustained sub-bass on charge), `applyTremorEffects` (sandWorm.ts ~860, camera shake). RIG — the `worm-model` rig-shot scenario (poses the REAL ctx.sandWorms.list[0], angles head/side/3q/arc/charge).
- **Shipped (ⓕ):** removed the **C13 dorsal-armor SCUTE loop** (`sandWorm.ts` — the 13 keeled spine plates that read as a stegosaurus sawback). The user wants the smoother **Dune-leviathan silhouette**. **Kept** the segmented body (tapered segments + the circumferential ridge rings — those read as body *segmentation*, not a dorsal crest; Dune worms are segmented). Removal consumes no `rand` (scutes were hand-modeled) → determinism-safe.
- **Verify (headless):** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (worm = creature, not a placement POI; tsc is the relevant gate).
- **Visual iteration:** appearance-verified via the `worm-model` rig (poses the REAL worm mesh, regenerated with the new code — for a CREATURE the surfaced silhouette is placement-independent, so the rig is representative; the rig anti-pattern is wreck-burial-specific). side + 3q renders: a coherent smooth segmented worm, scutes GONE, no holes/floating remnants (**mesh count 66→53**). Routine removal → light pass, no sev≥2. The worm-in-MOTION FEEL → the user's M12 walk-test.
- **Cycle scope:** ⓕ only this cycle. The feel-critical ⓖ (charge-dive — the defining M12 unit) gets cycle 66 with proper dive-pose iteration; ⓗ (audio) cycle 67. ~10 cycles headroom → no need to cram the feel-critical work.
- **Decisions:** none (a straightforward execution of "remove dorsal ridges"; the keep-segmentation-rings interpretation noted for the walk-test). Post-mortem SKIPPED (pure polish cycle — no new D-entries / tier move / system addition).
- **Spend:** ~130K (the worm recon + the scute removal + verify:all + 3 rig renders + the doc suite); campaign total ~17.55M; cycle **65/75**.
- **Commit:** `9ac3ea0`.
- **Next (cycle 66):** **M12 ⓖ attack = charge-straight then DIVE from current position (no airborne jump)** — FEEL-CRITICAL; replace the `tickLunge` high arc with a head-first plunge, render the dive pose via the real worm view. **Verdict: CONTINUE** — M12 not complete (ⓖ + ⓗ remain before the M12 milestone pause); M12's marker is un-consumed. `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 66 — M12 ⓖ: sand-worm attack = charge → breach-and-DIVE (no high jump) (2026-06-22) — SHIPPED ⚡ULTRACODE
- **Planned:** the feel-critical M12 unit — remove the silly high-jump attack → charge-straight then dive from the current position.
- **Shipped (D266):** **rewrote `tickLunge`** (`sandWorm.ts`). The old lunge launched the body CENTER on a `sin(t·π) * SANDWORM_BREACH_ARC_PEAK` (+20 m) parabolic arc — sailing the whole worm over the player (the "jump"). NOW: the body center HOLDS at the charge depth (back-ridge exposed) through the **strike** (the HEAD rears up out of the sand via pitch+bend to bite), then **DIVES head-first** down to underground (pitch swings to head-down, center plunges, accelerating). The body never goes airborne.
  - **Pose math extracted to `applyLungePose(worm, t)`** (exported) + exported `applyMeshTransform` + a `__game.poseLunge(t)` debug hook + `worm-model --angle=strike|dive` rig angles → the rig renders the EXACT pose the game runs (no rig-vs-real drift — the C63 false-pass class).
  - **New tuning:** `SANDWORM_LUNGE_STRIKE_PITCH` (0.30) · `DIVE_PITCH` (0.6) · `STRIKE_BEND` (1.2) · `DIVE_BEND` (1.6) · `FORWARD_OVERSHOOT` (6); replaced `SANDWORM_BREACH_ARC_PEAK`.
- **Verify (headless):** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (creature behavior; tsc is the gate).
- **Visual iteration (real pose render, drift-free — 3 rounds):** rendered the REAL pose via `poseLunge`. **Numeric proof of no-hop:** strike (t=0.5) centerY=1.77 < groundY=5.97 (body below ground), head reared +13.5 m (a low breach to bite); dive (t=0.82) centerY=−4.15 (plunging), head driven head-first below ground. Rounds: fixed the rig camera framing + forced `yaw=0` for a clean side profile + moderated `DIVE_PITCH` 0.85→0.6 (the rigid pose see-saws the tail up on a steep dive — kept it a natural sounding flick, not a tail-launch). Renders read as a low surfacing strike → a head-first dive. **Attack FEEL (timing/menace/exact pitch) → the user's M12 walk-test** (feel can't be self-verified — the campaign per-tier pause). NOT a punt: the defining structure (no hop · breach-strike · head-first dive) is done + verified; the residual is feel-tuning, the human's gate.
- **Known limit (D266):** the worm pose system is rigid (yaw+pitch) + a symmetric `applyBodyBend` — it can't do a true "head dives under while the tail stays put" without a see-saw; a path-following dive is the deeper fix if the user wants it.
- **Decisions:** **D266** — breach-and-dive over a high arc; rig renders the real pose; the see-saw limit. friction 2.
- **Spend:** ~280K (the design + the tickLunge rewrite + applyLungePose extraction + the poseLunge hook + the rig angles + 3 render rounds + D266 + verify:all + the doc suite); campaign total ~17.83M; cycle **66/75**.
- **Commit:** `d5586f9`.
- **Next (cycle 67):** **M12 ⓗ alert audio → a quiet low rumble + screen-shake buildup** (the LAST M12 unit). **Verdict: CONTINUE** — M12 not complete (ⓗ remains before the M12 milestone marker). After ⓗ ships, the cycle PAUSES at the M12 milestone for the user's worm-attack FEEL walk-test + alert-rumble LISTEN. `consecutive_no_progress` stays 0 (SHIPPED).

## Cycle 67 — M12 ⓗ: alert audio = quiet rumble + screen-shake buildup → M12 COMPLETE → ⏸ PAUSE (2026-06-22) — SHIPPED + PAUSED ⚡ULTRACODE
- **Planned:** the last M12 unit — the alert audio → a low quiet rumble + a screen-shake buildup (mysterious, "you don't know what it is"), not a loud roar.
- **Shipped (D267):** **removed the loud `playWormRoar()` from `enterAlert`** (`sandWorm.ts`) — the roar now only fires on the lunge STRIKE (the payoff) + death + the stationary breach. **The sustained C16 rumble graph (reused UNCHANGED) now starts on ALERT** (`applyWormRumble` includes `'alert'`, was charging-only): a QUIET sub-bass ramping `0 → SANDWORM_ALERT_RUMBLE_MAX (0.28)` over the alert windup, then louder by proximity through the charge. **`applyTremorEffects` gained a buildup factor** ramping the camera-shake from ~0 over the alert duration (full on charge/retreat); `SANDWORM_TREMOR_SHAKE` constant (was hardcoded 0.10). The dread now CREEPS IN rather than announcing with a roar.
- **Verify (headless):** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (creature audio/camera logic; tsc is the gate).
- **Visual iteration:** N/A — AUDIO + a camera-shake feel (no visual mesh). The audio NODE graph is unchanged (the proven C16 rumble) → no new C16-lifecycle risk; the driving logic was self-audited (silent-emerging on alert, fades on leaving alert/charging, no node leak). **SOUND quality + the buildup FEEL → the user's M12 LISTEN** (audio can't be self-verified — the campaign per-tier pause). Not a punt: the wiring is complete + verified; the sound quality is inherently the human gate.
- **Decisions:** **D267** — the worm alert is a quiet mysterious dread buildup (rumble + growing shake), not a roar; reused the C16 graph (changed WHEN/level, not the nodes). friction 1.
- **✅ M12 COMPLETE** (ⓕ ridges C65 · ⓖ breach-dive C66 · ⓗ alert audio C67).
- **Spend:** ~180K (the audio/tremor driving rework + new tuning + D267 + verify:all + the doc suite); campaign total ~18.01M; cycle **67/75**.
- **Commit:** `7d693ed`.
- **⏸ PAUSED — M12 milestone review** (`checkpoint: milestone`; M12 is the last tier before the `### Milestone: M12 sand-worm` marker → all M12 units Shipped → milestone reached). `status=paused`, `awaiting_approval=true`, `stop_reasons=["milestone-review"]`. **Verdict: STOP (pause).** The loop does NOT schedule another cycle. **Human action:** walk-test the worm-attack FEEL (the C66 charge → breach-and-dive — no high jump) + LISTEN to the alert rumble/shake buildup (C67 — quiet, mysterious, building). Then `/campaign-approve` → **M13** (the FINAL review-fix tier: ⓘ gunshot+reload SFX all guns · ⓙ lower smoother speeder hum). `consecutive_no_progress` stays 0 (SHIPPED).

## M12 review fix (C68) — smoother worm dive: natural bend, tail curls under (2026-06-22) — SHIPPED (within the M12 pause)
- **User feedback during the M12 review:** "the dive should be a bit smoother — the worm should bend in a natural way, not have its tail pointing straight out at an angle; ideally the tail tip is never seen, it bends down toward the terrain so you never know how long the worm is."
- **Fix:** reworked the dive (`sandWorm.ts`) from a rigid head-down PITCH (which see-sawed the tail straight up — the D266 limitation) into a **natural BEND CURVE**. The dive pitch decays the strike rear to 0 (no see-saw); a new `worm.lungeDive` (0..1) drives `applyBodyBend` per-child: the HEAD leads DOWN (`SANDWORM_LUNGE_DIVE_HEAD_DROP` 2.2×R) + the TAIL CURLS DOWN into the terrain (`SANDWORM_LUNGE_DIVE_TAIL_DROP` 1.7×R), the mid-body crest near the surface. Removed `SANDWORM_LUNGE_DIVE_PITCH`/`_BEND`; added the `SandWorm.lungeDive` field. D266 updated (see-saw RESOLVED).
- **Verify:** `verify:all` PASS. Rendered via the worm-model `dive` angle (now `--t=<0..1>` to scan): t=0.6 = a coherent curved body diving in (head down, body arcing); t=0.7 = mostly submerged, tail curling under; **no straight-out tail, tip never visible**. Strike pose unchanged (head rears to bite). Feel → the user's walk-test.
- **No cycle increment** (a within-pause fix, like the M11 batch-1 "C63 cont" fixes). Campaign stays ⏸ paused at the M12 milestone for the user's re-validation. **Commit:** `b7b6a52`.

## M12 milestone — APPROVED ✅ (gate released · 2026-06-22)
- Human `/campaign-approve` at the M12 milestone-review. The user walk-tested + approved the full M12 sand-worm tier: the breach-dive attack (C66) + the C68 dive smoothing (natural bend, tail curls under, tip never seen) + the quiet alert rumble/screen-shake buildup (C67). Nothing to fold (a milestone review carries no proposal). **Gate cleared:** `status: active`, `awaiting_approval: false`, `stop_reasons: []`, `consecutive_no_progress: 0`.
- **Consolidation:** 1 post-mortem draft pending (`hide-mesh-when-unregistering`, C64) — left for a `/consolidate-shared-memory` pass whenever the user wants (not blocking the resume).
- **Resumed** → the loop continues into **M13 — the FINAL review-fix tier** (ⓘ gunshot+reload SFX all guns · ⓙ lower smoother speeder hum), pausing at the M13 milestone (audio LISTEN), which ENDS the M11→M13 review-fix pass. 67/75 cycles (~8 headroom). Launch with `/loop /campaign-cycle`.

## Cycle 68 — M13 ⓘ: gunshot + reload SFX for all guns (2026-06-22) — SHIPPED ⚡ULTRACODE
- **Planned:** M13 ⓘ — add gunshot + reload SFX for all guns (the first of the 2 M13 units). *(The numbered cycle 68 — distinct from the earlier `b7b6a52` within-pause "C68" M12-dive review fix, which didn't increment the count.)*
- **Recon (Explore agent):** mapped the 4 weapon specs (scrap_gun/amban_rifle/pulse_rifle/energy_pistol) + the combat fire path (`updateCombat`→`preFire`→`fireRanged`) + the reload path (`updateReload`) + the audio module (`getAudioInternals`, the `sfx` bus, `noiseBuffer`, the one-shot synth pattern + the C16 lifecycle for sustained voices).
- **Root gap found:** `combat.ts preFire()` (the shared melee+ranged pre-fire setup) played the MELEE whoosh (`playSwing`) for EVERY weapon → guns had no muzzle report.
- **Shipped (D268):** `preFire` now dispatches by `spec.kind` — melee → `playSwing`, gun → `playWeaponShot(id)`. **4 new one-shot synths** (`audio.ts`, on the `sfx` bus): scrap_gun = a sharp ballistic crack + report boom; amban_rifle = heavier/deeper boom + a sub thump (marksman weight); pulse_rifle = a rapid SHORT zappy "pew" (~0.1 s so the 0.13 s auto-cadence doesn't muddy); energy_pistol = a meatier charged-release zap + a sub thump. **Reload now weapon-aware** (`playReloadGun(heavy)` — the amban chunkier + a mag-seat thunk). Only scrap_gun + amban_rifle reload; pulse_rifle self-recharges + energy_pistol is charged → no reload (correct).
- **Verify:** `verify:all` PASS — tsc + placement 0/0 ×5 + colliders 0/40 (audio only; tsc is the gate).
- **Visual iteration:** N/A — AUDIO only. The Web Audio graph is all one-shots (start/stop, GC after — the existing one-shot convention; no sustained-voice C16-lifecycle risk), self-audited; pause-safe (combat gates on `isPlaying`; `preFire` only runs on a real shot → no dry-click gunshot). **SOUND quality → the user's M13 LISTEN.** Not a punt: the wiring is complete + verified; the sound quality is inherently the human gate.
- **Decisions:** **D268** — per-weapon gunshot SFX dispatched from preFire (was a melee whoosh for all); reload only for the guns that reload. friction 1. Post-mortem: no new candidate (one-shot SFX synthesis is covered by `web-audio-synthesis.md`).
- **Spend:** ~250K (the recon + 4 synths + the preFire dispatch + the weapon-aware reload + D268 + verify:all + the doc suite); campaign total ~18.41M; cycle **68/75**.
- **Commit:** `PENDING`.
- **Next (cycle 69):** **M13 ⓙ speeder engine → a lower-pitched, smoother hum** — the LAST review-fix unit. **Verdict: CONTINUE** — M13 not complete (ⓙ remains before the M13 milestone marker). After ⓙ ships, the cycle PAUSES at the M13 milestone (audio LISTEN), ENDING the M11→M13 review-fix pass. `consecutive_no_progress` stays 0 (SHIPPED).
