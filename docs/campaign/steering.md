# Steering inbox — Infinite Sands campaign

Write notes here; the loop picks them up at the next cycle boundary (before deciding the next unit).
- To **redirect**: describe the change (e.g. "smaller chunks", "skip S4, go to S6").
- To **pause**: write `pause` on its own line, or delete `.gamedev-framework/overnight.lock`.
- At the **S5 save pause**: review the schema plan in campaign-log.md, then `/campaign-approve`.

(empty)
