# Steering inbox — Dustfall campaign

Write redirections here **anytime**. The running `/loop /campaign-cycle` reads this at the
next cycle boundary, applies your notes (re-prioritize, add/drop a unit, change emphasis),
and archives them under "Applied" below. You do **not** need to stop the loop to steer it.

Special keywords:
- Write **`pause`** on its own line → the campaign pauses cleanly at the next boundary.
- Write a unit id or milestone name + a note → that work is re-prioritized / re-scoped.

---

## Pending (unread by the loop)
_(empty — drop steering notes above this line)_

---

## Applied (archived by the loop)

See [steering-archive.md](steering-archive.md). Most recent: **C10 (2026-06-28)** — pod is too boxy → redesign the exterior CYLINDRICAL (+ research refs, + update the interior to match). Triggered a research pass (`docs/research/escape-pod-cylindrical.md`) + a direction pick before the rebuild. Prior: C53 — pause before M10 for a user review. C44 — remove the watchtower + ruin the well (D252).
