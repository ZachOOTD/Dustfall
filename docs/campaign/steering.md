# Steering inbox — Sharpen & Deepen (M7-R refinement → M8-M12 world-deepening)

Write notes here; the loop reads them at the top of each cycle.
- To redirect: describe the change. To pause: write `pause` on its own line.

## Standing directives (2026-07-13, user)
- **MODELS NEED REAL THICKNESS** (locked_constraint `models-need-thickness`). No paper-thin
  zero-thickness double-sided shells — hulls/walls/canopies/greebles get visible wall depth;
  torn edges show a cross-section. Verify from grazing/edge angles. When M7-R ships the hull
  thickness work, FORMALIZE this as a CLAUDE.md architecture rule (extends rule 7).
- **100% accurate collision** — every visible mass gets a matching collider (rule 9), swept,
  not just the big pieces. (M7-R found the dorsal containers had none.)
- Queue order is priority order: M7-R (Skyfall fixes) → M8 vultures → M9 archetypes →
  M10 vignettes → M11 tube-wreck retirement → M12 new biome. ~4M budget for this overnight
  (ceiling 8.75M); it works the queue in order + stops cleanly when the cap is hit.
- Run overnight (checkpoint none); Zach reviews the batch in the morning.
