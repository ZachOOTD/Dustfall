# Steering inbox — Dustfall "Sharpen & Deepen"

This is the redirect inbox for the running campaign. Write a note here anytime;
the loop picks it up at the next cycle boundary (before deciding the next unit).

- To **redirect**: describe the change (e.g. "skip M6, go straight to M7", "M4 also add a distant-wind-through-wreck creak").
- To **pause**: write `pause` on its own line (or run `/campaign-status --stop`, or delete `.gamedev-framework/overnight.lock`).
- To **approve a sanctioned pause** (Skyfall pre-detail / a save-version bump): use `/campaign-approve`.

(empty)
