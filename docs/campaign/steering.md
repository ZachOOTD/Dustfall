# Steering inbox — The Deep Desert

Write notes below this header; the loop reads them at the TOP of each cycle (so a note lands at
the next cycle boundary, not mid-cycle). To halt immediately, stop the `/loop` directly.
- To redirect: describe the change. To pause: write `pause` on its own line.

## Standing directives (still in force from prior runs)
- **MODELS NEED REAL THICKNESS** (rule 7); collision matches models, swept (rule 9).
- **Never `git stash` here; run code-writing agents ONE AT A TIME** (read-only research exempt).
- **Push is HELD** — commit to `campaign/2026-07-18-deep-desert`, never merge to master.
- **Model split:** Fable for thinking/planning (main loop), Opus for execution subagents.
- **Trust the playtest over a green gate**; builders declare `userData.intendedOpening`.
- Sled ride feel + dune vista scale are HUMAN checkpoints — never self-certify them.
