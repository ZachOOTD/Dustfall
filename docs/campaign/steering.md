# Steering inbox — Dustfall campaign

Write redirections here **anytime**. The running `/loop /campaign-cycle` reads this at the
next cycle boundary, applies your notes (re-prioritize, add/drop a unit, change emphasis),
and archives them under "Applied" below. You do **not** need to stop the loop to steer it.

Special keywords:
- Write **`pause`** on its own line → the campaign pauses cleanly at the next boundary.
- Write a unit id or milestone name + a note → that work is re-prioritized / re-scoped.

---

## Pending (unread by the loop)
_(empty — drop steering notes above this line)_

---

## Applied (archived by the loop)

See [steering-archive.md](steering-archive.md). Most recent: **2026-07-01 (post-walk-test) — POD + WORLD CONSISTENCY (major re-scope).** Two issues: (1) the crash→exit SWAPS models + teleports — `tickStepOut` disposes the interior crashed cabin + drops a SEPARATE exterior wreck; the user wants ONE pod: crash + break the door = already in the real game world, the SAME pod (interior+exterior, ONE enterable model) persists there, you can walk back in. (2) the intro plays at DAWN (too dark) + the light/time CHANGES on step-out — the user wants a consistent bright **MIDDAY + clear skies**, identical from crashing-down through step-out (no time/world jump). May need the exterior pod redesigned to be enterable (reuse the ship-model work). → supersedes the polish loop; became the active work. Prior: **R5a (2026-06-30)** — the redesigned cockpit HULL METAL reads "too pristine and shiny"; the user preferred the OLDER rugged/matte metal texture. → re-scope the cockpit hull material to a worn/matte/rugged idiom (roughen + drop the metalness-shine/envMap, keep the neutral non-brown color that killed the barrel read, layer real grime/edge-wear). Aligns with the gate's own "too clean for the Mad-Max/Dune tone" finding. Prior: **C10 (2026-06-28)** — pod is too boxy → redesign the exterior CYLINDRICAL. Prior: C53 — pause before M10. C44 — remove the watchtower + ruin the well (D252).
