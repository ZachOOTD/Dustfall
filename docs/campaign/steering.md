# Steering inbox — Sharpen & Deepen campaign (M7 Skyfall resume)

Write notes here; the loop picks them up at the next cycle boundary (before deciding the next unit).
- To **redirect**: describe the change.
- To **pause**: write `pause` on its own line, or delete `.gamedev-framework/overnight.lock`.
- At the **post-blockout walk-test pause** (after S1+S2): walk the greybox interior, then `/campaign-approve`.
