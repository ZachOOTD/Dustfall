# Steering inbox — Dustfall campaign

Write redirections here **anytime**. The running `/loop /campaign-cycle` reads this at the
next cycle boundary, applies your notes (re-prioritize, add/drop a unit, change emphasis),
and archives them under "Applied" below. You do **not** need to stop the loop to steer it.

Special keywords:
- Write **`pause`** on its own line → the campaign pauses cleanly at the next boundary.
- Write a unit id or milestone name + a note → that work is re-prioritized / re-scoped.

---

## Pending (unread by the loop)
we need to keep the same escape pod model consistent through the whole intro all the way through to the actual game. the problem i'm seeing is after we've crashed and get out of the pod, the world immediately changes and there's a different exterior pod model. I want when you crash and then break open the door you are already in the actual game world, the same pod that we rode down in exists there, you can go back in it and walk around if you wanted. also don't like how the lighting/time changes when you exit the pod,this should stay consistent and now it looks like the dawn is too dark, lets do closer to midday and make the world time/clear skies/lighting looks the same when you are crashing down to the desert as wehn you release the door and step out into the world so everything is consistent. you may need to redesign the external pod to do this but looks like we've already done some work there for the ship model so lets make sure everything works and makes sense. does that make sense to you? anything i need to clarify?
_(empty — drop steering notes above this line)_

---

## Applied (archived by the loop)

See [steering-archive.md](steering-archive.md). Most recent: **R5a (2026-06-30)** — the redesigned cockpit HULL METAL reads "too pristine and shiny"; the user preferred the OLDER rugged/matte metal texture. → re-scope the cockpit hull material to a worn/matte/rugged idiom (roughen + drop the metalness-shine/envMap, keep the neutral non-brown color that killed the barrel read, layer real grime/edge-wear). Aligns with the gate's own "too clean for the Mad-Max/Dune tone" finding. Prior: **C10 (2026-06-28)** — pod is too boxy → redesign the exterior CYLINDRICAL. Prior: C53 — pause before M10. C44 — remove the watchtower + ruin the well (D252).
