# Steering archive — Dustfall campaign

Processed steering directives, dated, with the action taken. Moved here by the
loop at the cycle boundary so they're not re-applied each cycle.

---

## 2026-06-29 (Phase 2 milestone WALK-TEST feedback, via the Netlify preview link) — descent-feel fixes + forward reqs

The user walk-tested the descent (Phase 2) via the deployed preview and gave 5 notes:
1. **Screen shake too much + too early.** Wants it PEACEFUL while still far/high (orbit); shake ONLY when going through the atmosphere (re-entry); and gentler. → **FIXED (post-C17):** removed the constant descent base-rumble (`addTrauma(0.04+re*0.45)` → `addTrauma(re*0.18)` — re-entry-only + gentle), softened the eject kick (0.95→0.55) + the tumble buffet (`0.05+settle*0.30` → `settle*0.12`).
2. **Exterior pod model should match the INTERIOR's size** for consistency. → captured as a roadmap **Phase 4** req (the exterior `placeCrashedPodWreck` must match the cabin CAB_R/WALL_H — same vessel inside vs out).
3. **Player teleports outside the pod on landing — wants to START in the pod (in the desert) + RELEASE the door to walk out** (the vision's "blow the pod door"). → captured as a roadmap **Phase 4 T4.1** req (rework the greybox `stepOut`: wake INSIDE the pod, the BLOW-DOOR is the player-triggered step-out).
4. **Weird purple lines across the landscape** (the dunes) when nearing the surface. → **FIXED (post-C17):** the dune lee-shadow trough colour was a saturated blue-violet (`vec3(0.28,0.26,0.44)`) reading as purple lines; desaturated to a cool dusty shadow (`vec3(0.33,0.27,0.31)`).
5. **Interior pod detail/improvement pass** — acknowledged as LATER ("decent first model for now"). → backlog.

**Action:** #1 + #4 fixed immediately (descent-feel, this pass) + pushed so the preview link refreshes for a re-test; #2 + #3 captured as Phase 4 reqs in the roadmap; #5 to backlog. Campaign stays paused at the Phase 2 milestone (the user re-tests the shake/dunes, then `/campaign-approve` → Phase 3).

**ROUND 2 (same day, after re-test):** the user: "the shake is still way too much. i also want to try WITHOUT the pod tumbling — keep the pod UPRIGHT and have it more slowly descend through the phases, more SEAMLESS: watch the planet get closer, fall through the atmosphere, see space transition to sky, the ground slowly approaching." → **REVERSED T2.3's tumble (a design change, like the box→cylindrical pod):** removed the camera tumble entirely (the pod stays upright + level facing the window; `shipExplode` is now just a brief blast flash + a fading cabin glow, NO scripted tumble, mode='seated'); the controller's `applyIntroTumble` is left as an unused guarded no-op (in case a SUBTLE version is wanted later). **Slowed the descent** `DESCENT_DURATION` 8→18s + `SHIP_EXPLODE_DWELL` 4→2.2s for a slow, seamless fall. **Shake cut to a whisper** (`re*0.18`→`re*0.05`, eject kick `0.55`→`0.18`). The descentProgress vista already does space→sky→ground (orbital planet+stars → cross-fade → dawn sky + dune ground); the slower pace lets it read seamlessly. tsc + smoke `{ok:true,beats:10}` clean. Pushed for re-test. (The user is experimenting — "i want to TRY without the tumbling" — so this is a tried direction, not necessarily final; T2.3's tumble code is preserved as a no-op.)

**ROUND 3 (same day):** the user re-tested + said the shake was STILL too much + "turns my camera around and way to the side all over the place, disorienting" (root cause: `addTrauma` applies ROTATIONAL pitch/yaw/roll and STACKS per-frame → the per-frame calls in tickDescent + tickParachute SATURATED the trauma → the view spun; removed the per-frame shake from both, kept only the one-time per-yank jolts; softened the eject nudge to a one-shot 0.1). ALSO: "don't like the dune view at the end — make it actually look like how the real terrain looks, and have the player looking off onto the horizon through the viewport." → a procedural-modeler reworked `LOWALT_FS` (3 rounds) to MATCH the real game desert (rendered the `crashed-pod` rig as the target): warm tan/ochre sand + the real dawn-sky gradient (warm tan horizon `0xe2b582` → dusty blue-grey zenith `0x6c8aa8`) + a clean HORIZON mid-window the player gazes OUT toward — replacing the stylized barchan dune-ridge field (no more sharp ridges / purple troughs / blown-white crests). Verified own-eyes vs the real-desert target (matches) + d0 orbital intact + d05 cross-fade clean. tsc + smoke clean. Pushed for re-test.

**ROUND 4 (same day):** the user: "the camera changes between phases + freezes a bit — more seamless. it's hard to see the ground in the last phase — i want to physically see the ground get closer till impact then screen goes black. and we're starting too far from the planet — start in a close orbit above the atmosphere, then through the atmosphere, then falling to the desert + impact." → **Camera/pacing (main loop, sequence.ts):** removed the parachute beat's lever-snap (`faceControl(−π/2)`) — it stays facing the WINDOW (−Z) so the player watches the ground to impact (no jump, the gag's prompt cues the pull); shortened `SHIP_EXPLODE_DWELL` 2.2→1.2s (the fall starts sooner, less static "freeze"). **Visual arc (procedural-modeler, podScene.ts, 3 rounds):** CLOSE-ORBIT start — the planet now large/near at descentProgress 0 (BASE_S 2.55 + BASE_PY sink → the curved world + atmosphere limb fill the porthole, stars beyond) instead of a distant dot; GROUND-RUSH at p→1 — a new `uRush` uniform (ramps `((p−0.82)/0.18)²` over the final leg) drops the horizon up off the window, pulls the near sand close, grows + scrolls the dunes, drops the haze + boosts the dune relief, so the surface RUSHES UP close at impact (fixed `uNear` saturating at ~0.9 — it couldn't tell 0.9 from 1.0 = the "stays a distant horizon" bug); the calm mid-approach horizon look is preserved. Verified: d0 close orbit (planet large), d07 calm horizon, d1 ground rushing up close; the parachute faces the window (−Z) confirmed in-engine. tsc + smoke clean. (Felt MOTION continuity = walk-test; the ground-rush is a shader-projected fake, not real terrain geometry — fine at the fast-fall timing.) Pushed for re-test.

**ROUND 5 (2026-06-30):** the user: "the desert looks upside down + maybe moving the wrong direction; want it more realistic with a clear horizon; want it to look like the pod is FALLING over time; maybe black out briefly between each phase to feel smoother." → **Desert (procedural-modeler, LOWALT_FS, 4 rounds):** root cause of the "upside-down/wrong" read = the horizon had collapsed into haze (no sky/ground anchor) + the ground-scroll was a no-parallax UNIFORM translation (reads as sliding sideways, not falling). FIX: a real PERSPECTIVE ground projection driven by a falling altitude (`D = H/(below+eps)`, `H` shrinks across the descent → near rows map to bigger/nearer sand = true parallax = falling), a CRISP anti-aliased horizon at every altitude (high in the approach, sinking as you fall), capped aerial haze (only a thin band at the horizon, not erasing the ground). Now reads realistic (matches the crashed-pod desert), right-side-up, clear horizon, the ground swelling up to meet you. **Blackouts (main loop, sequence.ts):** a brief DIP-TO-BLACK (`_phaseFade`, 0.35s) at the descent + parachute transitions (advanceBeat cuts to black; the new beat fades in) so the camera/vista change happens under the black. Held off on a falling-CAMERA motion (the user dislikes camera movement → the falling reads via the vista). Verified d07 clear horizon + d1 ground close + right-side-up. tsc + smoke clean. (Scroll DIRECTION over real-time motion = walk-test; one sign flip if reversed.) Pushed for re-test.

---

## 2026-06-29 (received during cycle 17, the Phase 2 milestone) — A pushed campaign branch + a playable preview link to walk-test each cycle

**Directive:**
> "want to set up a separate branch on github that we can commit to after each cycle that i can create a playable link for and go test it as we continue developing."

**Action taken:** surfaced at the Phase 2 milestone pause (the campaign was already pausing for the walk-test). The campaign already commits each cycle to the `campaign/escape-pod-intro` branch locally; the new ask is to PUSH it to GitHub + deploy a playable preview link (so the in-progress escape-pod intro can be walk-tested via a URL, with `FEATURES.escapePodIntro` ON in that preview build — separate from the live master deploy). Investigated the existing deploy mechanism + proposed a concrete plan to the user at the pause; pushing/deploy is outward-facing so it's confirmed with the user before executing (NOT done unilaterally). Once set up, each cycle's push refreshes the preview link. See the C17 milestone-pause handoff + next-session-prompt.

---

## 2026-06-20 (applied at cycle 44) — Remove the watchtower; POIs must read as long-decayed, no signs of living human life

**Directive:**
> "don't like the watchtower, lets remove the watchtower. the watchtower is too much of a sign of infrastructure, everything in the world should be a wreck/decayed for a hundred years, a watchower still standing reads as recent and that people could be close by which is not what i want, i want the player to feel alone and have almost no signs of living human life, this goes for pois as well."

**Action taken (cycle 44, the "solitude pass"):**
- Removed the `watchtower` archetype entirely (component + assembler + `ARCH_WEIGHTS` + collider-audit list).
- Re-scoped the `well` into a long-dry collapsed ruin (no bucket/rope/working crank) — kept but de-peopled (user-confirmed scope: option 2 + the well treated per option 3).
- Codified the principle as a standing constraint: [decisions.md](../decisions.md) D252 + [vision-deltas.md](../vision-deltas.md).
- Older wreck POIs (satellite, wrecked_tank, debris_field, husks, derelict, debris_trail) already read as decayed — left untouched per the confirmed scope.

## 2026-06-20 (received during cycle 53; applied) — Pause before starting M10 for a user review

**Directive:**
> "lets pause before starting M10 so I can review and plan accordingly"

**Action taken (recorded C53):** added a **PAUSE-FOR-USER-REVIEW gate at the M9→M10 boundary**. M9 continues unattended (⑫ real-rope-physics · ⑬ real-cloth-physics — they're M9, not M10); when M9 completes, the cycle that WOULD start M10 ⑭ instead PAUSES (`status: paused`, `awaiting_approval: true`, `stop_reasons: ["steering-pause-before-M10"]`) so the user reviews + plans M10 (the final tier) before it begins. Recorded in `campaign-state.json` (`pause_before: "M10"`) + a ⏸ marker in `roadmap.md` before the M10 line + the ⑫/⑬ next-session-prompts carry the reminder. (The campaign's other pause — the Phase-B milestone after M10 — is unchanged; this adds an earlier user-requested gate.)

## 2026-06-20 (received at the M9→M10 pause; applied) — Resume into M10, but defer the drop-pod intro + hold Phase-A/B feedback

**Directive (verbatim):**
> "ok lets continue with the campaign loop cycle. just want to hold off on doing the drop pod start sequence as that is a big feature that i want to tackle later in more detail. there's lots of feedback to give on phase A and B but want to wait till we run through the remaining before we tackle"

**Context:** the C53 `pause_before:M10` gate fired cleanly at C56 (M9 complete). The user started the dev server + walk-tested, then resumed.

**Actions taken (recorded at the C56→C57 boundary):**
1. **Approved + cleared the gate** — `status: active`, `awaiting_approval: false`, `stop_reasons: []`, `pause_before: null`. The loop resumes into M10.
2. **Deferred ⑯ drop-pod-intro-cutscene** — removed from the M10 autonomous tier (M10 = ⑭ machete + ⑮ hover-bike + ⑰ pickup-instancing only); moved to `backlog.md` §A as a HELD feature to be designed in detail later via its own `/feature-slice`. Marked DEFERRED on the roadmap M10 line.
3. **Phase-A/B feedback deferred to the post-M10 gate** — the user is holding all Phase-A/B feedback until M10 ships. Noted on the "Phase B — Build-out complete" milestone in `roadmap.md` so that pause is framed as the big feedback + walk-test session.

## 2026-06-30 (morning) — user walk-test of the overnight build → REBUILD v2 (real-world physical intro)
The user walk-tested the C19-C26 overnight build + gave major direction (the root: the intro was built at an offset + faked space/descent + a 3-model teleport-stitched pod). USER-APPROVED architectural shift → REBUILD v2 (full plan in docs/feature-escape-pod-intro.md '## REBUILD v2'):
- **Space = a wrapping celestial skybox** (real moving stars, a real-scale-look planet, a real atmosphere limb, its own scene, no desert-sky bleed). Decision: skybox space (camera-relative) NOT true real-scale physics (the float-precision wall ~10km).
- **ONE physical pod model** — physically enter it in the ship, physically eject (no teleport), ride it down in that same interior, physically crash, wake in it, physically exit. 100% consistent + multiplayer-ready.
- **The descent/crash happens PHYSICALLY in the real world** — the viewport shows the actual world the player spawns into; the pod physically crashes into it (a friend could watch your pod come down). NOT a shader faked over a window.
- **Cockpit redesigned off the greybox box**; the **corridor fully modelled**.
- **Sequence fixes:** the parachute lever-pull happens MID-descent (currently reads as after landing); blackouts longer (~2s+, not a flash); impact = blackout.
- **Decisions (AskUserQuestion):** (1) skybox-space + physical-descent [not true-scale floating-origin]; (2) re-plan then rebuild in order (R1 re-grounding → R2 space → R3 pod → R4 descent → R5 ship interiors).

## 2026-07-02 — SHIP walk-test feedback (verbal, in-chat) — the SHIP-FIX batch
Cockpit: the tapered hull kept the OLD box collision (off); side boxes/furniture float (placed for the old walls); the windscreen is many pieces that don't connect — wants ONE clean glass sealed to the hull; overall cleaner/simpler, nothing overlapping/floating (z-fight class). Chair: REMOVE the front harness entirely; remove the floating block top-back; the backrest tips forward (uncomfortable) → recline back; move the chair back; clean consoles; FULL collision (chair+consoles). Corridor: clipping near the doorway (the top vent + the left wall) + a coplanar-face sweep. Bay pod: the metal ribbing has two models overlapping (clipping); the door is wide open — should START CLOSED → openable → walk in → sit → lever, all in-ship. Engine room: a REAL engine room behind a GLASS SLIDING DOOR at the corridor end, on fire, visible through the window, unreachable (replaces the blocky floating fire). Deferred by the user: pod/landing-sequence feedback (more coming) + a performance pass (freezes between phases). "We really need to make this perfect."

## 2026-07-02 — additional ship feedback (verbal, mid-play; folded without stopping Cluster A)
(1) POD-BAY ENTRANCE rework → folded into CLUSTER B: the wall just "opens up" onto the pod — no real doorway; the right-side floating blocks w/ yellow cylinders (the bolt clamps) look out of place; the yellow archway overlaps the wall; messy/overlapping. Wants a DISTINCT, CLEAN entranceway/doorway into the escape pod, seamlessly integrated with the corridor language (which the user likes elsewhere). (2) PLANET PERSPECTIVE bug → its own parallel fix (sky.ts): the planet grows huge when backing down the hallway + shrinks near the window — the camera-anchored celestial has zero parallax. Fix: anchor the planet in SHIP space (fixed position ~400m out) during the ship beats so its size is constant wherever the player stands.

## 2026-07-02 — the player-gated boarding flow + eject view + ship audio (verbal; folded into B/C, A untouched)
(1) → CLUSTER B: the EXACT boarding sequence, ALL player-gated, nothing automatic: checkEngines → see the fire → told to go to the pod → walk there → the pod door is CLOSED → press E to OPEN → physically WALK IN yourself (no scripted carry) → press E on the SEAT to sit → press E on the LEVER to eject. Remove the auto-eject fallback timers — the sequence WAITS on the player at every step (prompts guide; player-gated by design). (2) → CLUSTER C: the eject moment — physically eject + SEE the real ship exterior + the planet through the window for a few seconds, IN-WORLD (not a separate scene), THEN a slight fade → the ship-explosion moment (that part may stay its own staged scene). (3) → CLUSTER C: AUDIO — remove the desert wind + game music during the ship/intro beats (the normal game soundscape/music still runs during the intro — wrong in space); the intro keeps only its own audio (hum/klaxon/intro music cues).

## 2026-07-02 — pod door: closed through descent + MERGED with the viewport (verbal; → CLUSTER B)
(1) The pod door must be FULLY CLOSED before eject AND stay fully closed through the descent + crash (it currently rides down open/ajar). It opens only: player E-opens to board (then seals), and blown/kicked open at the wake. (2) DESIGN CHANGE — merge the DOOR and the glass VIEWPORT into ONE front-facing unit: the viewport IS in the door. The player faces the door+viewport for everything — E-open to board, sit facing it, sealed, watch the whole descent through its glass, crash, then kick THE SAME door open into the desert. No turning sideways to interact vs look. Implications: relocate the hatch to the viewport azimuth (front, −Z), the exterior-skin hatch gap + bay docking orientation follow, the wake/stepOut flow uses the front door.

## 2026-07-02 — washed-out step-out (regression, parallel fix NOW) + "same instance" seamless landing (→ CLUSTER D)
(1) REGRESSION: after exiting the pod the first time, everything is BRIGHT/WASHED OUT (different from before). Suspects: the wake exposure lift (CABIN_WAKE_EXPOSURE 1.62) not restored on the LIVE stepOut path (the one-pod re-scope SKIPS disposePodScene — the old restore point), and/or the strong wake lights (fill 7.3, rakes, hatch flood 12) persisting un-parked in the enterable pod. → a targeted parallel fix immediately (podScene/sequence — no overlap with the running Cluster A). (2) "Different INSTANCE inside vs outside the pod on first exit — it shouldn't be; the same instance, the pod just now exists in the world, I spawn inside it and can walk back in." → CLUSTER D (the seamless landing, after B/C): unify the pod at the CRASH (not at stepOut), wake already in the real world state, the player WALKS OUT themselves (no teleport at stepOut; release control/HUD at the threshold), exterior appears never (it's there from the crash), any interior exposure shift EASES (eye-adaptation) instead of snapping.

## 2026-07-02 — ONE canonical pod model across ALL phases + the descent texture-crawl (verbal)
(1) The pod model is STILL different per phase: the user LIKES the docked bay pod; wants the EXACT SAME model (interior + exterior) on the ship → falling → on the surface. Currently 3 constructions (bay buildDockedPodExterior / ride buildPodScene / landed unify+buildExteriorSkin), size-matched but separate. → CLUSTER B builds the CANONICAL POD as a shared module (interior + exterior + the merged glass front door) + uses it in the bay; CLUSTER D swaps the descent + surface onto the same module (dovetails with the "same instance" seamless landing). (2) During the fall, the INTERIOR TEXTURE crawls/moves rapidly — the procedural grime/paint likely samples WORLD position, so the pattern slides as the pod drops 600m. Fix: sample OBJECT-LOCAL coords (pin the texture to the surface). Queued immediately after the washed-out-fix agent lands (same file, podScene.ts).

## 2026-07-02 — camera resets to face the DOOR/VIEWPORT at every transition (verbal, partially garbled — interpretation confirmed-pending)
On EJECT the camera starts facing FORWARD at the merged door/viewport; and after EVERY blackout / beat transition (eject → descent → parachute → impact → wake), RESET the view to face the door/viewport — the player always comes out of a fade oriented at the window, immediately re-grounded in what's happening. (The door/viewport is the visual anchor of the ride.) → CLUSTER B (owns the boarding flow + beat framing; faceControl at beat inits). NOTE: the tail of the user's message was garbled — interpretation flagged for their confirmation; free-look stays available between transitions.

## 2026-07-02 — REGRESSION: player trapped in the cockpit chair at game start (verbal; → folded into B1 as B1.f, urgent)
Cluster A's new furniture colliders box in the seated capsule (the chair collider ~7cm aft + the dash/console colliders ahead) — the player can't move when the cockpit beat hands over control. smokeIntro missed it (it jumps beats, never physically stands up). Fix in B1: ≥0.25m egress clearance, a collider-free stand-up path, and PROVE with real KCC motion (move ≥1m from the seat + walk aft through the doorway), not clearance numbers.

## 2026-07-02 — cockpit DESIGN round 2 (verbal; queued behind B1, same file)
(1) The windscreen glass still doesn't connect to ALL sides of the hull cleanly — make cockpit+glass a SEAMLESS, clean design that also looks COOL, not just a flat piece of glass (a shaped/raked wrap canopy built FROM the hull profile so it seals by construction; slim intentional framing only; interest from the shape, not clutter). (2) The ship CONTROLS look messy — overlapping/floating pieces; redesign cleaner + more STREAMLINED (coherent surfaces, recessed screens, purposeful groupings). (3) The 2 boxes left+right of the cockpit hull are out of place now — REMOVE + redesign that space (clean hull wall or something integrated that belongs; no free-standing crates). Launch when B1 frees shipScene.ts; parallel-ok with B2 (flow, different files).

## 2026-07-02 — pod door design: KEEP the domed circular porthole, ON the door (verbal; sent live to B1)
The merged door/viewport is NOT a flat glass door: a solid riveted door with the existing DOMED CIRCULAR viewport glass integrated in it (the same domed porthole character as today's cabin), sized to carry the descent view, bezel integral (no doubled rings).

## 2026-07-03 — the planet-approach descent arc (verbal; folded live into CLUSTER C as C3)
After the explosion, the descent doesn't read as falling TOWARD the planet — the distant planet "just fades away and goes to the next phase" (unrealistic). Want: a smooth realistic arc — falling toward the planet (it GROWS/approaches in the porthole), into the atmosphere (limb/glow fills the view → the existing plasma re-entry), through it (the sky blend → the ground approach). Brief ~0.5-1s dips-to-black between phases are OK where a seam needs smoothing; continuity preferred. Constraint: the planet-approach animation runs only in the descent beats (the parallax fix stays for ship-walking).

## 2026-07-03 — PERFORMANCE degrading + the platform question (verbal; strategic)
Frequent frame drops, freezes, stutter — "wondering if we're hitting the limit of what we can do in the browser." User floats: (a) a separate LAUNCHER (out of the browser), (b) transitioning to GODOT. "Either way we'll need to continue optimizing and find the best path forward." → PLAN (after C+D land, since they're reshaping the intro code): (1) the PERFORMANCE PASS with hard numbers first — profile the real hitches (the between-phase freezes smell like shader-compile + scene-build hitches on beat init; frame drops = draw calls/lights/shader cost — all likely fixable in-browser) + fix the top offenders; (2) a PLATFORM DECISION BRIEF — launcher (Electron/Tauri: cheap, native feel, but same WebGL underneath — NOT a perf fix by itself) vs Godot port (a full rewrite — months; all procedural systems/shaders/intro redone) vs web+WebGPU (the actual in-browser ceiling lift), with effort/risk/benefit numbers → the USER decides the path (an engine port is not an autonomous call).

## 2026-07-03 — perf plan approved + the LOADING-SCREEN preload (verbal)
User approved: finish C+D → then the PERFORMANCE PASS. Headline item (user's idea): a LOADING SCREEN on the first New Game that PRELOADS all the intro scenes + force-compiles the shaders that cause the between-phase freezes (build the ship/pod/hauler/engine-room geometry + warm every material/shader — e.g. renderer.compile / a hidden warm-up render — behind an honest progress bar before the cockpit appears; subsequent beats then swap prebuilt scenes with no compile hitches). Plus profiling with before/after numbers + the top frame-cost fixes, then the platform decision brief (launcher vs Godot vs WebGPU) for the user's call.

## 2026-07-03 — remove the starter loadout (intro path) + the chute-gag bug (verbal, pre-bed)
(1) Starter loadout (scrap_bar + full canteen at boot) REMOVED on the escape-pod-intro path — empty pockets; the tutorial's scattered scrap/cloth + the pod's salvage kit are the bootstrap. The non-intro path keeps the ABL loadout (its balance rationale holds there). Flagged: if the user meant remove EVERYWHERE, it's a one-line change. (2) The parachute gag did NOT fire when the user salvaged the pod panel in real play → sent live to the D agent as D5: decouple the gag from the tutorial phase machine — arm at unify/crash, fire on the PRY EVENT itself regardless of tutorial state; prove on the real pry path in two orders.

## 2026-07-03 — SHIP OVERHAUL round 2 (playtest feedback + 2 reference images) — "far from done, messy, not seamless"
W1 COCKPIT (start here): (a) CANOPY REDESIGN per the references — glass DOMINATES (panoramic wraparound, thin structural frames/struts, hull must NOT block the view; redesign the front hull portion so glass lines up + connects cleanly) — the WOW moment seeing space/the planet; current = flat + hull-obstructed. (b) The front console is TOO TALL — blocks pilot-seat vision → lower. (c) The yellow lever caps (right of console) don't connect to their shafts. (d) REMOVE both rectangular side-wall panels w/ the yellow bottom bar (they clip through the hull). (e) The ceiling console's colored buttons FLOAT — connect them. (f) Remove the OLD pre-taper geometry sitting outside the tapered hull; overall: old+new modeling overlaps everywhere → simplify, streamline, everything connects + looks purposeful. (g) REMOVE the chair's full collision + the INVISIBLE leftover collider behind the chair (walk the cockpit freely).
W2 POD BAY re-architecture: the pod STILL reads as a different model — must be the EXACT canonical pod (fully modeled interior + see-through viewport) everywhere. NEW LAYOUT: an OPERATIONAL sliding door (engine-room style) in front of the pod → a SMALL CORRIDOR/AIRLOCK behind it → the pod's own door at the end (airlock-style connection) → the pod body mostly EXTERIOR to the ship. Clean up the hallway pipes/arches crossing in front of the pod.
W3 SHIP MISC: (a) the grey bolts on walls+floor are rotated WRONG (undersides exposed, not flush) — rotation must match their surface; (b) the pipes at the hallway entrance END ABRUPTLY → route into the wall / a thicker archway to hide the ends; (c) the engine-room engine needs more detail.
W4 PLANET PARALLAX REGRESSION: the planet STILL grows relative to the ship as the player walks back (in the real built game, post-fix) — re-diagnose in the real context.
W5 CREW QUARTERS (new): opposite side of the hallway from the pod, further down toward (not too close to) the engine room — sliding doors + a clean entranceway (clear any pipes/models blocking the door front).
THEN: the user has a separate escape-pod-SEQUENCE feedback batch to give after the ship work.

## 2026-07-03 — STANDING RULE: collision matches models exactly (→ CLAUDE.md rule 9 + a ship-wide audit in the wrap)
User: collision doesn't match the models in places; some collision uses OLD models that no longer exist. RULE: always update collision when updating a model; collision matches the visible geometry exactly unless stated otherwise. → CLAUDE.md Architecture rule 9 (permanent). Sent live to W1 (cockpit collider sweep + whole-cockpit motion probe + a --colliders overlay in its report). The W2/W3/W5 briefs carry the rule. The END-OF-OVERHAUL WRAP adds a SHIP-WIDE COLLISION AUDIT: every collider vs the visible geometry across cockpit/corridor/bay/engine/quarters, stale ones deleted, proven by motion probes.

## 2026-07-03 — ESCAPE-POD SEQUENCE feedback (playtest; → W6 sequence + expands W2) — "we really need to get this right"
W6 SEQUENCE (launch now, parallel with W1 — sequence.ts/sky.ts/podScene.ts): (1) CAMERA: stop fixing the camera — seated = FREE-LOOK; orientation set ONCE at first seating (facing the door), never touched again (supersedes the re-anchor-at-transitions spec). (2) THE SHIP VIEW: currently reads as 2 different views (the depart recession, then the fade+re-framed explosion) → ONE continuous view: receding from the ship WHILE it explodes (no fade-and-reframe). (3) PLANET APPROACH too fast → slower + TWO phases: far-and-slow drift toward the planet, then closer approaching-the-atmosphere. (4) IMPACT: a few frames show the view ABOVE the pod (landscape visible) — the view must ALWAYS stay inside the pod. (5) THE ZERO-SHIFT HANDOFF (the "different instance" is STILL felt): NOTHING may change when exiting the pod — no lighting shift (it's STILL super bright/washed out at exit → REMOVE the wake exposure lift + ease entirely; original world lighting from the wake onward; light the cabin with real lamps), no weather/time/fog change (normalize ALL intro world-state high during the fall — the ground state IS the plain survival world from crash onward; the weather at crash stays through exit), the pod persists identical + re-enterable. Wake = spawning into the world; exit = walking out a door, nothing more.
W2 EXPANDED (the canonical pod): the pod is TOO SHORT/STUBBY in the world → taller proportions. The INTERIOR needs a big overhaul — the viewport circle OVERLAPS the door, overlapping models/textures, weird placements → clean redesign where everything fits + connects, consistent with the ship + the new W2 airlock corridor entrance. The EXACT same model through the whole sequence (the user: "i don't think you're getting this correctly" — this is the #1 repeated ask).

## 2026-07-03 — the pod door sits slightly SLANTED when closed (verbal; sent live to W6 as item 6)
Should be closed cleanly/flush. Suspects: an animation ending not-exactly-closed (the seal swing landing off 0, the release judder's sine residue, the cabin door's sealed state), or a static pivot/mount offset. Verify flush at EVERY sealed stage (bay pre-open, post-seal, descent, impact, pre-kick wake).

## 2026-07-03 — pod porthole proportions (verbal; → W2 spec refinement)
The viewport/porthole is TOO BIG for the door, and its circular bezel CLIPS the pod's hull sides. W2's door+porthole rebuild must size the porthole to sit comfortably WITHIN the door slab (clear margin to the door edges on all sides) and keep the bezel fully on the door face (no wrap/clip past the pod's curvature). Pairs with: the taller pod body + "the circle overlaps the door" from the same batch.

## 2026-07-03 — the pod is too PALE/BRIGHT (verbal; sent live to W2a) — match the ship
The canonical pod reads pale/bright (sun-bleached beige aluminium) against the rest of the ship. It must MATCH the ship's worn-gunmetal + grime idiom — one vessel family, one tone (cockpit/corridor/bay/pod). Retune the pod material set (exterior skin + interior) to the shipScene metal family; keep the riveted capsule character. Verify with side-by-side shots against ship surfaces.

## 2026-07-04 — W2 WALK-TEST feedback (playtest, pre-bed; 4 screenshots + 1 cockpit reference) — the X-queue overnight batch
COCKPIT (X1, revise the WHOLE shape — "may be easier to get rid of most of it and start fresh... we need to get it right"): (a) side hull wall pipes + the long light-grey rectangle + darker grey rectangle (back wall → glass) REMOVE — messy; (b) the orange light rectangles along the beam ribbing are misplaced on the FRONT ribs now (back ribs still OK); (c) bolts mis-oriented (cockpit instance of the W3 bolt bug); (d) the yellow box console models on the right+left cockpit walls REMOVE; (e) THE CANOPY REDESIGN: glass like a real sci-fi ship (user reference: framed multi-pane panoramic canopy, Millennium-Falcon-like) — multiple ANGLED PANES connecting, wrapping part of the TOP + RIGHT + LEFT, replacing today's flat boring front sheet; main complaint = not enough view into space. KEEP: the chair, the console (console may be redesigned if it serves the bigger change), the arched rib design. Do research.
SHIP FLOW/STATE (X3): (f) hull GAPS left+right of the pod-bay entrance — you can see SPACE; hallway wall must connect cleanly; (g) after check-engines + fire, the pod door CANNOT be opened until the player detours to the cockpit and back — must be able to board straight from the engine check; (h) the red-alert flashing must PERSIST until actually seated + LAUNCHED (currently stops earlier).
AIRLOCK/POD (X2 — the #1 ask AGAIN, 3rd repeat): (i) airlock more detailed; (j) the bay pod needs COLLISION; (k) the bay pod is STILL not the same model — the interior is EMPTY and you can see SPACE from inside it (screenshot); must be the literal same pod as the eject/crash sequence; (l) the pod door is STILL slightly ajar/slanted pre-eject; (m) INTERIOR REDESIGN: levers/consoles/buttons more detailed + realistic; (n) seated pre-eject the view must still be IN THE SHIP (you haven't ejected yet); (o) at eject the pod flips to the LIGHT/BRIGHT version again — remove the pale version from ALL phases.
LANDED POD (X5): (p) remove the sand streaks + mound around the crashed pod; (q) a metal sheet blocks the pod doorway after landing; (r) the landed pod's viewport/door model differs from the ship's one + the door doesn't look right; (s) can't walk back INTO the pod after the crash — must be re-enterable.
HALLWAY (X4): (t) windows in the hallway; (u) a small CREW QUARTERS room off the hallway LEFT side (opposite the pod), down toward the engine room (refines W5).
Execution: overnight autonomous batch; plan carefully, verify thoroughly, very high quality bar.

## 2026-07-04 — X-queue clarifying answers (pre-bed): canopy = FULL REBUILD (+ hauler nose roughly matched) · eject view = BAY UNTIL EJECT (swap under the blast) · quarters = LIVED-IN BASICS · hallway windows = ONE LONG VIEWPORT STRIP (right wall). Overnight autonomous execution approved. Plan: docs/next-session-prompt.md (the X-queue).

## 2026-07-04 — KNOWN RIG LIMITATION (ship-explode scenario): the porthole-explosion frame shows a PALE noon-flooded cabin — a FALSE frame. The scenario is a LIVE capture (the loop runs for FX), so per-frame updateLighting re-derives noon and stomps the rig's vacuum dim (the real game's applySpaceMode isn't active on the rig's jump path). The in-game eject cabin is VERIFIED dark (X2a). Fix if it matters later: a per-frame dim hook in the scenario's capture loop. Do NOT art-direct the cabin tone from this scenario's frames.

## 2026-07-04 — MORNING WALK-TEST feedback (2 screenshots; → the Y-queue, PLAN-FIRST per user) — "we really need to make sure we get it right this time"
PROCESS: build the FULL plan first, the user READS it before any implementation; ask clarifying questions; collaborative design gates.
COCKPIT (3rd rebuild — the structure itself is wrong): still reads as "a tunnel with a window at the front; the glass doesn't line up with the hull; messy, not connected; nothing like the reference photos." REMOVE the front half of the cockpit hull (wall+ceiling) entirely and rebuild: MULTIPLE GLASS PANES wrapping the LEFT + RIGHT + TOP ("kind of like a dome glass around the cockpit") joined by a METAL SKELETON (like a real ship canopy / the references). From the SEAT you look right/left/up and see glass. KEEP the seat + front console. User offers to guide from a presented design.
HALLWAY: (a) the viewport must be REAL GLASS showing ACTUAL SPACE — the star-backdrop read as "some fake kind of window thing" (remove it; real sky through real glass; make real space never-empty); (b) Z-FIGHTING/clipping: hallway↔cockpit entranceway (overlapping coplanar models flashing), crew-quarters entrance sides, the airlock/pod entrance — sweep + fix; user offers screenshots if needed; (c) REMOVE the emoji from "ENGINE FIRE" prompt — no emojis anywhere.
POD: (d) GLITCH on first open: a "wall of the escape pod" shows in front of/below the doorway (screenshot: lower half of the opening is black); (e) interior is "messy and all overlapping — the previous version + the newer version" (BOTH console generations coexist = a build bug) → SCRAP the whole interior consoles/detail and rebuild from scratch, clean + fitted (seat kept); (f) "Press E to sit" doesn't fire on first tries (eventually works) — fix the gate; (g) pipes visibly clip through the wall when seated (screenshot); (h) REMOVE/redo the messy left-side lever assembly; (i) the LEVER-PULL crash sequence must be interactive-visible: each E = the lever visibly pulls down, 3rd pull snaps the handle off; (j) NEW: after sitting, the POD ROTATES (90/180°?) so the viewport faces out into SPACE, then eject — "rotate and then eject"; (k) the door STILL reads crooked/tilted in SOME descent phases; (l) LANDED-POD COLLISION: none at first, then appears (a state change) — the user got TRAPPED inside when not leaving immediately; collision must exist consistently from the crash, walk in/out freely forever, NO state flips; (m) the pod's GROUND/BASE visually changes once on the planet — find + stop the inconsistency; (n) if the KCC can't fit the doorway, widen it — but keep the model clean.

## 2026-07-04 — MODEL QUALITY TOOLING approved (user idea + refinement): build BOTH now (Y5)
(1) THE MODEL STAGE — an isolated studio environment (own script, rides the rig harness): mounts any single builder alone (neutral grid, 3-point light, auto-turntable 8-12 angles + close-ups) for build-iteration + user review BEFORE game integration. (2) GEOMETRY LINT — automated invariant checks with positions: no floating islands (every mesh touches a neighbor/mount), no coplanar z-fight face pairs, no floor/terrain penetration, no out-of-envelope pieces. Flow: build on stage → lint clean → user review (hero pieces) → integrate → in-game shots + lint again. Rationale: the recurring defect classes (floating/overlap/misaligned) are machine-checkable invariants; stills from sparse angles keep missing them. Context bugs (lighting/state) still need in-game gates.

## 2026-07-04 — LOADING SCREEN + THE PROMOTION TO MASTER (verbal; → Y6 + the release step)
(1) Y6 LOADING SCREEN: after New Game the FIRST FRAME of the desert shows during loading — instead the MAIN MENU scene (the desert + rising sun backdrop) must persist with the loading bar through the whole load, then cut straight into the spaceship intro. No in-between desert frame.
(2) THE RELEASE (after the Y-queue finishes + the user's walk-test sign-off): merge the escape-pod-intro version into the MAIN GAME — master + the GitHub playable link become this version (intro ON by default); "sunset the older one" = the old direct-to-desert New Game start. The intro version becomes the current real game.

## 2026-07-04 — cockpit MULLIONS feedback (verbal; sent live to Y1 mid-build): "way too blocky, don't connect together cleanly, weird angles" — the Mk-III skeleton members need: refined member profiles (not raw boxes — chamfered/beveled beams), REAL JOINT treatment at every node (members must meet cleanly — mitered/node-capped, no butt-joint gaps or overshoots at angle changes), and a REGULAR deliberate pane rhythm (no odd irregular angles between members).

## 2026-07-04 — hauler EXTERIOR nose glazing (verbal; sent live to Y1): NOT a domed bubble — FACETED. Flat panes of glass connected by mullions that together APPROXIMATE a dome-ish shape (polyhedral greenhouse read), no actually-rounded/curved glass surfaces. Matches the interior canopy construction (flat trapezoid panes on a skeleton) — inside and outside read as the same faceted structure.

## 2026-07-04 — hauler THRUSTERS (verbal; sent live to Y1; corroborates the lint's 7-floater finding): the exterior thrusters are SEE-THROUGH (open/unbacked bell geometry) and look NOT CONNECTED to the ship. Fix both: solid-reading bells (closed/back-faced/inner-surface geometry — no seeing through the engine) + a real thrust structure physically mounting them to the hull.

## 2026-07-04 — cockpit Mk-III REVIEW round 1 (user, on the pre-commit frames): (1) the console is a bit TALL — blocks the view; shorten the BACK/TOP part of the console. (2) REMOVE the small blocks on the FURTHEST LEFT + RIGHT sides of the console — the ones with the lighter-grey rectangle that reads almost like a handle.

## 2026-07-04 — the LEVERS (verbal; → Y7): (1) EJECT lever: simpler + functional + an ACTUAL PULL-DOWN ANIMATION when the player ejects (currently fires with no visible lever motion). (2) PARACHUTE lever: simpler too, AND it mounts too HIGH — a weird reach for a seated player; rethink the position (seated-natural reach, low, beside/forward of the seat) and the design. Y3's minimal+chunky rebuild landed the simpler DESIGNS; the animation + reposition are the open items.

## 2026-07-04 — cockpit Mk-III ROUND 2 + pod-bay blockers (user test on the release build; 2 screenshots)
COCKPIT (better but "still needs a lot of work"): (1) TOO MANY mullions too close together — blocks the view; fewer/wider panes; (2) the glass panes read MISSING between members — need a subtle visible glass presence in every cell; (3) mullions still a bit TOO THICK and read flat-grey — need a more detailed metal treatment; (4) the FLOOR doesn't match/connect cleanly to the new front; (5) the glass BOTTOM doesn't connect cleanly to the floor (sill↔floor junction); (6) the lights above float IN the glass over the chair (re-seated strips read detached; the node-boss+orange-rod reads odd); (7) MANY GREY RECTANGLES protrude down where the glass meets the hull (screenshot) — remove/integrate; (8) verify MULTIPLE angles; hard self-critique for anything disconnected/unrealistic; find additional improvements proactively.
POD BAY: (9) the pod ENTRANCE is STILL BLOCKED by part of the model + a BOLTED BAND around the pod crossing the doorway (screenshot) — gap every wrap band/ring at the door aperture; (10) the pod DOOR reads as a DIFFERENT MODEL during the eject phase, then switches to the right one post-eject but with WRONG/TOO-BRIGHT lighting or metal — door parity + the bright-cabin hunt across seal→rotate→eject→release→descent.

## 2026-07-04 — pod door FACE parity + the porthole side gap (user screenshot; sent live to the door-parity agent): (a) one FACE of the pod door is more detailed than the other — make BOTH faces the same: handle + the detailed viewport/bezel assembly on EACH side; (b) from an angle you can SEE STRAIGHT THROUGH the viewport's SIDES — the porthole aperture's cylindrical side wall doesn't connect the two faces; seal the bore (a real through-tube joining both bezels, no gaps).
