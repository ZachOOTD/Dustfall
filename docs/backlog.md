# Backlog

Unprioritized ideas, bugs, polish, debt. Tags:

- `[bug]` — something broken or visibly wrong
- `[feat]` — new gameplay or mechanic
- `[polish]` — feel, UX, visual refinement
- `[debt]` — code cleanup, refactor, doc improvement
- `[idea]` — speculative, needs validation before promotion

When ready to ship one, promote it into a session in [roadmap.md](roadmap.md).
Use `/triage-ideas` to bulk-classify a free-form dump.

---

<!-- entries below this line — newest at the bottom -->
<!-- (procedural world generation — shipped in AAI/AAK) -->
<!-- (creature companion — shipped in AAE) -->

[feat/polish] Player model → Rey/real-human quality — **NOW TRACKED IN [docs/feature-player-model.md](feature-player-model.md)** (5-cycle arc; ACH's "Rig to Rey-tier" detail work shipped band-wraps/glove/scarf/belt/backpack/boots but an honest audit found a wrong silhouette — rigid barrel on stick-legs, blank face — so it was re-planned). Status:
- ✓ **PM-A silhouette** (ACI): slim+taper torso, bigger head → reads as a slim draped human.
- ✓ **PM-B.1 hood** (ACI): wraps the skull (was a floating mushroom-disc).
- ✓ **PM-S foundation** (ACJ, D136): procedural SkinnedMesh — arms + legs are continuous skinned tubes (elbow/knee/wrist bend smoothly, no seam). NEW `src/player/skinnedLimb.ts`. Hands fixed (orientation + thumb mirror); FP viewmodel hands removed.
- ✓ **PM-B.2 face** (ACJ): goggles + brow + nose. ✓ **PM-B.3 face** (ACJ): pale lower-face scarf wrap (distinct from skin).
- ✓ **poncho removed** (ACJ, D139) + junction fillers (hip-cap spheres + widened pelvis + bigger deltoid).
- ✓ **PM-C outfit** (ACK, D140): fitted wrapped tunic that hugs the body lathe (not a flared poncho); belt/pouch/pack/shoulder re-verified.
- ✓ **Realism pass** (ACK, D141/D142): proportions (HEAD_R→0.115 ≈ 1:7.7), contrapposto, player skin+cloth → PBR (MeshStandard + micro-bump + baked occlusion), glassy goggles, slim curled hands, real boots, fuller-cranium head. Model now at its in-pipeline ceiling = believable STYLIZED human.
- ⬜ **PM-D** cloth physics (Verlet drape — adds real motion-drape to the tunic + a possible cloak; shares a solver with iteration-plan Cycle 4 rope). OPTIONAL/deprioritized vs breadth.
- ⬜ **PM-S.3** torso/neck-head skinning — the true torso↔limb junction blend (currently filler-bridged via deltoid/hip-cap). OPTIONAL.
- ⬜ **PM-E** deeper texture (more visible weathering/dye/leather/normal detail). OPTIONAL.
- ⬜ **[polish] In-game lighting mood for figure solidity** (D142): the player reads far more solid under key/rim than the game's flat bright-desert ambient (`lighting.ts` AMBIENT_BASE vs SUN_INTENSITY_MAX). Lowering ambient / raising contrast = solid in-game, but changes the WHOLE game's look — an aesthetic decision, surface before doing. Biggest remaining realism lever for actual play.
- ⬜ **Photoreal = D107 asset fork** — import a rigged+textured humanoid. The only path past "stylized"; breaks zero-asset. User's call.
- Stretch: sled-on-back when undeployed (design fork — surface first).
File: `src/player/playerRig.ts` + `src/player/skinnedLimb.ts`. Verify via the Playwright harness `npm run rig-shot` (D138) — `--pose/--angles/--closeup/--lit=form`; `__game.rigStudio()` (D134/D137) still works for interactive checks.

— — — ACL (2026-06-01) overnight breadth — — —

✓ **SHIPPED in ACL** (removed from active backlog; see changelog + D143-D145): worm twilight-breach audio attenuation · megaWreck ground-level salvage panels · night-sky stars (twinkle/drift/cloud occlusion) · Dune sweeping sandstorm (directional wall) · in-storm movement penalty · 3P upper-body aim twist-IK · speeder angular damping · item viewmodel fidelity (large_tent/bedroll/lantern kits) · NEW amban rifle (ranged weapon) · NEW desert shrew creature. Already-implemented (verified, dropped): footstep-dust-at-foot, walk-cycle↔footstep sync, 3P camera teleport-snap wiring.

✓ **DONE (ACM static + ACN live) — visual-triage of the ACL features**. ACM verified the static layer (star geometry, storm wall, shrew model, rifle viewmodel, aim-twist plumbing). ACN built a ticking Playwright `--scenario` harness (D149) + verified the live-feel items: shrew flee (bolts ~8m @ ~3.2 m/s, settles, resumes wander), **dynamic** aim-twist (D148 — was a constant bias, now leads into turns by turn-rate), rifle fire/reload (ammo 3→2 on fire, →8 reload). Cursor-trap during `rig-shot` also fixed (D147). Removed from active backlog.
🟡 **[polish] aim-twist feel — BUMPED ACV** (`AIM_TWIST_TURN_GAIN` 0.10→0.18); owed a foreground confirm that the continuous-turn lead now reads right (tune further if needed). Also still owed: in-MOTION feel of star twinkle/drift + storm-wall sweep timing (a `--scenario` for those, or a foreground watch).
⬜ **[debt] shrew save-restore determinism** (D144) — restore matches shrews by id assuming deterministic same-seed procgen; if shrew scatter becomes non-deterministic or the seed changes between save/load, saved shrews won't match (boot procgen stands, no crash). Revisit if shrew spawn changes.
🟡 **[polish] amban rifle balance + acquisition — ACQUISITION FIXED ACY**: added to the `massive` wreck loot table (chance 0.02, rarest hero find — mirrors energy_pistol@0.03; was dev-loadout-only). Model also got a scope/mag/recoil-pad upgrade (ACY Lane 1). REMAINING: balance playtest vs scrap_gun/energy_pistol (range 60 / dmg 3 / cd 1.6 / mag 8) — foreground.

[polish] Sled mechanics further tuning (post-ACD playtest). The ACD slope-slide + Option-B-tilt + item-rest-height fixes shipped the core feel, but a focused tuning session would benefit from:
- **Slope-slide speed feel**: current `SLED_SLOPE_SLIDE_GAIN = 6.0` produces ~5 m/s at 10° and ~15 m/s at 30°. Might be too aggressive at very steep slopes; playtest with varied dune steepness and tune.
- **Slope-slide friction / static threshold**: `SLED_KINETIC_FRICTION = 0.15` gives a ~1.4° static threshold. Could be raised for "stickier" slopes (sled stays put on gentle inclines until pushed).
- **Collision feel with world objects**: sled now slides freely downhill into wrecks / cacti / rocks; behavior at collision is "stop dead" (kinematic body, dynamic items bounce off). Could feel better with some elastic bounce, audible thump, or a brief slow-down ramp before contact. Test scenarios: sled slides into a wreck wall at 5 m/s, sled hits a rock outcrop while being towed, sled collides with another sled.
- **Item rest height fine-tune**: ACD shrank the body cuboid hy 0.10 → 0.02 so items rest ~13cm above terrain (~7cm above visual deck base). Could go even tighter (hy=0.01 → items ~10cm above terrain) but body collider may become too thin for stable terrain contact. Playtest first.
- **Sled-to-sled collision**: multiple sleds in the world could collide; behavior currently untested.
- **Tow physics when blocked**: what happens when a towed sled hits an immovable obstacle (cliff face, megaWreck wall)? Currently the rope-snap-inward constraint should handle it (sled stops, rope-snap pulls it back if player keeps moving), but worth a focused playtest.

[feat] Sled riding mechanic — TABLED. Goal: player stands on top of a sled, sled slides downhill (or is towed), player rides along with it. Multiple attempts in the ACC playtest follow-up couldn't make it robust:
- Manual platform-ride detection (raycast + AABB+Y fallback): detection mostly worked but had per-frame misses that accumulated drift.
- Adding the sled's per-frame XZ delta to player KCC's `desired` BEFORE compute: KCC's slope-projection ate ~20% of the delta when player stood on the tilted body (Option B), causing the player to drift off the sled.
- Adding the delta AFTER compute (bypass KCC): drift dropped to ~10% but the player's Y still followed gravity instead of the sled's Y, the gap built, detection eventually dropped.
- Tracking `_frameDeltaY` + sticky ride state + full 3D delta application: even with all of this, the player still fell off after a few seconds. KCC's interaction with a tilted moving kinematic body is fundamentally not designed for this.
PRESERVED in code as foundation for next attempt:
- `sled._frameDeltaX/Y/Z` (computed each frame in `updateSleds`, unused by anything now)
- Sled body tilts to match terrain slope (Option B) — cleanly handles uphill-terrain-poking-through-the-deck which was a precondition for any ride to work.
NEXT-ATTEMPT IDEAS:
- Full "parent player to sled" approach (Option C): when on sled, completely override `setNextKinematicTranslation` to (sled.tr + savedLocalOffset + inputMotion). Skip KCC entirely while riding. Jump (Space) exits ride state.
- Spawn an invisible "ride peg" dynamic body at the sled center (the original "branch on sled" trick the user discovered): a thin upright cylinder that intrudes slightly into the player capsule's lower hemisphere. As the sled drags the peg via friction, the peg's lateral motion shoves the capsule via Rapier's contact resolution. Mirrors the branch case the user observed working.

[polish] ABQ deferred iteration items (all on src/player/playerRig.ts; verified statically in ABQ, need in-motion verification): (1) walk cycle in real motion — ABQ verified at static phase=π/4 + 5π/4; the math is correct but in-motion may need amplitude tweaks once running tick-driven (vs eval-pose). (2) Pauldron R1+ — was already reading well in baseline, untouched in ABQ. (3) Hood drape one-side asymmetry — minor cosmetic gap noted but cuttable. (4) Side-by-side gait cadence: footstep audio fires on separate timer from animation; sync would lock them at heel-strike. (5) Foot IK mid-state transition snap (idle→walking on slope shows brief reset to flat) — cosmetic, low-priority.

[feat] ACC shipped — throw items on sled deck. Sled top-deck collider (friction 0.95, slightly inset to read as "in the bed") + kinematic-rider promotion (D119) + additive save schema for ridingSledId/Pos/Quat + aimable throw arc (full cam-Y velocity). Items thrown onto deck physically rest there + ride with sled as it tows; player can pick them back up via existing E-take.

[feat] B1 Phase 2 RopeEndpoint refactor — SHIPPED in ACC. NEW `src/world/rope.ts` with RopeEndpoint union + Tether{a,b} + resolveEndpointWorldPos. NEW `src/util/playerPos.ts` (3rd consumer triggered the lift). Sled.tether is now a RopeEndpoint (sled is implicit second endpoint). Save additive: 'sled' kind in tether discriminator + optional tetherSledId. D120 codifies. **REMAINING for Phase 3** (post-ACC): (a) RMB-on-rope raycast-pick UX — CUT from ACC per scope-cut #1 (conflicts with D77 RMB metaphor; needs design pass), (b) non-sled tether constraint physics — **SHIPPED ACE** (`ropeConstraint.ts` extraction) + **ACF** (`killDrag.ts`, first non-sled caller — D131), (c) new endpoint kinds: world_anchor stake **SHIPPED ACE**; raider_corpse + sandworm_carcass **SHIPPED ACF** (D131-D132). Phase 3 substantially complete; only the (a) RMB raycast-pick UX remains.

<!-- (procedural-character arc ABP→ABY followups — ARCHIVED ACQ: the rig arc is complete and was superseded by the PM-A/B/C/S + realism rebuild ACI→ACK, now at its in-pipeline ceiling, tracked in feature-player-model.md. The old ABT/ABU/ABV/ABW/ABX/ABY "shipped X, remaining Y" notes + the ABS-deferred head/cloth/sub-pivot list are historical cruft; their live-items shipped across the rebuild. Remaining real-playtest items fold into the foreground-verification cluster — D150.) -->

[polish] Per-item viewmodel readability at 3P distance — ABR P3 verified the dual-mesh swap mechanism works correctly (scrap_bar 5 meshes → branch 3 meshes incl CylinderGeometry attached to rig's rightHandAttach). But held items can be small/dark at 3P distance + blend with rig from a few meters away. Per-item scale or brightness review needed for 3P context. Items to review: canteen, machete, scrap_gun, scrap_bar, bandage. Could add a "thirdPersonScale" or "thirdPersonColorBoost" hint to ItemDef for items that need 3P-specific tuning.

[polish] 3P camera real-playtest pass — ABP shipped Rapier raycast collision + smoothed follow + 3P pitch clamp; verified mechanically (eval gives correct camera positions for the static scenarios) but NOT yet verified in actual game playtest (walking into wreck walls, rapid F-toggles, mounting speeder mid-3P, etc). 30-60 min playtest pass would surface any edge cases. ABQ noted that mount/dismount/save-load should set ctx.player.cameraSnapNextFrame but the wiring isn't in place — camera will lerp across teleports as a visible glitch.

[polish] Held items in 3P real-playtest pass — ABP shipped dual-mesh swap (D113) but only verified mechanically. In actual game: equip canteen + drink, equip machete + swing, equip scrap_gun + reload, equip bandage. Confirm all 4 swap into rig's right hand correctly + render visibly + FP viewmodel inverts visibility on F toggle.

<!-- (FP viewmodel forearm wraps — ARCHIVED ACQ: obsolete; viewModelHands.ts was DELETED in ACJ/ACK when the FP hands were removed for the SkinnedMesh rig.) -->
[feat] base-building mechanics
[idea] remove HUD stat bars in favor of audio/visual/text cues ("you could use a drink")
[feat] multi-worm population — AAP shipped single-worm procgen biome-seeded spawn + noise-scaled detection. Extending to N worms per world needs save-schema bump (currently `sandWorm: { ... } | null` field; would become `sandWorms: [...]`), per-worm min-separation logic, and playtest pass to confirm N>1 doesn't ruin the early game.
[feat] sandworm deeper encounter design — AAP added procgen spawn + noise detection; ABJ shipped weakness/feeding behavior (B12); ABO shipped 'ambush' state (silent submerged → snap to lunge within 12m, 90s cooldown) + dawn/dusk surfacing modifier (×1.30 detection radius in twilight windows); ACC shipped ambient twilight breach (180-400m visibility band, 8min cooldown, ~30% chance per twilight window — pure threat-display, returns to patrol on exit). REMAINING: retreat-and-stalk loop (deferred from ABO per cut #3 — extend tickRetreat to optionally re-enter 'alert' at retreat-end if player still close + worm has HP).

<!-- (satellite dish structural framework + collision — shipped ABO C4: 6 radial backing struts on convex side via createMetalMaterial + central back hub; dish panels switched FrontSide; attachAabbCollider on dishPivot blocks walk-through from climbed-up path.) -->
<!-- (engine heat shield back panel — shipped ABO C5: paired FRONT + BACK lathe shields offset 0.10m (rule 7), BackSide-cloned material darkened to 0x3a1d10 reads as real double-wall plate.) -->
<!-- (stamina tow factor — tuned ABJ: 2.0× → 1.5× — sprint duration when towing 3s → 4s) -->
<!-- (remaining tutorial coverage gaps — shipped ABJ C13: locker_kit + large_tent_kit hints extended with RMB pack-up. All major placeables now mention RMB explicitly.) -->
<!-- (poi.ts scavenger-camp magic-number lift — shipped ABJ D1: 12 numbers lifted to Tuning.SCAVENGER_CAMP_*) -->
<!-- (scrap_gun reload action — shipped ABE: R-key reload + playReloadGun SFX) -->
<!-- (saved companion huddle state — shipped ABJ C1: combined v10→v11 schema bump per D108) -->
<!-- (texture overhaul — shipped ABH: 4 new procedural shader factories (metal/paint/stone/skin) applied across weapons, placeables, world props, creatures. Zero new texture files per D107.) -->
[feat] real PBR texture treatment — D107 formalized the zero-texture-files policy via procedural shaders. Future visual ceiling lift would require revisiting D107 + budgeting for the full PBR pipeline (UV authoring for programmatic geometry, CC0 PBR licensing, 50-200MB asset budget). Speculative until the procedural-shader visual quality is the bottleneck.
[feat] salvage mechanics overhaul — AAR/AAS/AAT/AAU shipped tactile pry + extract + variant interiors + per-component loot + electrical glow + condition tiers + rectangular access-panel proportions + recessed integration. ABG+ABI shipped panel-interior visibility fix (BackSide body + hollow rim frame) and relocated 3 procgen panels that were buried inside their wreck volume or overlapped by sibling decorations. REMAINING: (1) per-panel reachability audit — partial: ABE shipped 1 ground-level megaWreck panel; remaining catwalk panels tracked separately below; (2) thermal-cut tier with fire-starter (alternative pry tool). [Archived 2026-05-24: (3) rare key-card panels, (4) salvage durability per-wreck, (5) weld-kit restore — see Archive section.]
[feat] POI overhaul — partial progress. AAQ shipped themed clusters (military_convoy + refugee_caravan). ABF shipped (2) journals at 5 flagships. ABJ shipped: (1) comm_relay cluster (B3 — 3rd ClusterKind) + 1/3 of (3) biome-specific POI kinds (A4 dune buried cockpit). **ABK shipped: (3a) salt outpost + (3b) rocky entrance** — completes the biome-specific POI family (3 biomes × 1 POI each). [Archived 2026-05-24: (2b) flagship NPC beats — see Archive section. Journal narrative beats remain shipped.]
[idea] dynamic POI model generation system — procedurally compose unique POI silhouettes per-seed instead of hand-modeling each kind. Vocabulary of parts (hull pieces, engine bells, antenna struts, breach patches, salvage panels) + composition rules (this kind has a cockpit + tail + N engines arranged on a tilt; that kind is a flat slab with a turret stub on top). Per-seed variation: bigger/smaller, more or fewer breaches, different tilt angles, paint scheme. Goal: every wreck in a procgen world looks distinct without authoring 50 hand-modeled variants. Big architecture lift; would supersede some of the hand-modeled flagship modules (engineBlock.ts, satelliteDish.ts) and the wrecks.ts procgen palette. Needs design pass + scope discussion before code. Speculative.
[feat] cloth physics — real Verlet/spring-mass simulation on the fabric panels for wind-responsive cloth (tent walls, large-tent door, sails). ~400 LOC, 4-6h focused session. Cheap intermediate: vertex-shader wind ripple in `fabricMaterial.ts` (~30 LOC, ~80% of the visual effect at ~5% of the cost). Backlogged from AAY/AAZ where the user explicitly said "defer".
<!-- (save/load tracks flags.devMode — shipped ABJ C12: combined v11 bump per D108) -->
[debt] print-hints-mode commit flow risks skipping `/session-end` docs — pattern recurred twice (AAW+AAX backfilled in AAY; ABB-ABH 7-session bundle backfilled before ABI). Both times the actual backfill was done end-to-end at the next /session-end invocation, but the gap between commits and doc-updates means changelog/roadmap/decisions can drift. Consider adding a CLAUDE.md note or hook reminding the agent to invoke /session-end before further commits when momentum is high.
✓ **NON-ISSUE (verified ACV)** — speeder spin angular damping: X/Z rotations are physically LOCKED via `setEnabledRotations(false,true,false)`, so a bump can't tilt the bike off-axis; only Y (yaw) rotates and is damped (mounted 1.2/s, unmounted 2.5/s). No worldspace-tilt recovery concern by design.
<!-- (give all dropped items rigid-body physics — shipped ABM B7: Pickup gains optional Rapier body; spawnDroppedPickup opts.world creates it; per-frame updatePickups syncs; v11 save preserves transforms. Seed-spawn stays static.) -->
[feat] generalized rope attachment — re-scoped ABM, **CUT from ABO bundle per pre-committed scope-cut #1**. User chose to ship 7 of 8 items + cut B1 rather than rush a save-schema change late in the session. Plan still applies: (a) NEW UX path via RMB-on-item with rope wielded (reuses D77 wieldAction.ts dispatch — zero new visuals on pickups), (b) data-model refactor splitting Tether into Endpoint pairs (RopeEndpoint union: none/player/speeder/sled/pickup/world_anchor), (c) resolveEndpointWorldPos helper (lift getPlayerPos from sandWorm.ts + companion.ts to src/util/playerPos.ts — 3rd consumer triggers the lift), (d) save migration (additive `tether?: Tether` alongside legacy `tetherKind?` loader-only fallback; no version bump per D81+D108). Pickup bodies ready from ABM B7. Estimated 4-5h.
<!-- (texture overhaul for non-cloth/terrain surfaces — shipped ABH: metal/paint/stone/skin shaders applied to weapons, placeables, rocks, well stones, sandworm, lizard, companion.) -->
✓ **SHIPPED ACAA (FP-viewmodel playtest fixes, D170)**: (1) see-through grip-rings/coils on detailed FP items fixed — viewmodel now renders in its own scene in a 2nd depth-cleared pass (depthTest back on); (2) branch splinter-bristles/knots dropped + held/world branches unified via `buildBranchMesh`; (3) real layered torch fire (additive flame + ember sparks) shown only when lit (the old `transparent=false` viewmodel override that killed flame alpha is fixed too). Tool: NEW `npm run rig-shot --scenario=fp-item --items=<a,b>` renders the REAL first-person viewmodel.
🟡 **[polish] 3P torch flame doesn't animate/show** (ACAA): `updateHeld` drives the FP viewmodel's `torchFlame` group only; the 3P hand-attach copy's flame stays hidden (pre-existing). Low priority — only matters if you watch a lit torch in 3P. Would need updateHeld (or a parallel tick) to reach the rig-hand mesh.
🟡 **[polish] FP held-item night lighting** (D170): the viewmodel now uses fixed `vm.scene` lights (no longer world-lit), so held items don't dim at night. Readability win, but if it reads wrong tune the ambient/key/fill in `createViewModel`.
✓ **[polish] item viewmodel fidelity pass — COMPLETE (ACY + ACZ)**: ACY deep-polished the 12 high-visibility held items; **ACZ finished the remaining ~22** — deep-polished the 7 primitives (tent_kit, sled_kit, grill_kit, companion_pod, cactus_pulp, alien_fruit, raw_worm_meat), light-touched branch, and verified scrap/cloth/stake_kit + the already-upgraded kits (fire/locker/lantern/bedroll/large_tent) + cooked foods + creature-mesh meats (lizard/shrew/skewers) at parity. **Every item in the game now has a detailed/verified mesh.** Tool (for future tweaks): `npm run rig-shot --scenario=item-studio --items=<a,b,c> --angles=front,3q,left,top`. Remaining headroom (optional, low-priority): individual hero items could go deeper (energy_pistol idle glow, amban materials); the procgen hulls read dark so greebles/detail would surface more under a brighter-lit pass.
<!-- (salvage panel placement visual audit — shipped ABB: satelliteDish back-of-dish, crashedHull bell-throat, engineBlock bell-wall positions all fixed; openingWreck lookAt-positioned panels were already correct. Then ABG removed opening-wreck panels entirely per D15. ABI extended: rim rebuilt as 4-bar hollow frame (was solid plate occluding cavity post-BackSide-fix) + cargo_container / fuselage / escape_pod panels relocated off overlap-prone or buried positions.) -->
[feat] craftable hover bike — player builds it from found parts instead of starting with one
[feat] interactive opening cutscene — drop-pod descent from orbit through a window view of the desert planet, blinking alarm + parachute-deploy lever in front of player, player hits lever multiple times but it doesn't deploy and snaps off, pod collides with desert + blackout, wake up + open pod door + exit to start the game, scrap pieces scattered around the pod wreck to salvage, craft scrap_bar from them and pry the first salvage panel off own pod, that pry triggers a delayed comedic parachute deploy. Implementation approach: keep the existing CC-3 title screen; on NEW GAME, spawn the player inside a NEW small enclosed pod mesh (single-occupant scale) with a pre-baked descent animation (camera fixed inside the pod, window-mesh + skybox + ground-approach baked into a timeline), NOT a separate scene-swap like the title. Lever, blackout, pod-exit, parachute-deploy are all in-world interactions on the same scene the player will then play in. The existing W-era opening wreck (tapered fuselage with the skeleton + journal + tally marks + salvage panels — RR/AAJ build) STAYS as a separate POI placed near the new pod-crash spawn point — it becomes the first piece of world the player explores after exiting their own pod (the wreck is the previous occupant's larger ship, a narrative beat: "someone else crashed here before me"). The opening-scene anchor + seed-stability rules from AAI still apply.
<!-- (mega-wreck rebuild — shipped ABL: edit-in-place visual lift. Procedural shader vocab applied to all hull/rust/pipe materials. Tapered ellipsoidal cylinder shell drapes over the box-wall aft. 6 rust band wraps. Exposed vertical ribs + torn hull-plate fragments at the mid-hull break. Preserved colliders + all 8 panels + shelter zone + journal — zero gameplay impact. Closes BB-2/BB-3 era visual debt.) -->
[feat] procgen wreck expansion — ABA shipped first-cut; ABC+ABD shipped (1) 2 more hullSegment variants (OPEN_TRUSS, FUEL_BARRELS), (2) gunship class, (4) breach patches helper, (5) PROCGEN_COMPOSITE_SHARE 0.35→0.50. ABJ shipped (2b) 4th class science_vessel + (3) biome-bias on recipe pick (salt→PLATED, rocky→OPEN_TRUSS, dune→FUEL_BARRELS — B4). ABN shipped (2c) 5th class bulk_hauler (longest hull 7-8 parts, 3-4 panels, cargo_container palette). ABO shipped (6a) engineBlock POC migration via 'flagship_engineBlock' fixed-recipe class — proves the composite vocabulary CAN express a hand-modeled flagship; engineBlock.ts kept on disk for revert. REMAINING (6b): if engineBlock POC reads well in playtest, sweep remaining 4 flagships (megaShip / megaWreck / satelliteDish / crashedHull) using the same flagship_<kind> fixed-recipe pattern (~6-8h).
[feat] multiplayer server — networking layer enabling multi-player Dustfall; depends on player model below; see shared-memory/multiplayer-mvp-scope.md for the server-auth-first + snap→interpolate→predict spectrum guidance
<!-- (high-fidelity player model — shipped ABO A3: procedural primitive rig in src/player/playerRig.ts; capsule torso + sphere head + 2 hip pivots + 2 shoulder pivots; hand-coded walk cycle 1.6/2.4 Hz; idle breathing + crouch state; createSkinMaterial localSpace=true per D109. F-key 3P camera toggle in src/core/input.ts; syncCameraToBody branch in controller.ts offsets 2.5m behind + 1.5m above using cam direction as spring-arm. D110. Future MP unblocks "see other players"; rigged GLB upgrade still possible (replace primitive rig in playerRig.ts; raider.ts GLB infra is the reference).) -->
[feat] character customization — color / clothing / accessory variants on the player model; unblocked once the rigged player model exists; opens cosmetic depth for multiplayer identity
<!-- (saved journal read/unread state — shipped ABJ C2: per-kind set persisted v11; interact prompt suffixes "(read)" for already-read kinds) -->
<!-- (megaWreck catwalk panel reachability — DONE: ABE added 1 ground panel between the bells; ACL added 2 more ground-level exterior hull panels (#9 aft -X side wall + #10 bow +X side wall, megaWreck.ts ~L1477). All megaWreck loot now ground-reachable. ARCHIVED ACR.) -->
<!-- (megaWreck bow hull-shell extension to bow — shipped ABN: half-cylinder thetaStart=0..π caps the upper bow box, open underside preserves -X side entrance, matches aft ellipsoidal scale for continuous silhouette. Closes ABL deferred item.) -->
[polish] dropped-item playtest tune — ABM defaults (linear damping 0.6 / angular damping 0.8 / friction 0.85 / restitution 0.15 / density 0.6) need in-play signal. May need to bump friction higher if items roll forever on slopes, or tune density if heavier items feel weightless on toss.
<!-- (additional shader variants — shipped ABJ Tier 2: woodGrainMaterial + boneMaterial + glassMaterial all added. Applications: sled deck/rails/runners + locker body+lid (wood-grain), opening-wreck skeleton (bone), dune cockpit cracked-window (glass). Canteen + lantern globe deferred — no actual glass surface in those meshes.) -->
<!-- (companion runs to pre-mount position — shipped ABN: new getPlayerPos(ctx) helper in companion.ts reads ctx.speeder.body.translation() when mounted, mirrors sandWorm.ts pattern.) -->
<!-- (procedural shaders crawl on moving entities — shipped ABN: D109. opts.localSpace added to skinMaterial + paintMaterial; applied to companion + sandworm + lizard + speeder. Static surfaces keep world-space sampling for coherent weathering.) -->
<!-- (cloth + bandage viewmodels expand on movement — shipped ABN: opts.disableShimmer added to fabricMaterial; applied to cloth + bandage viewmodels; skips wind displacement + uniform-tick + switches noise to object-local.) -->
<!-- (stale fire+cloth wreck POI — shipped ABO C1: scavenger camp had the fire ring + bandage pickup; stripped to fuselage-only. If user meant a different POI, surface in next triage.) -->
[polish] item viewmodel fidelity pass remainder — ABO C3 shipped 6 of 8 worst-flagged (cooked_cactus_pulp, cooked_lizard_meat, cooked_worm_meat, fire_kit, grill_kit, locker_kit). ~19 ItemDefs remain at primitive/basic-shader complexity. Suggested next batch: large_tent_kit, bedroll_kit, lantern_kit (placeable kits all simple boxes); cactus_pulp raw (already has some detail); raw_lizard_meat (uses makeLizardVisual — ok); torch + flashlight (have custom emissive — lower priority).

[polish] per-item accurate collision shapes — currently `spawnDroppedPickup` builds a single AABB cuboid collider from the mesh bounding box (`pickups.ts` lines ~350-375). Items with complex silhouettes (machete blade, scrap_gun barrel, branch with offshoot twigs, lizard-on-stick) collide as oversized boxes that don't match the visible shape. Visible artifacts: items wedging against geometry at gaps the box won't fit through, "invisible cushion" around thin items resting on the sled deck, items appearing to float on the ground because the box bottom is below the visible mesh bottom. **DEFERRED until AFTER the viewmodel fidelity pass remainder above** — collision shapes derived from the meshes need to follow whatever the new meshes are. Doing collision first would be wasted work. Implementation when ready: per-item `ItemDef.makeCollider?(world, body)` factory that returns a compound or multi-cuboid collider matching the silhouette. Default falls back to current AABB. Probably 1-2h per item × maybe 8-10 worst items.
<!-- (3P camera spring-arm collision — shipped ABP Tier 3: Rapier raycast from player head along inverse camera direction; clamps camera to hit.toi - 0.3m pushback. Per D112.) -->
<!-- (ABO A3 player rig + 3P camera polish pass — shipped ABP across all 4 active tiers: proportions overhauled + 7 mismatched-scavenger clothing layers + knee/elbow sub-pivots + 3-phase walk + FOOT IK + 3P collision + camera offsets bumped 3.2/1.8 + smoothed follow + pitch clamp + dual-mesh held items + FP forearm wraps. D111-D113. Walk-cycle-to-footstep-cadence sync deferred; rig shadow softness deferred; mouse-look-orbit-around-player deferred — all stretch polish.) -->
[polish] 3P walk-cycle to footstep cadence sync — playerRig.ts gait runs on its own sin-wave timing; controller.ts footsteps fire on distance accumulator. They don't lock — visually a footstep can fire mid-air. Either drive footsteps from rig gait phase, or drive gait phase from footstep cadence. ~1-2h.
[polish] 3P mouse-look ergonomics — current implementation reuses FP PointerLockControls rotation directly. Users from games like Souls expect mouse to orbit AROUND the player in 3P rather than aim FROM the player. Trade-off vs FP↔3P aim consistency. ~2-3h to make optional via setting.

✓ **FIXED ACS — ACF carcass tow/harvest flow**. Investigation found harvest was actually BLOCKED while towing (the towed branch returned before the loot branch), so the dramatic "towed-then-harvested can't cut loose" was unreachable, and a harvested carcass became fully inert. Fix: the towed branch now lets you **carve meat with E while towing** (LMB still cuts loose); `lootSandWorm` keeps the interact tag while the carcass is towed; the interaction raycast still targets a towed-looted carcass — so you can tow → carve → cut loose in any order. tsc clean; logic-traced; owed a foreground confirm (tow a worm carcass behind the speeder, E to carve, LMB to cut loose). Files: `sandWorm.ts` (lootSandWorm), `interaction.ts` (towed branch + target-push + shared harvestWorm helper).

[debt] gamedev-framework feedback from ACF smoke-test — two findings worth folding into the framework: (1) session-start step-5 dispatcher over-classifies ("fan out 2 agents") BEFORE reading code; ACF Option A turned out ~90% shared-serialization-point infra + lopsided per-entity remainder + observation-dependent visual-triage (single preview server) — none fannable. The skill needs a "re-classify after reading the architecture" beat so the a-priori guess isn't load-bearing. (2) `decisions.md` (~70K tokens) exceeds the whole session-start doc-read budget; the skill should say "grep recent D-entries" or the file should split recent/archive. Lives in `~/projects/gamedev-framework/`, not this repo.

🟡 **[polish] in-storm sensory degradation — PARTIAL ACW (#134)**: fog + vignette ✓ACL; ACW added the missing **camera pitch/roll sway** (scaled by perceivedIntensity) + a **master audio low-pass** that muffles the mix as the storm engulfs the player. Foreground feel-tune owed (amp/cutoff). Near-zero forward visibility is already covered by ACL's FogExp2 ramp.
[feat] real rope physics with slack — segmented/Verlet rope sim instead of the current cosmetic catmull-rom sag + inextensible position-snap constraint (ACE ropeConstraint.ts); ropes should hang, drag, and go taut like real rope.
✓ **SHIPPED ACAB (Cycle 6, D171)** — daytime sky variation: a procedural FBM cloud layer in the sky-dome shader driven by a NEW `weather.cloudiness` field that slowly wanders clear↔overcast (independent of storms); overcast flattens the world lighting + veils sun/moon/stars; storms force the sky overcast (ominous telegraph before the dust wall). Verify via `npm run rig-shot --scenario=sky`. Optional follow-ups (not done): cloud shadows dappling the terrain, a second high-cirrus layer, god-rays.
[feat] rare "wreck yard" biome — ship-graveyard/junkyard with highly condensed wrecks (large + small); 4th biome alongside dune/salt/rocky, spawns rarely.
[idea] wreck-yard-exclusive loot — items obtainable only in the wreck-yard biome; specific loot TBD, likely tied to a future mechanic.
[idea] Sarlacc pit (Star Wars / Boba Fett style) — rare dune-biome hazard, falling in = death, throw items in for a TBD effect; exact mechanic unscoped.
🟡 **[feat] desert shrew CATCH/COOK — SHIPPED ACR** (mirror of the lizard). Wired: `shrew.ts` (`'dead'` state + `damageShrew`/`applyDeadShrewPose`/`lootShrew` + dead-skip in update), `combat.ts` dispatch branch (`getShrewForCollider→damageShrew`), `interaction.ts` (`'shrews'` registry + dead-shrew raycast target + `'take'` case → `raw_shrew_meat`), `items.ts` (`raw_shrew_meat`/`cooked_shrew_meat`) + COOK_MAP, `save.ts` (persist/restore dead + looted, mirror lizard). tsc clean; harness confirmed the KILL (deadState='dead' + 'take' tag). **Foreground-confirm owed**: the take+cook end-to-end loop couldn't be headless-verified (combat/interaction raycast-aim on a small fleeing AI critter isn't reliably scriptable — the take CASE is a verbatim lizard-copy, so real play should be fine). The "burrows into sand" flavor was NOT added (the shrew flees) — separate polish if wanted.
[feat] **DEEP CAVE SYSTEM → companion egg** (acquisition path; REVERTED ACV — held off pending a proper cave build). The egg-acquisition SPINE was built + working in ACV (`2d4035b`) but placed in the existing shallow `rockyEntrance` chamber (a single 4×3×2.5m room), which is NOT the vision — so it was reverted (ACV `git revert`, restoring companion-at-side). **VISION (user, ACV)**: a genuine underground cave SYSTEM — goes DEEP into the terrain, dark, SPRAWLING with branching passageways you could get LOST in; entirely below the surface, reached via some kind of surface OPENING you descend through. The companion egg lives deep inside. This is a substantial procedural-cave-generation build (tunnels/chambers below terrain + descent opening + dark navigation + getting-lost feel + lighting), NOT an extension of the shallow rockyEntrance. **Needs a design pass before code** (how to generate the sprawl: cellular-automata / tunnel-carving / hand-authored modules? collision for walkable passages below the heightfield? lighting/torch model for the dark?). **RECOVERABLE INFRA**: the acquisition spine (egg + `'eggs'` interaction + `despawnCompanion` + additive `companionAcquired` save [legacy default true, no version bump] + `main.ts` boot reconcile) is intact in commit `2d4035b` to cherry-pick once the real cave exists; the gated-acquisition PATTERN is in `shared-memory/save-schema-migration.md` (D158). Pair with: companion proc-character rebuild (Cycle 7 finish) + "Pebble" rename (already ✓ACQ).
[polish] rename the companion to "Pebble" (UI/journal/prompt copy).
[feat] lie-down-to-sleep — camera lerps low to a fixed pose just above the bedroll, sleep, wake in the same pose + stand; replaces instant-sleep overlay. Needs lie-down/get-up anims (gated on player rigging progress).
[feat] rope-attach to the speeder by clicking its rear mount bar (replaces the mount-while-holding-rope tether-transfer flow); lets the speeder be roped/pulled like other endpoints.
✓ **SHIPPED ACU (#50, D157)** — rope leaves inventory only when BOTH ends anchored (sled + stake/Pebble/speeder/static-pos); stays carried while player-held so you can tie the 2nd end. `applyTether` centralizes it; drop-with-G releases the sled; LMB floor-drop removed. User-confirmed.
✓ **SHIPPED ACW (#146)** — sandstorm wind pushes physics bodies: `stormWindAccel(weather)` → per-frame impulse on dynamic dropped pickups, velocity nudge on the parked speeder, slide-velocity nudge on the kinematic sled. World-intensity-driven (D163). Foreground feel-tune owed.
✓ **FIXED ACQ** — E near the speeder mounting without looking: ACQ added the `SPEEDER_MOUNT_LOOK_DOT` gate (camera must face the bike within ~60°, not proximity alone). Verified ACV.
✓ **[bug] 3P rig on speeder — SHIPPED ACV (D159) + RE-SOLVED ACX**. Repositioned to the rider seat with a seated pose (ACV); ACX re-solved the pose from scratch via the `bike-truth` numeric-IK sweep harness (D165/D166): forward lean pivoted at the waist (D167 — fixes torso-disconnect), hands on the bars (~5cm), butt back on the seat (`SPEEDER_RIG_SEAT_Z` 0.28→0.36), legs splayed astride (feet ~22cm from pegs). Dedicated 3P **chase cam** shipped (`SPEEDER_3P_CAM_*`). REMAINING (foreground): in-motion riding feel + exact feet-on-pegs contact.
✓ **SHIPPED ACW (#149)** — 3P interact prompt now projects the hovered object's world hit point to screen + pins the prompt there (was crosshair-centered → read detached in 3P); FP unchanged. `getHoverWorldPos` getter on interaction.ts. On-object placement foreground-verify owed (headless couldn't stage a live 3P hover).
✓ **FIXED ACQ+ACU** — iron stake model: sand mound removed (ACQ), rope-loop ring seated at the top touching the shaft (`STAKE_LOOP_OFFSET_X/Y`), and `resolveEndpointWorldPos` stake case resolves the actual ring world pos (offset + yaw). Verified ACV.

---

## Archive (parked — may revisit)

Intentionally not active. Detail preserved so the call is reversible. Each entry includes the parking rationale; move back to the main section + drop the `-parked` suffix if it earns its slot back.

[feat-parked 2026-05-24] **flagship NPC beats** — hostile raider holdouts + friendly hermit NPCs at hand-modeled flagships (megaShip, megaWreck, satelliteDish, crashedHull, engineBlock). ABF shipped narrative-beat journals; this would add live NPCs to inhabit those places — raiders to fight, hermits to trade with or learn lore from. **Parked**: needs upstream design decisions about NPC AI scope (combat-only? dialogue? trade economy?) before being worth a ~4-6h session. Revisit when the gameplay loop wants more "lived-in" world signals than journals alone can carry.

[feat-parked 2026-05-24] **salvage durability per-wreck** — each wreck has finite mass across all panels; deplete completely → "corpse" wreck rendered with dimmed material to signal it's been fully stripped. **Parked**: closes the salvage loop conceptually but the current "infinite-stripping per-panel" model doesn't read as broken in playtest (panel condition tiers per AAT already provide pacing). Revisit if the salvage economy needs tightening or if the world starts to feel exhausted of salvage targets.

[feat-parked 2026-05-24] **rare key-card panels** — gated behind a quest item. A rare panel kind that needs a key-card item (found in another rare panel? quest reward?) to pry, gating a high-tier loot drop. **Parked**: nice long-tail variety hook but adds a new item type, new panel rarity tier, AND a quest mechanic — too many degrees of design freedom without a clearer goal. Revisit if/when the salvage variety feels stale or a quest system gets prioritized.

[feat-parked 2026-05-24] **restore corroded panels via weld kit** — new item (weld_kit) that lets player upgrade corroded panels to standard quality, inverting D96 (which made condition deterministic + immutable). Requires: condition becomes player-mutable, must move into save schema (currently derived at boot). **Parked**: adds a player-power-curve element but ABT's deterministic condition is working well; the inversion would also require a new schema bump for what's essentially a quality-of-life polish. Revisit if corroded panels start feeling like a frustration tax rather than a flavor variation.

[feat-parked 2026-05-24] **add machete back as wreck loot** — ABK-tail swapped the starter loadout from machete to scrap_bar (the scrap-bootstrap fix). Player now starts unarmed for melee and must craft pipe_staff. Could add machete as a small-chance procgen wreck loot drop so it's findable within ~2-3 minutes of starting. **Parked**: the "start unarmed" framing is gameplay-intentional now; whether it feels too punishing needs in-play signal before adding the safety-net drop. Revisit if playtesters report early-game-too-hostile.

— — — ACN (2026-06-01) user-reported bugs — — —

✓ **FIXED ACT (D151) + confirmed ACU** — speeder-dismount footprints: real root cause was footprints/audio dead in FP entirely (rig gait bookkeeping was behind the 3P visibility gate). Fixed by hoisting it; user foreground-confirmed in ACU.
✓ **FIXED ACU (#40)** — random player speed spike: clamped the KCC horizontal corrected delta (penetration-recovery lurch). User-confirmed.
✓ **FIXED ACU (#42, D156)** — sled clips through POI: shapecast-clamp vs fixed non-terrain colliders, gated by `Tuning.SLED_POI_COLLISION`. User-confirmed.
✓ **SHIPPED ACO — ambient dust off at night**: the always-on tan `ambientDust` drift fades to 0 across dusk by `ctx.time.sunHeight` (`AMBIENT_DUST_NIGHT_FADE_LO/HI` 0.02/0.20) + hides entirely at night → stars read + calm night. (The near-white firelight `dustMotes` left on.) Verified via the `night-sky` harness scenario.
✓ **[bug] salvage panel interiors clip through POI walls — RESOLVED (ACP audit + ACAI)**. The `panels` harness sweep + 15-call-site audit found the 6 common procgen kinds render fine (not systemic); the lone faceYaw offender was `buriedCockpit`. **ACAI removed the buriedCockpit POI entirely** (user: "reads pretty bad — a sphere with panels weirdly phasing through it"), so the one remaining offender is gone. If the user sees OTHER panels clip, re-open + point at the POI. Tool: `npm run rig-shot --scenario=panels`.

— — — ACT (2026-06-01) idea dump — — —

🟡 **[polish] upgrade item models — ACW fixed broken; ACY deep-polished the 12 hero held items**. ACW fixed the two BROKEN ones (raw/cooked_shrew_meat). ACY (Lane 1) rebuilt the 12 high-visibility held items to highly-detailed (see the item-viewmodel-fidelity entry above for the list + the remaining ~22). REMAINING = the kits/foods/materials pass.
✓ **[polish] held items sit correctly in 3P hand — SHIPPED ACW+ACX**: `ItemDef.handAttachTransform` (Phase-A hook) authored for all held WEAPONS — machete/scrap_bar/pipe_staff (ACW, +Y lift so the grip seats in the fist) + scrap_gun/energy_pistol/amban_rifle (ACX, `rot.x≈π/2` to rotate the barrel from down to forward + lift). Tools/consumables (canteen/torch/flashlight/bandage) verified to seat acceptably with no transform. Each verified in the `held-item` rig-shot scenario (arm posed out, close hand framing). Remaining = food/scrap/kits, which are minor (placed or briefly-held).
🟡 **[feat] 3P use animations — PARTIAL ACW; DEFERRED ACY (user)**: ACW added `ItemDef.playUseAnim3P` + a machete diagonal-chop. REMAINING (scrap_bar pry, canteen drink, gun recoil, bandage apply) **deferred per user (ACY)** — a different rigged player model may be imported later, which would obsolete per-item hand-authored 3P use-anims. Revisit only after the player-model direction is settled.
✓ **SHIPPED ACW** — lizard + shrew walking gaits (diagonal-pair leg step while moving, via `creatureGait.ts`). Foreground feel-tune owed (D150).
✓ **SHIPPED ACW** — shrew burrow-on-approach (new FSM state — dives nose-down into the sand within SHREW_BURROW_RADIUS, sand puff, re-emerges). Foreground dive/puff timing owed.
✓ **SHIPPED ACW (#183)** — speeder dust trail (speed-gated pooled particles behind/under the bike, ≈2× on boost).
✓ **SHIPPED ACW (#184)** — speeder engine-ignition glow (nozzle emissive + PointLight ramp with speed, die when parked).
✓ **SHIPPED ACV+ACX** — seated 3rd-person rig stance + dedicated 3P chase camera while mounted on the speeder (seated pose ACV; numeric-IK re-solve + chase cam + footstep-through-items depth fix ACX). See the 3P-rig-on-speeder entry above.
[idea] wrap player model in a cloth robe with real cloth physics to mask the low-poly rig.
✓ **SHIPPED ACV** — sled drag marks lightened (`FOOTPRINT_SLED_COLOR_HEX` 0x3d2918 → 0x6e5236).
🟡 **[polish] add detail to existing POI models — PARTIAL ACY/ACAO**: `addHullGreebles` vocabulary now 7 types (seams / rivets / vent boxes + ACAO louvered vents / circular ports / fins / antenna stubs, rule-7 depth, tagged `isWreckDecoration`) on ribbed-cylinder + plated-rectangular hulls, with impact-flank asymmetry (~78% of greebles + the damage patch cluster on the hit side, ACAO). REMAINING: greebles on the hand-modeled flagships + the boxy/truss/barrel procgen variants; the procgen hulls read fairly dark, so a brighter-lit visual pass would surface the detail more.
🟡 **[feat] salvage-panel variations — PARTIAL ACY**: panel **size variants** (small/standard/large via `SALVAGE_PANEL_SCALE_*` + the existing `scale` arg, deterministic per-panel roll) shipped. Component layouts already vary by panel KIND (`PANEL_COMPONENT_PALETTES`). REMAINING (optional): per-panel variable slot counts + a genuinely new panel `kind` with a distinct interior look.
✓ **SHIPPED ACY (#190, D168)** — dynamic salvage-panel placement on procgen wrecks: `findPanelMount` raycast surface-sampler (jittered ±Z-flank ray grid → first hit that's outward + flat (4-ray probe ring) + not on a tagged decoration + clear of placed panels; cardinal `faceYaw`, part-local return) replaces the single hardcoded `panelAnchor` (kept as a fallback). Verified by `__game.panelBuryAudit` (75/77+ pass across 3 seeds). REMAINING: the hand-modeled curved `engine_bell`/`escape_pod` panels still flag the audit (pre-existing curved-surface cases the sampler doesn't touch — they're `panelAnchor:null`/hand-modeled); could be re-mounted via the sampler if it's extended to those parts.

— — — ACAF (2026-06-05) idea dump — — —

✓ **SHIPPED ACAH** — night dust ground-clamp (motes stay low at night, stars unobscured); mounted-vs-on-foot lighting (D180 — lighting follows the speeder while mounted); speeder tow-bar reseat + antenna slow-blink beacon; DEV MODE Backquote keybind (badge unclickable while pointer-locked); cloud shadows on the terrain; scrap model overhaul (shared `buildScrapMesh`) + remaining-item audit (folded into the D177 material-cache sweep); loot bootstrap = scrap scatters around wrecks (D178); rare vulture (D179 — perch/flee/shoot-for-meat); sandworm shelter-immunity.
[feat] procedural speeder bikes that must be repaired before they're functional
🟡 **[feat] overhaul procgen wrecks + salvage-panel placement — ACAJ CONTINUATION (T3)**. The `wreckForms.ts` toolkit (ACAJ T1) is built; T3 applies it to the shared part vocabulary (`wrecks.ts` hull/cockpit variants + `procgenWreck.ts` `HULL_SEGMENT_VARIANTS` → Lathe hulls + exposed formers + real `makeBreach` holes), which levels up all ~80 procgen wrecks. Keep the recipe/assembly/biome system + `panelAnchor` fallbacks. Verify `panels` bury-audit + a `wreck-visual` strip.
[feat] opening cutscene: player descends from space in an ODST-style drop pod
✓ **[feat] mega-wreck exterior rebuild — SHIPPED ACAJ T2 (D185)**: was a box cluster; rebuilt the exterior with a curved toolkit hull (tapered fuselage + exposed rib formers at the fracture + asymmetric breaches + tapered bridge cap), FrontSide + noCollider, interior/colliders/panels/shelter/journal untouched. Reads as a downed ship. (The boxy INTERIOR is intentionally preserved — a ship interior is boxy; D185.)
🟡 **[feat] ACAJ continuation — finish the wrecks+perf overnight** (the `wreckForms.ts` toolkit is ready): T3 procgen-fleet vocabulary level-up (above), T4 half-burial + `makeSandMound` (sink wrecks into the dunes; the mound color reads cool under studio lights — tune in-context), T5 greeble + functional-asymmetry pass, **T6 (never-cut) WebGL wreck perf merge** (`mergeGeometries` each wreck's static meshes by material; panels/colliders stay separate; measure via `perf-probe` — folds the §207 wreck perf debt), T7 InstancedMesh/LOD. See `next-session-prompt.md` + the plan in `.claude/plans/archive/ACAJ-*`.
🟡 **[polish] vulture in-MOTION feel (ACAI, foreground-owed D150)** — ACAI shipped the full rig + anims + relocate-and-land FSM + dynamic-body death tumble (all logic-verified + pose-screenshotted). What a human still needs to tune in a real playtest: the FLAP cadence (`VULTURE_FLAP_HZ`/`FLAP_AMP`/`WING_EXTEND`), the relocate flight ARC (`FLEE_SPEED`/`CRUISE_HEIGHT`/`CLIMB_RATE`), the landing flare feel (`LAND_DESCENT`/`LAND_SPEED_FACTOR`/`LAND_ARRIVE_DIST`), the spot radius, and the death tumble (`DEATH_SPIN`/`SETTLE_*`). The headless slow clock can't judge any of these.
🟡 **[polish] cloud-shadow strength tune (ACAH, foreground)** — `CLOUD_SHADOW_SCALE`/`DARKEN` set subtle at gameplay angle; confirm the moving dapple reads right (not too strong/weak) across overcast levels in a real playtest.

✓ **SHIPPED ACAI** — vulture fully rigged: joint-pivot rig + per-state anim (idle/flap/landing/death), branch-accurate perch (captured during dead-tree gen — `TreePerch[]`), relocate-and-land FSM (flies to another tree + re-perches), dynamic-body death tumble (real physics, mirrors dropped-item bodies), dead-tree trunk colliders (D181-D182). Also removed the buried-cockpit POI (read as a sphere with panels phasing through it — deleted `world/buriedCockpit.ts` + its `poi.ts` placement + `BURIED_COCKPIT_COUNT`). Pre-tier perf optimizations: `compileAsync` boot pre-warm (fixed the startup freeze from un-shared shader programs), pickup geometry-merging (draw calls 2386→~1150), metal material→uniforms (worked example for the §208 follow-up). **Follow-ups (same-day):** big two-segment wingspan + no-dune-clip flight + flap/glide + shadow + banking; **carcass ecology (D183)** — vultures circle bone ribcages (soaring orbit), lizards/shrews cluster at carcasses, vultures swoop down + grab + carry off prey; **ecology reactions (D184)** — a swooped shrew can burrow to escape (a race), and a circling vulture is attracted to dropped meat/carcass + scavenges it off the ground.

🟡 **[polish] vulture swoop/circle in-MOTION feel (ACAI f/u, foreground-owed D150)** — the circling orbit, swoop dive, and carry climb-away are logic-verified (`vulture-circle`/`vulture-hunt` PASS) but the cadence/feel needs a human: orbit speed/radius/height (`VULTURE_CIRCLE_*`), swoop speed + grab arc (`VULTURE_SWOOP_SPEED`/`GRAB_DIST`), hunt frequency (`VULTURE_HUNT_COOLDOWN`/`HUNT_RADIUS`), carry duration. Also: tune `VULTURE_CIRCLE_COUNT` (how many birds wheel) for atmosphere vs density.

— — — ACAH (2026-06-05) perf + platform follow-ups — — —

[debt] **WebGL perf pass — the next big frame win.** Post-ACAH the pickup-merge halved draw calls (2386→~1150); the WRECKS now dominate (~4900 of ~4900 scene meshes / most remaining draw calls). Levers: InstancedMesh or geometry-merge the wreck hulls + repeated props (scrap/branches/rocks → ~1 draw call each, needs the pickup raycast to handle instanceId), distance LODs for far props, shadow-pass tuning (cut shadow-casters — pickups already castShadow=false; wrecks are the cost). Fold the WRECK side into the next-session mega-wreck rebuild (rebuilding that geometry anyway). Tooling: the `perf-probe` rig-shot scenario + `window.__bootT` boot marks (added ACAH).
[debt] **Finish material-factory → uniforms** (wood/fabric/skin/bone/glass/paint/stone) like metal got in ACAH, to collapse the ~100 compiled shader programs toward ~70 → faster real-GPU startup compile (the headless harness uses software-GL so it can't measure this freeze; `compileAsync` already parallelizes it). Each factory: move its baked `.toFixed()` consts to uniforms + a uniform `uLocalSpace` branch, then drop its `customProgramCacheKey`. metal is the worked example. Lower-instance factories give less benefit (do highest-instance first: wood, skin).
[idea] **Desktop packaging (Electron).** For DISTRIBUTION (standalone/offline/Steam-able game), NOT a perf fix — Electron/Tauri still render the same Three.js→WebGL→GPU path, so ~same FPS. Possible REAL perf edge: discrete-GPU selection on dual-GPU laptops + Chromium flags (vsync/GPU raster) + no background-tab throttling. Prefer Electron over Tauri for WebGL consistency (OS webviews are flakier). See `shared-memory/desktop-packaging.md`. Worth a quick experiment IF low frames are on a dual-GPU laptop.
[idea] **Renderer / engine exploration** (as content + perf demands grow). (a) Three's **WebGPURenderer** — the API-level frame win (better batching, less driver overhead, compute); works IN-BROWSER too, so it's not tied to leaving the web — biggest realistic upgrade short of a rewrite. (b) Native-engine rewrite (Bevy/Godot/Unity, or Rust+wgpu) — large gains but throws away the entire Three.js/TS codebase = a different project; only if the web path is genuinely exhausted. Keep developing on the current stack for now; re-evaluate when frames become the wall.
[note] **Ongoing perf budget** — re-profile with `perf-probe` (draw calls / scene meshes / programs) + `window.__bootT` as content is added each session; the costs that regressed this arc (un-shared shader programs D175/D177, per-instance pickup meshes) are the patterns to watch when adding new world props/materials.

<!-- ACAK (mega-wreck from-scratch dagger + interior) — deferred / owed -->
[bug] **Mega-wreck WALK-TEST re-confirm owed (ACAL).** The user walked ACAK + ACAL fixed the reported issues (exact trimesh collision D189, natural light D190, fracture entrance + ramp, lee-flank panels, de-floated interior D191) — but the FIXES themselves are screenshot-verified, not yet re-walked. `npm run dev`: push into bow/flanks/island/engine-bells (collision shouldn't let you through), walk up the lee sand-ramp into the fracture, confirm both lee-flank salvage panels are flush + pry-able + the journal/shelter register, and judge whether the natural-only interior is too dark. Flag residual floaters with a screenshot (the hunt is down to sub-0.6m nits).
[polish] **Mega-wreck interior — too-dark check + material depth (ACAL).** Lighting is now natural-only (D190); if the interior reads too dark in-app the fix is WIDER openings (fracture/breaches), NOT re-adding fake lights. Then material depth: debris + big planes read as flat single-value metal (want rust/grime/edge-wear — ties to "painted-metal rust" below) + contact-AO/grounding tints at object-to-floor seams. **(ACAP W3 partly addressed this — the shared `createRustedHullMaterial` now has form-AO + warm oxidation-zones + bare-metal scuff-flecks, so the mega-wreck HULL surfaces read weathered-with-depth; the INTERIOR debris/floor planes may still want contact-AO grounding tints.)**
[polish] **Mega-wreck exterior — final dagger polish (ACAK).** Reads as a sleek crashed dagger but the bow is a touch needle-thin; richer mid-span greeble + a downwind debris fan would lift it. Re-run `megawreck-critique`. Lower priority than the interior.
✓ **[debt] Verification — a procgen-wreck rig-shot framer — SHIPPED ACAO.** `__game.spawnProcgenWreckRig(cls, seed)` (deterministic spawn at a fixed anchor + orientation-pin so the detail flank faces the camera) + the `procgen-wreck` rig-shot scenario (reuses the `flagship` find/frame/mesh-count logic, ground-aware framing, `--seeds=` sweep + `--zoom`). Procgen visual work is now screenshot-verifiable. Also fixed a Windows vite-orphan teardown hang in the harness.
[idea] **Real procgen breach HOLES would need a geometry cut (ACAO / D197).** `makeBreach` (decal-style recessed void + flaps, no boolean cut) does NOT read as a hole on small intact procgen hulls (~1m) — the skin occludes the void; proud, it reads as a bump. Kept the flat dark damage patch. If real holes are wanted: loft the hull WITH a gap there (build-in the opening), or add a lightweight CSG step — both bigger than a decal. Lower priority than the flat patch already reading fine.
✓ **[polish] Procgen wrecks — T5 greebles + impact asymmetry — SHIPPED ACAO.** `addHullGreebles` vocabulary 3→7 (louvered vents / circular ports / fins / antenna stubs) + impact-flank asymmetry (~78% of greebles + the damage patch cluster on the hit side) on RIBBED_CYLINDER + PLATED_RECTANGULAR; bury-audit clean. (Real `makeBreach` holes reverted → the [idea] above, D197.) REMAINING (optional): biome-weighted greeble distribution (salt→corrosion, rocky→structural, dune→tanker) — assessed ACAP as low payoff + hard to verify through the single-biome framer; skipped.
🟡 **[debt] Wreck perf — T7 LOD/instancing — MEASURED + mostly cut ACAP.** A `perf-probe` deep-dump (ACAP W1, D198) showed the static-merge already won the wreck draw-call budget (the remaining hand POIs got merged: opening wreck/rockyEntrance/saltOutpost). Distance-LOD of far wrecks is now LOW-ROI. REMAINING (attended) high-value perf: **(a) speeder static-merge** — 86 unmerged meshes, always on-screen, but tag its animated/interactive parts (`headlamp`/`speederSeat`/`speederTowBar`/wheels) `noMerge` FIRST; **(b) pickup InstancedMesh** — 340 branch+scrap meshes ≈ 340 draw calls, but each pickup is individually takeable so it needs an interaction-raycast (`instanceId`) rework.
