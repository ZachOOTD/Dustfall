# Backlog

Unprioritized ideas, bugs, polish, debt. Tags:

- `[bug]` — something broken or visibly wrong
- `[feat]` — new gameplay or mechanic
- `[polish]` — feel, UX, visual refinement
- `[debt]` — code cleanup, refactor, doc improvement
- `[idea]` — speculative, needs validation before promotion

When ready to ship one, promote it into a session in [roadmap.md](roadmap.md).
Use `/triage-ideas` to bulk-classify a free-form dump.

---

<!-- entries below this line — newest at the bottom -->
<!-- (procedural world generation — shipped in AAI/AAK) -->
<!-- (creature companion — shipped in AAE) -->

[feat] base-building mechanics
[idea] remove HUD stat bars in favor of audio/visual/text cues ("you could use a drink")
[feat] multi-worm population — AAP shipped single-worm procgen biome-seeded spawn + noise-scaled detection. Extending to N worms per world needs save-schema bump (currently `sandWorm: { ... } | null` field; would become `sandWorms: [...]`), per-worm min-separation logic, and playtest pass to confirm N>1 doesn't ruin the early game.
[feat] sandworm deeper encounter design — AAP added procgen spawn + noise detection; remaining vision items: more than "sees player → charges" (e.g. ambush state, retreat-and-stalk loop, weakness/feeding behavior, dawn/dusk surfacing).
[feat] satellite dish structural-framework backing geometry — visible from above when player approaches from the convex side; currently uses DoubleSide as a known cheat (documented in satelliteDish.ts AAL comment)
[feat] engine block heat shield back panel — modeled thickness instead of FrontSide-only (AAL switched DoubleSide → FrontSide; back face is mostly occluded but a thin back layer would improve oblique-angle reads)
[idea] stamina tow factor playtest — current 2.0× drain discourages sled travel; audit flagged this; needs in-play signal before tuning
[polish] remaining tutorial coverage gaps — ABA added grill_kit, scrap_bar, large_tent_kit. Still missing: RMB context actions (UU-2 — there's no first-hint for "press RMB on a tent to pack it"), sled cargo (QQ — no hint clarifying that the sled stub is for ropes, the deck is for cargo). Lower priority than ABA additions.
[debt] poi.ts scavenger-camp magic-number sweep — assessed in AAO and deferred. Hand-coded camp has ringR=0.55, stone size 0.10-0.14, ash 0.85, bandage offset 1.0/0.8 etc. — aesthetic one-offs not meaningfully feel-tunable. Best done as part of a future scavenger-camp rework when the POI's design is being revisited anyway. Lifting them now would bloat Tuning without future iteration value.
[polish] scrap_gun reload action — AAN added the .no_ammo empty crosshair; closing the loop needs an actual reload UX (R-key? E-on-gun? consumes scrap_bullet?) + a reload SFX (mechanical clack + bullet-in-chamber click).
[feat] saved companion huddle state — AAO huddle resets on every game load (state field defaults to 'idle' on respawn). Acceptable today; if "huddle persistence across saves" matters (saving in a storm should reload with companion still huddled), needs save schema additive bump.
[feat] texture overhaul — game-wide visual upgrade. Today everything is flat-color MeshLambertMaterial + procedural shaders (terrain rust/sand, hull rust streaks per OO, concrete weathering). Move to PBR-ish material treatment: normal maps on hulls/wrecks for surface detail, subtle albedo variation on cloth (tents/bedroll/canteen), worn-metal masks on weapons. Sources: CC0 PBR (PolyHaven, ambientCG) OR extend the existing procedural shader vocabulary. Decide bundle-cost stance first (current zero-texture-files policy per D3 — would need to revisit). Big atmospheric win; biggest visual upgrade since the procedural shader era (OO).
[feat] salvage mechanics overhaul — AAR/AAS/AAT/AAU shipped tactile pry + extract + variant interiors + per-component loot + electrical glow + condition tiers + rectangular access-panel proportions + recessed integration. REMAINING: (1) per-panel reachability audit — megaWreck engine bell panels at 7.7m and catwalk stub panels at 11.5m are reachable only via catwalk stairs; consider adding ground-level secondary panels for accessibility; (2) thermal-cut tier with fire-starter (alternative pry tool); (3) rare key-card panels gated behind a quest item; (4) salvage durability per-wreck (each wreck has finite mass across all panels; deplete completely → "corpse" wreck dimmed entirely); (5) player ability to "restore" corroded panels via weld kit (inverts D96 — condition becomes player-mutable, must move to save).
[feat] POI overhaul — partial progress. AAQ shipped themed clusters (military_convoy + refugee_caravan). REMAINING: (1) third cluster kind (comm-relay: satellite_dish + tower + debris); (2) per-POI narrative beats (lone-survivor journal entries at hand-placed flagships, hostile raider holdouts, friendly hermit NPCs); (3) biome-specific POI kinds (salt = corroded scientific outpost, rocky = subterranean entrance, dune = buried spaceship cockpit). The (3) item is the largest — would need new POI modules per biome, breaking the AAQ composition-only pattern (D93). (2) is the natural follow-on and reuses existing flagships.
[idea] dynamic POI model generation system — procedurally compose unique POI silhouettes per-seed instead of hand-modeling each kind. Vocabulary of parts (hull pieces, engine bells, antenna struts, breach patches, salvage panels) + composition rules (this kind has a cockpit + tail + N engines arranged on a tilt; that kind is a flat slab with a turret stub on top). Per-seed variation: bigger/smaller, more or fewer breaches, different tilt angles, paint scheme. Goal: every wreck in a procgen world looks distinct without authoring 50 hand-modeled variants. Big architecture lift; would supersede some of the hand-modeled flagship modules (engineBlock.ts, satelliteDish.ts) and the wrecks.ts procgen palette. Needs design pass + scope discussion before code. Speculative.
[feat] cloth physics — real Verlet/spring-mass simulation on the fabric panels for wind-responsive cloth (tent walls, large-tent door, sails). ~400 LOC, 4-6h focused session. Cheap intermediate: vertex-shader wind ripple in `fabricMaterial.ts` (~30 LOC, ~80% of the visual effect at ~5% of the cost). Backlogged from AAY/AAZ where the user explicitly said "defer".
[polish] save / load tracks `flags.devMode` — currently in-memory only (AAX); a dev-saved game continues as a regular game (no badge). Could be additive v10→v11 with `bornInDevMode?: boolean`, or just left as-is (the inventory items survive, which is the gameplay-relevant part).
[debt] backfill changelog entries for sessions where the user committed via direct paste-back without invoking `/session-end` — AAW and AAX were committed mid-conversation in AAY; the changelog now has retrospective entries for them, but in general the print-hints-mode flow risks skipping doc updates. Consider adding a reminder in the print-hints output.
[bug] speeder spin angular damping playtest — ABA shipped angular damping (2.5/s) but the bike body's setEnabledRotations limits to Y only; need to confirm that worldspace tilts after bumps recover correctly. Lower-priority follow-on.
[feat] give all dropped items rigid-body physics — items roll down hills, fall, settle naturally
[feat] generalized rope attachment — anchor either end to bike / world objects / items / arbitrary surface
[polish] texture overhaul for non-cloth/terrain surfaces — metal, rust, rocks, sandworm, lizards, item viewmodels
[polish] item viewmodel fidelity pass — upgrade detail on every inventory-item mesh
[bug] salvage panel placement visual audit — ABA P3 migrated the 4 legacy panels to addAccessPanel via wrapper-Group + my best-guess +Z direction per panel (esp. crashedHull bell-throat panel B, satelliteDish back-of-dish, openingWreck lookAt-positioned). Need an in-game walkthrough across multiple seeds to verify the migrated panels don't float / clip / have door-into-hull orientations. The architectural migration is sound; the per-panel rotations may need 1-2 tweaks per site.
[feat] craftable hover bike — player builds it from found parts instead of starting with one
[feat] interactive opening cutscene — drop-pod descent from orbit through a window view of the desert planet, blinking alarm + parachute-deploy lever in front of player, player hits lever multiple times but it doesn't deploy and snaps off, pod collides with desert + blackout, wake up + open pod door + exit to start the game, scrap pieces scattered around the pod wreck to salvage, craft scrap_bar from them and pry the first salvage panel off own pod, that pry triggers a delayed comedic parachute deploy. Implementation approach: keep the existing CC-3 title screen; on NEW GAME, spawn the player inside a NEW small enclosed pod mesh (single-occupant scale) with a pre-baked descent animation (camera fixed inside the pod, window-mesh + skybox + ground-approach baked into a timeline), NOT a separate scene-swap like the title. Lever, blackout, pod-exit, parachute-deploy are all in-world interactions on the same scene the player will then play in. The existing W-era opening wreck (tapered fuselage with the skeleton + journal + tally marks + salvage panels — RR/AAJ build) STAYS as a separate POI placed near the new pod-crash spawn point — it becomes the first piece of world the player explores after exiting their own pod (the wreck is the previous occupant's larger ship, a narrative beat: "someone else crashed here before me"). The opening-scene anchor + seed-stability rules from AAI still apply.
[polish] mega-wreck rebuild — current megaWreck reads as unrealistic + blocky; remove existing model and redesign with higher fidelity + more interesting silhouette + realistic shipwreck proportions (the BB-2/BB-3-era hand-modeled Jakku-scale shell shipped pre-OO procedural-shader era — visually behind the rest of the world now)
[feat] procgen wreck expansion — ABA shipped first-cut (3 cockpit + 3 hullSegment + 2 engineModule + 2 tailStub variants; 2 recipes: corvette + freighter; 35/65 mix with legacy). Remaining: (1) more part variants per kind (especially hullSegment — currently 3 + paneled_tapered is the workhorse, more silhouettes would help), (2) more wreck classes (gunship, science_vessel, bulk_hauler), (3) biome-bias on recipe pick (salt = corroded short corvettes, dune = freighter), (4) breach patches as a decoration layer (currently absent — would add ragged hull holes per-seed), (5) ramp the composite share past 0.35 once playtest validates visual quality, (6) eventually replace legacy wreck-kind palette OR migrate flagship modules (engineBlock/crashedHull) into the system.
[feat] multiplayer server — networking layer enabling multi-player Dustfall; depends on player model below; see shared-memory/multiplayer-mvp-scope.md for the server-auth-first + snap→interpolate→predict spectrum guidance
[feat] high-fidelity player model — rigged 3D character mesh (currently no visible player body; first-person hands only via viewModel.ts); prerequisite for multiplayer ("see other players") AND for any third-person camera mode
[feat] character customization — color / clothing / accessory variants on the player model; unblocked once the rigged player model exists; opens cosmetic depth for multiplayer identity
