# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Transient screenshot capture from preview-toDataURL workflow
.claude/preview-shot.png

# Playwright rig-shot harness output (scripts/rig-shot.mjs) — dev artifacts
verification/*.png
# Gate-runner output (scripts/verify-chunks.mjs): per-leg tee'd stdout+stderr, and the
# per-leg screenshot dirs the parallel phase gives each leg via RIG_OUT_DIR.
verification/gate-logs/
verification/gate-shots/

# gamedev-framework campaign/overnight runtime state (lock, progress journal,
# cycle in-progress marker). Transient + machine-local — never commit.
.gamedev-framework/
shot-out/

# Z8 throwaway drape/timing diagnostics (verification lives in rig-shot gates + saved PNGs)
scripts/z8-*.mjs
